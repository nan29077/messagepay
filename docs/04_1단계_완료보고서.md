# 1단계(설계 및 Mock MVP) 완료 보고서

작성일: 2026-08-19
대상: 마스터 프롬프트 17항 **1단계 — 설계 및 Mock MVP**

---

## 1. 구현한 기능

### 1-1. 기반
- Next.js 16(App Router) + TypeScript + Tailwind v4 프로젝트 신규 구축 (독립 프로젝트)
- 토네이도 전용 디자인 토큰·공용 컴포넌트 (이모지 미사용, 라인형 아이콘 통일)
- 세션 기반 인증 3롤(후원자 / 크리에이터 / 관리자) + 관리자 감사로그
- PostgreSQL 스키마 **45개 모델 / 28개 enum**, AWS RDS·Aurora 호환 설계
- 무결성 가드 마이그레이션 (정산 원장 append-only 트리거, 부분 유니크 인덱스, CHECK 제약)
- 시드 데이터 (관리자·크리에이터 2명·MO 번호 2종·후원자·약관 6종·FAQ·정책)

### 1-2. 핵심 도메인
- **MO 수신 → 후원 거래 → 결제 → 방송 노출** 전 흐름
- 후원 상태머신 15단계 + 상태 전이 로그
- **중복 결제 4중 방어** (사업자 메시지 ID UNIQUE / 멱등키 / 1회용 링크 / PG 주문번호 재사용 + 승인 부분 유니크)
- 결제 타임아웃 시 **거래결과조회로 최종 상태 확정**
- 한도·속도·연속발송·실패잠금·신규 첫날 한도 정책 엔진 (GLOBAL/CREATOR/DONOR 우선순위)
- 금칙어 + 개인정보(주민번호·카드·전화·계좌·이메일·URL) 자동 마스킹 필터
- 정산 원장(복식 분개) + 수수료 정책 + 정산 요청/지급 + 환불 반대분개
- 1회용 보안링크(해시 저장, 단기 만료, 1회 소비)

### 1-3. 화면 (총 64개 페이지 / 7개 API 라우트)
| 영역 | 내용 |
|---|---|
| 공개 | 메인(크리에이터 코드 입력 중심), 이용방법, FAQ, 공지, 고객센터, 약관 3종, 로그인/회원가입, 크리에이터 신청, 크리에이터 후원 페이지 `/c/{code}` |
| 보안링크 | 계좌 등록(고지·동의 전체), 결제 확인(카운트다운), 등록 완료 |
| 후원자 | 후원내역, 결제내역, 등록계좌·자동출금 해지, 한도 설정, 크리에이터별 차단, 동의 이력, 환불 요청 |
| 크리에이터 | 대시보드, 후원내역+상세, 문자관리(7개 탭), 유튜브 연동, 오버레이 설정·테스트 후원, TTS, 자체방송 키, 후원설정, 금칙어·차단, 신고, 정산·정산계좌, 프로필 |
| 통합 관리자 | 23개 메뉴 (대시보드, 시스템 상태, 회원, 후원자, 크리에이터 심사, 코드, MO 번호, 수신문자, MT발송, 결제, 환불, 한도·이상거래, 유튜브, 방송·스트림, 오버레이·TTS, 정산, 수수료, 한도정책, 배너, 공지·FAQ, 신고·금칙어, 약관버전, 관리자 권한, 감사로그, **MO 시뮬레이터**) |
| 오버레이 | `/overlay/{creatorId}?token=` — SSE 구독, 대기열 순차 재생, TTS, 회오리 애니메이션, 투명 배경 |
| Mock | 헥토 결제창 대체, 구글 동의화면 대체 |

### 1-4. 안전장치
- `SAFE_MODE=true` — 실제 결제 승인·실제 MT 발송 차단
- `ALLOW_DIRECT_TRIGGER=false` — MO 수신 즉시 결제를 코드 레벨에서 강등 (금융사 서면승인 전까지 열리지 않음)
- 미구현 사업자 어댑터는 **예외를 던져** 조용한 mock 대체를 막음
- 운영 전환 경고 점검 `GET /api/health` → `productionWarnings`
- `/admin/simulator` 는 `APP_ENV=prod` 에서 화면·서버액션 모두 차단

---

## 2. 수정·생성한 파일

| 구분 | 규모 |
|---|---|
| 소스 파일 (생성 코드 제외) | 155개 / 약 22,400줄 |
| Prisma 스키마 | 1개 (45 모델) |
| 마이그레이션 | 3개 |
| 테스트 | 1개 파일 / 27개 케이스 |
| 문서 | 4개 (`docs/`) + `README.md` + `CLAUDE.md` |

주요 디렉터리
```
prisma/            schema.prisma, migrations/*, seed.ts
src/lib/           env, crypto, id, money, datetime, labels, logger
src/server/        db, auth, redis
src/server/adapters/   mo, mt, payment, youtube, tts, stream  (전부 인터페이스 + mock)
src/server/services/   donation-flow, donation-confirm, donor-registration, refund,
                       settlement, limits, content-filter, secure-link, idempotency,
                       broadcast-dispatch, overlay-bus, mt-templates
src/app/           공개 / r / my / studio / admin / overlay / mock / api
src/components/    ui, layout, brand, public, my, studio, admin, overlay
```

---

## 3. 테스트 결과

### 자동 테스트 — **27개 전부 통과**
`npm test` (Vitest + 실제 PostgreSQL)

| # | 시나리오 | 결과 |
|---|---|---|
| 1 | 미등록 번호 최초 MO → 결제 없음 + 등록 안내 MT | 통과 |
| 2 | 등록 링크 만료 | 통과 |
| 3 | 계좌 등록 필수동의 누락 실패 / 성공 시 빌키 저장 / 계좌 원문 미저장 / 링크 재사용 불가 | 통과 |
| 4 | 등록 사용자 정상 후원 → 결제·수수료·정산·방송 노출 | 통과 |
| 5 | 동일 MO Webhook 3회 재전송 → 거래 1건, 승인 1건 | 통과 |
| 6 | 짧은 시간 연속 발송 속도 제한 | 통과 |
| 7 | 일일 한도 초과 → 결제 없음 + 안내 MT | 통과 |
| 8 | 결제 타임아웃 → 조회로 승인/실패 확정 | 통과 |
| 9 | 결제 실패 → 방송 미노출 + 실패 MT | 통과 |
| 10 | 유튜브 연결 없음 → 결제 유지, 전송만 SKIPPED | 통과 |
| 11 | 방송 종료 상태 → 결제 유지, 전송 SKIPPED | 통과 |
| 12 | 금칙어 포함 문자 차단 | 통과 |
| 13 | 개인정보 포함 문자 자동 마스킹 | 통과 |
| 14 | 크리에이터 차단 후원자 | 통과 |
| 15 | 환불 → 정산 반대분개, 잔액 차감 | 통과 |
| 16 | 정산 원장 UPDATE/DELETE 차단 | 통과 |
| 17 | 정산 요청 한도 초과 거절 / 지급 시 원장 반영 | 통과 |
| 18 | 알 수 없는 수신번호 | 통과 |
| 19 | 결제 실패 3회 → 후원자 자동 잠금 | 통과 |
| 20 | 유튜브 할당량 소진 → 전송 실패, 결제 유지 | 통과 |
| 21~23 | CONFIRM_LINK 모드: 확인 전 미결제 / 두 번 클릭해도 1회 결제 / 시간 초과 자동 취소 | 통과 |
| 24 | DIRECT_TRIGGER 안전 강등 | 통과 |
| 25~26 | 대표번호+키워드 라우팅 / 키워드 없으면 미라우팅 | 통과 |
| 27 | 보안링크 토큰 원문 미저장 | 통과 |

### 수동 · 브라우저 검증
- `npm run typecheck` 통과, `npm run build` 성공 (64 페이지 + 7 API)
- 공개·스튜디오·관리자 **전 라우트 HTTP 200** 확인 (관리자 25개, 스튜디오 13개)
- 실제 브라우저 E2E: 최초 MO → 등록 링크 → 동의 → 모의 결제창 → 등록 완료 → 2차 MO → 확인 링크 → 결제 → `BROADCASTED`(MT/유튜브/오버레이 모두 SENT) → 정산 원장 3분개 확인
- 오버레이 브라우저 소스에서 **실시간 후원 알림 수신** 확인 (SSE, 마스킹된 표시명·금액·메시지·감사 스티커)
- MO Webhook 서명 검증: 서명 없는 요청 401, 서명 있는 요청 정상 처리, 재전송 DUPLICATE

---

## 4. 아직 Mock 인 기능

| 기능 | 현재 | 실연동 조건 |
|---|---|---|
| 헥토파이낸셜 내통장결제 | Mock 결제창 + Mock 승인/조회/취소 | 가맹점 계약, MID·라이선스·AES·HASH 키, 테스트베드 계정, **문자 트리거 방식 서면 승인** |
| MO 문자 수신 | Mock 어댑터 (서명 검증 로직은 실제와 동일) | MO 사업자 계약, 수신번호 배정, Webhook 스펙, 허용 IP |
| MT 문자 발송 | 인메모리 outbox | MT 사업자 계약, 발신번호 사전등록(KISA), 단가 계약 |
| 유튜브 라이브 채팅 | Mock 방송/채팅 | Google Cloud 프로젝트, OAuth 동의화면 검증, **할당량 증설 승인** |
| TTS | 브라우저 SpeechSynthesis | 상용 TTS 계약(자연스러운 한국어 다화자) |
| 자체 방송(RTMPS/HLS) | 키 관리 UI 만 | 미디어 서버 인프라, CDN, 대역폭 계약 |
| 본인인증(성인 확인) | 동의 체크만 | 본인확인기관 계약 |
| 정산 계좌 인증 | `verified=false` 고정 (관리자 수동) | 계좌실명조회 API |
| 생성형 배너 이미지 | SVG 플레이스홀더(문자 없음) | 이미지 생성 후 S3 에셋으로 교체 |
| 관리자 2단계 인증 | 스키마 필드만 준비 | 5단계 구현 |

---

## 5. 실제 계약 또는 API 키가 필요한 기능

착수 순서 기준 우선순위입니다.

1. **헥토파이낸셜** — 가맹점 계약 + MID/라이선스/AES/HASH + 테스트베드. **문자 수신을 결제 트리거로 쓰는 구조에 대한 서면 승인**이 가장 중요합니다.
2. **MO 사업자** — 수신번호 방식(전용번호 vs 대표번호+키워드) 결정 후 계약. 번호 원가가 직접적인 손익 요인입니다.
3. **MT 사업자** — 발신번호 사전등록, SMS/LMS 단가.
4. **Google Cloud / YouTube** — OAuth 클라이언트 + 동의화면 검증 + **할당량 실측 및 증설 신청**(1주차 최우선 검증 과제).
5. **AWS** — RDS/Aurora, ElastiCache, S3, KMS, Secrets Manager.
6. TTS, 본인인증, 계좌실명조회, 미디어 인프라.

---

## 6. 남아 있는 위험 (1차 보고서 이후 변동)

| 위험 | 상태 |
|---|---|
| R1 MO 문자 결제 트리거 승인 | **미해소.** 코드 레벨에서 `CONFIRM_LINK` 고정 + `DIRECT_TRIGGER` 잠금으로 방어 중 |
| R2 유튜브 할당량 | **미해소.** 할당량 가드·큐·경고 UI 구현 완료. 실측 필요 |
| R3 중복 결제 | **완화.** 4중 방어 구현 + 전용 회귀 테스트 상시 실행 |
| R4 미성년자 결제 | **부분 완화.** 만 19세 확인 동의 필수화. 본인인증 실연동 필요 |
| R5 정산 자금 보유 구조 | **미해소.** 법률·PG 자문 필요 |
| MO 번호 원가 | **완화.** 대표번호+키워드 모드 병행 구현 |
| 개인정보 방송 노출 | **완화.** 자동 감지·마스킹 + 원문 암호화 보관 |

---

## 7. 다음 단계 작업 (2단계 — 실제 문자·결제 연동)

1. 헥토파이낸셜 실 어댑터 구현 (`src/server/adapters/payment/hecto.ts`)
   - 결제창 파라미터 암호화(AES) / 해시 검증
   - 0원 인증 후 빌키 발급, 승인·조회·취소
   - **결제인증 후 10분 제한**에 맞춘 확인 링크 TTL(현재 300초) 재검증
2. MO 사업자 실 어댑터 + Webhook 스펙 매핑 + 허용 IP 등록
3. MT 사업자 실 어댑터 + 템플릿 사전 등록(필요 시) + 실패 재발송 큐
4. BullMQ 워커 분리 (결제 재시도 / 유튜브 전송 / MT 재발송 / 확인 만료 정리 배치)
5. 테스트베드 전 시나리오 회귀 (중복 결제 0건 확인)
6. 관리자 한도 정책 운영 값 확정

이후 3단계(유튜브 실연동), 4단계(자체 방송), 5단계(정산·운영 고도화) 순서로 진행합니다.
