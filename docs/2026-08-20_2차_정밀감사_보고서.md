# 메시지페이(MESSAGEPAY) 2차 정밀 감사 보고서

- 일자: 2026-08-20
- 범위: 후원 로직·프로세스 / 정산 로직·프로세스 / 회원가입·인증 / 통합 관리자 기능 / 앱 전반·코드 품질
- 방식: 5개 영역 병렬 코드 감사 + 핵심 치명 항목 실행 재현 검증
- 표기: `[검증됨]` = 실제 실행/재현으로 확인, `[추정]` = 코드상 강한 정황이나 실환경 확인 필요

> ⚠️ 이 보고서는 **점검·보고**용입니다. 수정은 지시를 받은 뒤 진행합니다. 아래 우선순위(P0~P3)는 권고안입니다.

---

## 0. 총평

핵심 결제·정산·개인정보 설계(멱등 3중 방어, 확인링크 원자적 1회 소비, append-only 원장 + DB 트리거, 전화번호 3분리 저장, 1회용 비밀값 해시 저장)는 견고합니다. 그러나 이번 감사에서 **운영을 즉시 마비시키는 치명 결함 4건**과, **금전·개인정보에 직접 영향을 주는 높음 결함 다수**가 확인되었습니다. 특히 두 건은 실제 실행으로 재현했습니다.

- **감사로그가 금액이 포함되면 무조건 실패** → 환불·정산·한도 등 돈을 다루는 관리자 액션이 "오류"로 표시되고 기록이 안 남음 `[검증됨]`
- **정산 계좌를 "인증 완료"로 만드는 코드가 아예 없음** → 원장에 잔액이 쌓여도 정산 요청이 영구 불가 `[검증됨]`

여러 감사관이 **독립적으로 중복 지적**한 항목(신뢰도 높음)은 각 항목에 `★중복지적` 으로 표시했습니다.

---

## 1. 치명 (P0 — 운영 차단 / 금전 사고 직결)

### C-1. 감사로그 저장이 BigInt에서 예외 → 금액 관련 모든 관리자 액션이 실패로 표시 `[검증됨]`
- 위치: `src/server/auth.ts:writeAudit` (`beforeValue/afterValue` 를 Json 컬럼에 저장)
- 호출부: `admin/transactions.ts:46,137,193`(monthlyCost·amount), `admin/accounts.ts`(minAmount/maxAmount/donationAmount), `admin/settlement.ts`, `admin/policy.ts` 등
- 내용: `after: { amount: 3000n }` 처럼 BigInt가 담긴 객체를 Prisma Json 컬럼에 저장하면 `JSON.stringify` 단계에서 `TypeError: Do not know how to serialize a BigInt` 가 발생합니다. 실행 재현 결과 `prisma.adminAuditLog.create()` 가 예외를 던지는 것을 확인했습니다.
- 실패 시나리오: 관리자가 환불 "승인" → PG 취소·원장 반대분개는 이미 완료 → `writeAudit`에서 예외 → `run()`이 잡아 화면에 "처리 중 오류가 발생했습니다" 표시. 관리자는 실패로 알고 **재클릭**(→ C-3 이중환불로 연결), 감사로그에는 아무 기록도 남지 않습니다.
- 영향 범위: MO 번호 등록, 후원자 한도 저장, 1건 후원금 범위 변경/일괄적용, 정산 상태 변경, 수수료·한도 정책 저장, 환불 승인/직접환불 등 금액을 audit에 넣는 액션 전부.
- 권고: `writeAudit` 내부에서 BigInt→string 정규화(`JSON.parse(JSON.stringify(v, (_,x)=>typeof x==='bigint'?x.toString():x))`) 후 저장. 미지정 시 `null` 대신 `Prisma.DbNull` 사용.

### C-2. 정산 계좌를 `verified=true`로 전환하는 경로가 존재하지 않음 → 정산 구조적 불가 `[검증됨]`
- 위치: `src/server/services/settlement.ts:createSettlementRequest`(인증 계좌 요구), `src/app/actions/studio.ts:saveSettlementAccountAction`(항상 `verified:false` 저장)
- 내용: 전 코드베이스에서 `settlementAccount` 의 `verified` 를 `true`로 **쓰는** 코드가 없습니다(검색 결과는 모두 `select`). 관리자 계좌 관리 액션(`admin/accounts.ts`)에도 계좌 인증 처리가 없습니다.
- 실패 시나리오: 크리에이터가 계좌 등록 → 관리자 화면엔 "미인증" 배지만 있고 전환 버튼 없음 → 정산 요청 시 항상 "정산 계좌 인증이 완료되지 않았습니다"로 실패. 원장 잔액이 아무리 쌓여도 출금 불가.
- 권고: 관리자용 `verifySettlementAccountAction`(FINANCE 권한 + `writeAudit`) 신설. 계좌 변경 시 자동 미인증 전환(현 동작)은 유지.

### C-3. 환불 승인에 상태 가드·직렬화 락 부재 → 이중 환불·이중 반대분개 `★중복지적(후원/정산/관리자)`
- 위치: `src/app/actions/admin/transactions.ts:approveRefundAction`, `src/server/services/refund.ts:approveRefund`
- 내용: `rejectRefundAction`은 `status !== 'REQUESTED'` 를 검사하지만 `approveRefundAction`엔 없음. `approveRefund`도 `DONE`만 조기반환하고 `REJECTED/FAILED` 재승인을 막지 않으며, `withAdvisoryLock`·조건부 UPDATE도 없음.
- 실패 시나리오: (a) 거절된 환불을 다른 탭에서 승인 → PG 취소 실행 + 환불 분개 추가. (b) 이중 클릭/동시 처리 → `adapter.cancel` 2회, `postRefundSettlement` 2회 → append-only 원장에 환불 분개 중복(되돌리기 불가) + `rollbackCounters` 2회로 카운터 음수화(한도 추가 개방). C-1과 결합 시 관리자가 "오류"로 오인해 재클릭하며 발생 확률이 높음.
- 권고: `approveRefund` 전체를 `$transaction + withAdvisoryLock('refund:'+id)` 로 감싸고 진입 시 `status==='REQUESTED'` 만 허용.

### C-4. 확인대기(PENDING_CONFIRM) 건이 한도에 예약되지 않아 일/월 한도 무제한 우회 `[추정]`
- 위치: `src/server/services/limits.ts:checkLimits`(카운터만 읽음), `donation-flow.ts:commitCounters`(결제 성공 시에만 증가), `executePayment`(결제 직전 한도 재검사 없음)
- 내용: CONFIRM_LINK 모드에서 카운터는 결제 성공 시점에만 증가. 확인 대기 중인 건은 어떤 한도에도 안 잡히고, `executePayment`는 한도를 다시 확인하지 않음.
- 실패 시나리오: 일 한도 10만원 후원자가 60초마다 5만원 문자 3건씩 반복 → 매번 카운터 0이라 전부 통과 → 확인 링크 30장 발급 → 순차 클릭 시 150만원 결제(한도 15배).
- 권고: PENDING_CONFIRM 생성 시 예약(pending) 카운터 증가(만료/실패 시 롤백), 또는 `executePayment` 진입 직후 `checkLimits` 재실행.

---

## 2. 높음 (P1 — 금전/개인정보/보안 직접 영향)

### H-1. `APP_ENV` 미설정 시 비밀번호 없는 최고관리자 로그인이 공개 노출 (fail-open) `★중복지적(회원/앱전반)`
- 위치: `src/lib/env.ts:27`(`APP_ENV` 기본값 `'local'`), `actions/auth.ts:testLogin`, `login/page.tsx`
- 내용: 테스트 로그인 게이트가 `env.appEnv !== 'prod'` 하나뿐인데 기본값이 `local`. 운영에서 `APP_ENV`만 누락하면 `/login`에 "최고관리자로 로그인" 버튼과 시드 비밀번호(`messagepay1234!`)가 노출되고 클릭 한 번에 `/admin` 전권.
- 권고: 게이트를 `appEnv === 'local'` 화이트리스트로 뒤집고 `NODE_ENV==='production'`이면 무조건 차단. `assertProductionSafety()`를 부팅 시(`instrumentation.ts`) 호출해 위반 시 기동 중단.

### H-2. `SESSION_SECRET`/`PHONE_HASH_SECRET` 하드코딩 기본값으로 조용히 동작 → DB 유출 시 전화번호 전량 복원
- 위치: `src/lib/env.ts:40-41,124-133`
- 내용: 두 시크릿에 코드 기본값이 있고 `assertProductionSafety()`는 예외를 던지지 않고 문자열만 반환. 한국 휴대폰 공간(10^8)은 기본 시크릿을 알면 전수 HMAC 계산으로 `phone_hash` 전량을 평문 번호로 복원 가능(3분리 저장 무력화).
- 권고: 시크릿 기본값 제거하고 미설정 시 모듈 로드 단계에서 즉시 throw. 세션용/검색용 키 분리.

### H-3. `requireCreator()`가 승인 상태를 검사하지 않음 → 미승인·정지 크리에이터가 스튜디오 전체 사용 `★중복지적(회원/앱전반)`
- 위치: `src/server/auth.ts:requireCreator`(creatorId 존재만 확인), `studio/layout.tsx`
- 내용: PENDING/REJECTED/SUSPENDED 프로필도 통과. `/creator-apply`는 신청 즉시 role을 CREATOR로 승격(승인 전)하므로, 아무나 신청 후 정산 계좌 등록/정산 요청/스트림키·오버레이 토큰 재발급 가능. 정지해도 세션·기능 유지.
- 권고: `requireCreator()`가 `status==='APPROVED'`를 확인. 정지/반려 전환 시 세션 무효화. role 승격은 승인 시점으로 이동.

### H-4. 웹(PC) 후원 멱등키가 제출마다 새로 생성 → 이중 출금 `★중복지적(후원/앱전반) [검증됨: 코드 흐름]`
- 위치: `src/components/public/web-donation-panel.tsx`(onSubmit에서 `requestId`를 매번 `crypto.randomUUID()`), `web-donation.ts:createWebDonation`
- 내용: 서버 멱등키 `web:{creatorId}:{donorId}:{requestId}`의 requestId가 재제출마다 달라져 `acquireIdempotency`가 항상 통과. 서버측에 후원자·크리에이터·금액 단위 중복 차단 없음.
- 실패 시나리오: 응답 지연 시 사용자 재클릭/새로고침 재제출 → 서로 다른 requestId로 두 건 생성 → 두 번 출금.
- 권고: requestId를 "금액·메시지 확정 시점"에 1회 생성해 상태로 보관, 성공 응답 후에만 회전. 또는 `(donorId,creatorId,amount,msgHash,60초버킷)` 단기 중복 차단.

### H-5. MO Webhook 서명 시크릿 미설정 시 무인증 통과(fail-open) + 운영 점검 목록 누락
- 위치: `src/server/adapters/mo/index.ts:40`(`!webhookSecret → {ok:true}`), `env.ts:assertProductionSafety`
- 내용: 시크릿 없으면 서명 검사 스킵. 남은 방어 `MO_ALLOWED_IPS`는 `x-forwarded-for` 첫 값이라 신뢰 프록시 없으면 헤더 위조로 통과. `assertProductionSafety`는 이 시크릿을 점검하지 않음.
- 실패 시나리오: 위조 웹훅으로 피해자 번호 명의의 확인 문자 유발(피싱) 또는 임의 DonorProfile·등록 링크 대량 생성. `ALLOW_DIRECT_TRIGGER=true`면 즉시 출금.
- 권고: 시크릿 없으면 fail-closed. `assertProductionSafety`에 필수 검사 추가, 신뢰 홉 기반 IP 추출.

### H-6. 웹 후원 표시명 이중 마스킹 → 방송·후원샵에 `***`로 표시 `★중복지적(후원/앱전반) [검증됨: 코드 흐름]`
- 위치: `src/server/services/web-donation.ts:102` (`maskPhone(donor.phoneMasked)`)
- 내용: `phoneMasked`(`010-****-5678`)를 `maskPhone`에 재투입 → 숫자만 남겨 `0105678`(7자리) → 9자리 미만 분기로 `'***'` 반환. MO 후원은 원본 번호를 마스킹하므로 채널별 결과가 다름.
- 권고: `donor.displayName || '익명의 후원자'` 로 대체(이미 마스킹된 값 재가공 금지). MO/웹 마스킹 로직 통일.

### H-7. 로그인 라우트 CSRF 방어 부재 → 강제 로그인/세션 주입
- 위치: `src/app/api/auth/login/route.ts`
- 내용: 라우트 핸들러는 Next 서버액션의 Origin 검증을 못 받는데 cross-origin 폼 POST를 받아 `Set-Cookie` 발급. Origin/Referer/CSRF 토큰 검증 없음.
- 실패 시나리오: 피해자가 악성 페이지 방문 → 공격자 세션 쿠키 주입 → 피해자가 `/my/account`에서 본인 번호 인증하면 피해자 DonorProfile이 공격자 계정에 연결(후원·결제·계좌 정보 열람).
- 권고: 로그인/로그아웃 라우트에서 Origin을 `env.baseUrl`과 대조하거나 서버 액션으로 전환.

### H-8. SMS 인증번호를 `Math.random()`으로 생성 `★중복지적(후원/회원/앱전반)`
- 위치: `web-donation.ts:randomCode`, `phone-link.ts`, `donation-lookup.ts`
- 내용: 결제 승인·타인 후원내역 조회·번호 연결을 여는 6자리 코드가 CSPRNG 아님. 저장·비교는 HMAC+timingSafe로 견고한데 엔트로피 원천만 약함.
- 권고: 전부 `crypto.randomInt(100000, 1000000)` 으로 교체.

### H-9. 계좌 등록 완료 처리에서 `registrationId` 소유권 미검증 + 결제사 응답 위조 방지 부재
- 위치: `donor-registration.ts:completeRegistration`, `r/[token]/complete/page.tsx`
- 내용: 링크의 `ctx.donorId`와 쿼리스트링 `registrationId`의 소유 일치를 확인하지 않고 상태 변경. `startRegistration`이 저장한 `providerTid`와 복귀 `tid` 대조·결제사 서명 검증도 없음. GET 렌더링 중 1회용 링크를 소비(프리페치·프리뷰 크롤러가 링크 소진).
- 권고: `registration.donorId===ctx.donorId && status!=='COMPLETED'` 검증, `tid` 대조, 부수효과를 명시적 POST로 이동.

### H-10. 고객센터 문의 rate limit 부재 + 신고자 userId가 크리에이터 화면에 노출
- 위치: `src/app/actions/support.ts`, `studio/reports/page.tsx`
- 내용: 비로그인 호출 가능한데 속도 제한 없음(`inquiry.ts`는 10분 10건 있어 불일치). `content`에 `[회원 문의 / userId=...]`를 박아 넣어 `/studio/reports`에 그대로 노출("신고자 정보 미표시" 안내와 모순).
- 권고: IP/사용자 단위 제한 추가, userId는 별도 컬럼에 저장해 관리자만 조회.

### H-11. 후원자 "후원 차단"이 실제로는 아무것도 차단하지 않음 `[검증됨: 코드 흐름]`
- 위치: `actions/donor.ts:toggleCreatorBlock`(`donorCreatorLink.blockedAt`만 갱신), 결제 경로는 `blockedDonor`/`donorProfile.blockedAt`만 조회
- 내용: `/my/blocks`가 갱신하는 `donorCreatorLink.blockedAt`을 읽는 결제 경로가 전무. 후원자가 차단해도 문자·웹 후원이 그대로 접수·출금.
- 권고: `checkLimits`에 `blockedByDonor`(링크 조회) 반영 또는 후원자 차단도 `blockedDonor` 행 생성으로 통일.

### H-12. 정책·수수료·약관 resolver가 `effectiveFrom/effectiveTo`를 무시 → 예약 등록이 즉시 시행 `★중복지적(후원/정산/관리자)`
- 위치: `settlement.ts:resolveFeePolicy`, `limits.ts:resolvePolicy`, `terms/privacy` 조회
- 내용: `where:{active:true}` + `orderBy effectiveFrom desc`로 첫 행 선택. `effectiveFrom<=now`, `effectiveTo` 조건 없음. "9/1부터" 예약 등록이 즉시 적용되고 원장에 잘못된 수수료가 확정 기록(append-only라 반대분개로만 정정).
- 권고: `effectiveFrom:{lte:now}` + `OR:[{effectiveTo:null},{effectiveTo:{gt:now}}]` 추가.

### H-13. 환불 수수료 환입을 "현재" 정책으로 재계산 → 원 분개와 금액 어긋남 `★중복지적(후원/정산)`
- 위치: `refund.ts:70`(`calculateFees(creatorId, refund.amount)`), `settlement.ts:postRefundSettlement`
- 내용: 원 거래 확정 수수료(`donation.pgFee/platformFee`)를 쓰지 않고 환불 시점 정책으로 재계산. 요율 변경 후 환불하면 크리에이터 잔액에 설명 불가능한 잔여 발생. `/admin/fees`의 "소급 안 됨" 안내와 모순.
- 권고: `postRefundSettlement`가 `donationId`의 기존 분개 합계를 환입하도록 변경.

### H-14. `vatIncluded`/`smsCost`가 정산 계산에 미반영(입력·표시만 됨)
- 위치: `settlement.ts:calculateFees`(pgFee/platformFee만 사용), schema/`admin/fees`/`studio/settlement`에서 입력·표시
- 내용: 문자 원가·부가세 필드가 DB·UI엔 있으나 어떤 계산에도 안 쓰임. 크리에이터에게 고지한 명세와 실제 차감 불일치.
- 권고: 계산에 반영하거나(별도 entryType 권장) 스키마·UI에서 제거.

### H-15. 정산 요청 목록에 페이지네이션(skip) 없음 → 오래된 요청 처리 불가 `★중복지적(정산/관리자)`
- 위치: `admin/settlements/page.tsx`(요청 목록 `take`만, `Pager`는 원장에만 연결)
- 내용: PAGE_SIZE 초과 요청은 화면에서 밀려나 승인/반려 접근 불가. 요청시각 내림차순이라 **가장 오래 기다린 건**부터 잠김. 해당 금액은 `pending`으로 크리에이터 `available`에서 영구 차감.
- 권고: 요청/원장 목록에 각각 `skip`+별도 Pager, 기본 정렬 오래된 순.

### H-16. `/api/health`가 무인증으로 DB/Redis 원문 에러·운영 설정 노출
- 위치: `src/app/api/health/route.ts`
- 내용: 인증·rate limit 없이 예외 메시지(호스트·포트·DB명·사용자명 포함) + `productionWarnings` + provider 모드 반환. `error.tsx`가 이 URL을 사용자에게 링크로 안내.
- 권고: 공개 응답은 `{ok:boolean}`로 축소, 상세는 관리자 세션/내부망 전용.

### H-17. 지급 시점에 정산 가능금·계좌 인증 재검증 없음 → 승인 후 환불 시 초과 지급
- 위치: `settlement.ts:markSettlementPaid`
- 내용: 요청 시점에만 `available` 검사. 승인~지급 사이 환불/조정 미반영. `available`은 음수면 0으로 clamp되어 화면에서 초과 상태가 숨겨짐.
- 권고: `markSettlementPaid` 내부에서 요약 재조회 후 `balance>=req.amount` + 계좌 verified 재검증.

### H-18. 정산 요청 상태 전이 TOCTOU + `markSettlementPaid`의 APPROVED 미검증 `★중복지적(정산/관리자)`
- 위치: `admin/settlement.ts:updateSettlementRequestStatus`, `settlement.ts:markSettlementPaid`
- 내용: before 조회와 update가 별 트랜잭션, update에 상태 조건 없음. `markSettlementPaid`는 `PAID`만 검사하고 `APPROVED` 재확인 안 함(락은 잡지만 검증 없어 무의미). 동시 "지급"·"반려" 시 반려 건에 실제 지급 가능.
- 권고: 전이를 `$transaction+advisory lock`으로 감싸고 `updateMany({where:{id,status:expectedFrom}})`의 count로 충돌 판정, `markSettlementPaid`에서 `status!=='APPROVED'` throw.

### H-19. 캘린더 `PAID_STATUSES`가 공용 상수와 달라 월 합계 과소집계 `★중복지적(정산/앱전반)`
- 위치: `studio/settlement/page.tsx:20`(로컬 4종) vs `components/studio/shared.tsx`(공용 6종)
- 내용: `PAYMENT_SUCCESS`, `BROADCAST_PENDING`(이미 결제 승인·원장 기록됨) 누락. 송출 지연 건이 원장 잔액엔 있는데 캘린더·월합계엔 빠져 "합계 300만인데 가능금 340만" 모순.
- 권고: 로컬 상수 삭제하고 공용 상수 import.

---

## 3. 중간 (P2 — 데이터 정합/운영 편의/UX)

- **M-1. 환불 반대분개가 트랜잭션 밖 + `(donation_id,entry_type)` 유니크 없음** — 결제 승인 후 프로세스 사망 시 원장 결손, 동시 요청 시 `DONATION_GROSS` 이중 INSERT로 잔액 2배(삭제 불가). 원장 무결성 근간. (정산) `[중간~높음 경계]`
- **M-2. `ADJUSTMENT`(조정) 분개 생성 액션 부재** — 두 관리자 화면이 "조정 분개로 정정하라" 안내하지만 생성 코드 없음. 오류 발생 시 프로덕션 DB 직접 INSERT 외 방법 없음(audit 미기록). (정산/관리자)
- **M-3. `markInquiryRead`가 무인증 공개 서버 액션** — `run()` 없이 export. 임의 호출로 미읽음 큐 조작. (회원/관리자 `★중복`)
- **M-4. `applyGlobalAmountBounds` updateMany에 where 없음** — 탈퇴·반려·정지 크리에이터까지 일괄 변경, count 표기도 부정확. (관리자)
- **M-5. 크리에이터 정지·MO 회수 후에도 발송된 확인 링크로 결제 완료** — `loadConfirmContext`가 크리에이터 상태·번호 배정 재검사 안 함. (관리자/후원)
- **M-6. MO 번호 사용중지/재고복귀 시 creatorId 잔존** — 과거 배정자가 남은 채 타 크리에이터 재배정 가능, 이력 오염. (관리자)
- **M-7. 환불 거절이 원상태 무시하고 `SETTLEMENT_PENDING`으로 복원** — `SETTLED` 건도 미정산으로 재분류. (관리자)
- **M-8. 대시보드 "한도 차단" 타일 숫자와 링크가 다른 데이터** — 숫자는 Donation 기준, 링크는 `mo-messages?result=BLOCKED`(항상 0건). (관리자)
- **M-9. 정책·시드·화면 곳곳 `effectiveFrom` 미필터로 약관 시행 전 노출** — `terms/privacy`가 미래 버전 즉시 공개, 동의 이력은 구버전 참조. (회원/앱전반)
- **M-10. 회원가입 시 `ConsentRecord` 미기록** — 이메일 가입·크리에이터 신청 동의가 DB에 안 남음(개인정보보호법 동의 증빙). (회원)
- **M-11. 이메일 소유권 인증 부재 + 계정 열거 + 가입 rate limit 없음** — 피해자 이메일 선점 가입 가능. (회원)
- **M-12. 크리에이터 신청 비원자적(트랜잭션 없음) + 신청 즉시 role 승격** — 부분 실패 시 role=CREATOR·프로필 없는 영구 잠김 계정. 반려 후 role 미복원으로 후원자 탈퇴도 불가. (회원)
- **M-13. 전화번호 연결 TOCTOU** — `update`의 where에 소유 조건 없어 동시 연결 시 타 계정 DonorProfile 강탈. (회원)
- **M-14. 탈퇴·번호 재사용 시 이전 사용자 동의 이력이 새 사용자에게 노출** — `/my/consents`가 `phoneHash` OR 조회. (회원)
- **M-15. 스튜디오 대시보드 mock 안내가 하드코딩** — 실연동 후에도 "모의 모드" 계속 표시. (앱전반)
- **M-16. 홈 화면 이용 한도 수치 하드코딩** — 정책 DB와 어긋남("1분 내"도 velocityWindowSec 단정). (앱전반)
- **M-17. `error.tsx`가 최종 사용자에게 `.bat` 실행 안내** — 운영 노출 시 백엔드 구성 유출, 브랜드 색상도 상이. (앱전반)
- **M-18. `--font-pretendard` 미정의로 폰트 스택 전체 무효화** — 브라우저 기본 글꼴 적용. `[추정: 렌더 환경별]` (앱전반)
- **M-19. 결제 확인 카운트다운 하이드레이션 불일치** — `Date.now()` 초기값 렌더로 경고·시간 튐(출금 화면). (앱전반)
- **M-20. 후원확인 시트 "10분 뒤 자동 종료" 미구현 + 세션 KV 데드코드** — 공용 PC에 결제내역 무기한 잔존. (앱전반)
- **M-21. 공개 복사 버튼에 execCommand 폴백 없음** — 비-HTTPS/인앱브라우저에서 번호·키워드 복사 항상 실패(키워드형 후원 처리 불가). (앱전반)
- **M-22. 이용방법에 PC 웹 후원 절차 없음** — PC 후원샵 실제 흐름과 안내 문서 불일치. (앱전반)
- **M-23. 서버 액션 catch가 Prisma 원문 예외를 화면 노출** — `withCreator` 등. (앱전반)
- **M-24. `calculateFees`의 net clamp가 원장과 어긋남** — 소액 후원에서 `netAmount≠원장 기여분`. (정산)
- **M-25. 트랜잭션 안에서 전역 prisma로 요약 조회** — 정산 요청 동시 유입 시 커넥션 풀 자기교착. (정산)
- **M-26. 원천징수 3.3% 계산이 국세청 분리방식과 다름** — 소득세/지방세 분리·10원 절사 미적용. (정산)
- **M-27. `deactivateFeePolicy`에 SUPPORT 권한 가드 누락** — 짝 액션엔 있음. (정산/관리자)
- **M-28. 목록 화면 통계 타일이 필터·기간 미반영** — payments/refunds/mo-messages/mt-messages. (관리자)
- **M-29. 활성 전역 한도 정책 1개 제약이 toggle로 우회** — 활성 GLOBAL 2개 공존 가능. (관리자)
- **M-30. 관리자 직접 환불 비원자적** — 실패 시 `REFUND_REQUESTED` 고착(승인·거절 불가). (관리자)
- **M-31. 결제 완료 매출 집계가 `REFUND_REQUESTED` 제외** — 환불 심사 중 매출이 사라졌다 거절 시 소급 복귀. (관리자)
- **M-32. TTS/오버레이/방송/유튜브 관리 화면 무제한 조회** — take·페이지네이션 없음, 유튜브 실패 집계도 300건 한정. (관리자)
- **M-33. 후원자 검색이 마스킹 문자열 부분일치** — 실제 번호로 조회 불가(`phoneHash` 정확매칭 미사용). (관리자)
- **M-34. 크리에이터 상세 사업자등록번호 원문 노출** — 마스킹 원칙과 불일치. (관리자)
- **M-35. 대시보드가 WEB/MO 채널 미구분** — "오늘 문자 접수"에 웹 후원 포함. (관리자)
- **M-36. `/api/inquiry` GET이 DB 쓰기 수행 + 15초 폴링** — 프리페치로 오읽음, 쓰기 부하. (앱전반)
- **M-37. 시드가 Banner 미생성** — 4개 배너 영역 항상 빈 상태. (앱전반)
- **M-38. 다수 목록 페이지 take/페이지네이션 없음** — faq/notice/my-blocks/moderation. (앱전반)
- **M-39. 보안링크 원문 토큰이 URL·PG returnUrl로 전파 + Referrer-Policy 없음** — 로그·Referer 유출. (후원/회원)
- **M-40. 웹 인증 오입력 시 TTL 재연장** — 5분 방어선 무력화. (후원)
- **M-41. 웹 후원 riskDetection에 donationId 없음 + 금칙어 차단 흔적 0** — 채널별 감사 비대칭. (후원)
- **M-42. 금액지정 파싱 규칙 비일관** — 2자리/8자리 미매칭 시 조용히 기본금 청구, 콤마/비콤마 분기 상이. (후원)
- **M-43. 대표번호(SHARED_PREFIX)에서 "5000원 키워드..." 순서면 라우팅 실패** — 파싱이 splitKeyword 뒤에 실행. (후원)
- **M-44. 필수 약관이 하나도 없으면 동의 없이 계좌 등록 통과** — 약관 교체 공백 구간. (회원)
- **M-45. 동의 이력이 화면 표시 버전과 다른 버전으로 기록** — orderBy 없는 find. (회원)

---

## 4. 낮음 (P3 — 정리/일관성/접근성)

- L-1. 차단 MO 메시지 result가 `ROUTED`로 남음(차단 집계 불가). (후원)
- L-2. 멱등키 `IN_PROGRESS` 고착(`abort()` 미사용). (후원)
- L-3. `setStatus` 비원자적 + 상태 로그 fromStatus 하드코딩. (후원)
- L-4. `rollbackCounters` 음수 허용 + executePayment 조기반환 목록에 환불상태 누락. (후원)
- L-5. `revokePaymentMethod`가 암호화 빌키를 그대로 PG에 전달(+실패 삼킴). (후원/회원)
- L-6. 만료 확인건·멱등키 정리 배치 미호출(cron 없음). (후원)
- L-7. 부분 환불 구조적 불가(`amount` 하드코딩). (후원/정산)
- L-8. velocity 고정윈도우 경계 2배 허용 + 유튜브 할당량 비원자 예약. (후원)
- L-9. `SETTLEMENT_PENDING` 즉시 덮어씀 + `SETTLED` 전이 부재(정산완료 상태 추적 불가). (후원/정산)
- L-10. append-only 트리거가 TRUNCATE 미차단. (정산)
- L-11. `applyRate` 음수 금액 방어가 자기계약 위반. (정산)
- L-12. 정산 요약 타일 "누적/월" 혼재 + 관리자·스튜디오 부호 규약 상이. (정산)
- L-13. 등록 화면 수수료 미리보기가 GLOBAL만 조회(CREATOR 우대 미반영). (정산)
- L-14. 로그인 실패 응답이 상태 구분(계정 열거) + 이메일 단위 잠금 표적 DoS. (회원)
- L-15. 세션 정리 배치·동시세션 제한·절대만료 상한 없음, secure 플래그가 NODE_ENV 기준. (회원)
- L-16. 탈퇴 시 결제수단 폐기가 트랜잭션 밖(부분 실패). (회원)
- L-17. 소셜 로그인 state 미검증(스텁) — 실연동 시 OAuth CSRF 위험. (회원)
- L-18. 대시보드 두 섹션 통째 복제(한쪽만 수정 위험). (관리자)
- L-19. TTS 사용현황 집계가 미설정을 "사용 중"으로 계산. (관리자)
- L-20. 크리에이터 목록이 businessNo를 select만(미사용) — 불필요 페이로드. (관리자)
- L-21. 페이지 번호 상한 검증 없음(존재하지 않는 페이지 빈 목록). (관리자)
- L-22. 용어 '후원 페이지'/'후원샵' 혼용(같은 화면 내). (앱전반)
- L-23. 옛 1588 번호 잔존(env 기본값·`.env.example`·mo-messages placeholder). (관리자/앱전반)
- L-24. `APP_BASE_URL` 기본값 3000 vs 실제 3025(오버레이·후원샵 URL 오류). (앱전반)
- L-25. 탈퇴 후 `?withdrawn=1`을 홈이 읽지 않음(확인 메시지 없음). (앱전반)
- L-26. 후원확인 시트 회원가입 유도가 `/login`으로 감(`<a>` 전체 리로드). (앱전반)
- L-27. 문의 위젯 다이얼로그 접근성(aria-modal·포커스 트랩·Escape) 부재. (앱전반)
- L-28. 미참조 자산 13개 + 주석·클래스명 messagepay 잔재. (앱전반)

---

## 5. 정상 확인된(잘 설계된) 사항

1. MO 재전송 3중 방어(providerMessageId·moMessageId·idempotency_key 유니크 + 경합 폴백) — 테스트로 검증됨.
2. 확인 링크 원자적 1회 소비(`updateMany where usedAt:null` + count 확인), 토큰은 HMAC 해시만 저장.
3. 결제 성공 건만 송출, 유튜브/오버레이 실패가 결제 결과를 뒤집지 않음.
4. 정산 원장 append-only(DB 트리거로 UPDATE/DELETE 차단), 지급/요청 advisory lock 직렬화 — 테스트 검증됨.
5. 개인정보 3분리(phoneHash/phoneEnc/phoneMasked) + 계좌 원문 미저장 + 로그 마스킹 + 빌키 승인 직전에만 복호화.
6. 세션 토큰 CSPRNG 32B + DB엔 HMAC 해시만, 탈퇴 계정 재로그인·재가입 차단 정상.
7. 미구현 연동(소셜·헥토·KMS)을 성공으로 위장하지 않고 예외 처리, SAFE_MODE가 실결제를 mock 강제.
8. 최고관리자 안전장치(본인 권한변경·마지막 SUPER_ADMIN 강등·크리에이터 겸직·비활성 승격 금지) 서버 검사.
9. 즉시형 결제(DIRECT_TRIGGER)가 서면승인 플래그로 이중 봉인.
10. 시드가 실제 파이프라인(handleMoInbound→executePayment→requestRefund)으로 데이터 생성.

---

## 6. 권장 수정 순서(제안)

1. **P0 즉시**: C-1(감사로그 BigInt) → C-2(정산 계좌 인증) → C-3(환불 이중처리) → C-4(확인대기 한도).
2. **P1 보안·금전**: H-1/H-2(운영 시크릿 fail-open), H-3(크리에이터 상태검사), H-4/H-6(웹 후원 멱등·마스킹), H-5(웹훅), H-7(로그인 CSRF), H-8(인증번호), H-11(후원자 차단 무효), H-12~H-19(정책·정산 정합).
3. **P2**: 원장 트랜잭션·유니크(M-1), 조정분개 UI(M-2), 무인증 액션(M-3), 권한 가드(M-27/M-4), 확인링크 정리(M-5/M-6) 등.
4. **P3**: 용어·자산·접근성·페이지네이션 정리.

각 항목은 지시를 받는 대로 수정하겠습니다. 우선순위나 범위 조정이 필요하면 알려 주세요.
