# 전체 진행(1~6단계) 완료 보고서

- 일자: 2026-08-20
- 지시: "전체 진행 해 줘. 헥토와 엠투넷은 구두 계약까지 마쳤고 계약을 진행할 예정이야 감안해서 진행해줘."
- 결과: **1~6단계 전부 코드 반영 + 실행 검증 완료.** 유닛 59건 / E2E 152건 전부 통과.

---

## 0. 검증 결과 요약

| 검증 | 결과 |
|------|------|
| 타입 검사 (`tsc --noEmit`) | 오류 0 |
| 린트 (`eslint src`) | 오류 0 |
| 유닛 테스트 (vitest) | **59건 전부 통과** (기존 27 → 신규 32건 추가) |
| 프로덕션 빌드 | 성공 |
| E2E (admin/studio/gap/round5/round6/round7) | **152건 전부 통과** |
| 검수 데이터 정리 | 문의 0 / 신고 0 / 시드 후원 6건 유지 |

신규 테스트 파일 2개를 추가했습니다.

- `tests/security-and-settlement.test.ts` (11건) — 정책 시행일, 속도 제한, 환불 수수료 환입, CSPRNG, 로그인 CSRF
- `tests/adapters.test.ts` (21건) — MTONET MO 파싱/검증, 헥토 서명·암호화·fail-closed
- `round7-e2e.mjs` (43건) — 문의 일원화, 답변 알림, 게스트 승계, 보안 회귀

---

## 1단계 — 치명 4건 (완료)

| # | 항목 | 처리 |
|---|------|------|
| C-1 | 감사로그 BigInt 예외 | **오탐으로 확정. 수정 철회.** 재현 스크립트에 `dotenv` 누락으로 Prisma 가 DB 접속 자체를 못 한 것이 원인이었습니다. `dotenv` 로드 후 RAW_BIGINT/NULL/DEEP/DATE 전부 정상 저장됨을 확인했습니다. Prisma 7 이 BigInt→문자열 직렬화를 자체 처리합니다. |
| C-2 | 정산계좌 인증 경로 부재 | `setSettlementAccountVerified` 액션 신설(SUPPORT 권한 차단 + 감사로그), 크리에이터 상세 화면에 실명확인/해제 버튼 추가 |
| C-3 | 환불 승인 이중 처리 | 진입 시 `status==='REQUESTED'` 검사 + `updateMany` 조건부 선점. 동시 2건 승인 시 1건만 통과, REFUND 분개 중복 없음을 실행으로 확인 |
| C-4 | 확인대기 건 한도 우회 | `executePayment` 진입 시 한도 재검사. 확인 링크를 모아뒀다가 한꺼번에 눌러도 `LIMIT_BLOCKED` 로 차단 |

> **C-4 후속으로 발견한 실제 버그**: 재검사가 속도 제한(velocity/streak) 카운터를 **두 번** 소진해, 설정한 제한의 절반에서 정상 후원자가 차단되고 쿨다운도 두 배 빨리 걸리는 문제가 있었습니다. `consumeVelocity: false` 옵션을 추가해 재검사는 카운터를 건드리지 않게 고쳤습니다. (기존 테스트 [8]번이 이 때문에 깨져서 발견했습니다.)

---

## 2단계 — 문의 로직 높음 5건 (완료)

| # | 항목 | 처리 |
|---|------|------|
| 1 | `markInquiryRead` 무인증 공개 액션 | `'use server'` 파일에서 제거하고 `src/server/services/inquiry.ts` 로 이전. 추가로 상세 페이지에 `requireAdmin()` 직접 호출(레이아웃과 페이지는 병렬 렌더링되므로 레이아웃 가드만으로는 쓰기 작업을 막지 못함) |
| 2 | 발송 제한을 클라이언트가 무력화 | 제한 키에 **항상 IP 포함**. 로그인 사용자는 계정 단위로도 함께 제한. 쿠키를 지우고 새 토큰을 받아도 제한이 유지됨 |
| 3 | 게스트 → 로그인 시 스레드 승계 없음 | `claimGuestInquiry()` 신설. 액션과 폴링 API 양쪽에서 승계. 회원 스레드가 이미 있으면 메시지를 옮기고 빈 게스트 스레드는 삭제 |
| 4 | `/support` 신고 본문에 내부 userId 노출 | `Report.reporterUserId` 컬럼 신설, 본문에는 사용자 입력만 저장. 크리에이터 화면에서 식별자 사라짐을 E2E 로 확인. 관리자 화면에는 접수자 컬럼을 새로 추가해 필요한 정보는 유지 |
| 5 | `/support` 접수에 제한 전무 | IP + 계정 단위 10분당 10건 제한 추가 |

**실행 검증**: 승계 5개 시나리오(신규 승계 / 병합 / 재승계 차단 / 미존재 토큰 / 타인 탈취 시도) 전부 의도대로 동작.

---

## 3단계 — 보안·금전 P1 (완료)

| 항목 | 처리 |
|------|------|
| APP_ENV fail-open | 화이트리스트(`local`/`staging`/`prod` + dev·stage 별칭). **미설정·오타는 안전하게 판정**(NODE_ENV=production → prod). 테스트 로그인·MO 시뮬레이터·개발 아웃박스를 `isLocal` 로 잠금 |
| 시크릿 fail-open | `SESSION_SECRET`/`PHONE_HASH_SECRET` 개발 기본값을 **로컬에서만** 허용. 그 외 환경은 모듈 로드 시 즉시 예외 |
| 부팅 안전점검 | `src/instrumentation.ts` 신설. 운영 설정 미비 시 **기동 자체를 중단**. (실제로 검증됨 — 잘못된 설정으로 띄우려 하자 서버가 거부했습니다) |
| MO 웹훅 fail-open | 시크릿·IP 목록이 없으면 통과하던 것을 **fail-closed** 로 전환(로컬 제외). 공용 `verifyMoRequest` 로 통일 |
| 크리에이터 상태 미검사 | `requireCreator()` 가 `APPROVED` 확인. 정지 시 세션 무효화. 미승인·반려·정지 채널에는 스튜디오 대신 상태 안내 화면 표시 |
| 웹 후원 이중 출금 | 멱등키를 **금액·메시지 확정 시점에 1회 생성해 고정**, 결제 성공 후에만 회전. 기존에는 제출할 때마다 새 키라 더블클릭이 2건 출금으로 이어질 수 있었습니다 |
| 웹 후원 표시명 `***` | 이미 마스킹된 값을 다시 마스킹하던 이중 마스킹 제거 → `익명의 후원자` |
| 로그인 CSRF | 로그인/로그아웃 라우트에 Origin 검증(Origin·Referer 둘 다 없으면 거절). 로그인 IP 단위 제한도 추가 |
| 인증번호 난수 | `Math.random()` → `crypto.randomInt()` (인증번호 3곳 + 크리에이터 코드 1곳) |
| 정책 시행일 무시 | 한도·수수료·약관 resolver 에 `effectiveFrom <= now`, `effectiveTo` 조건 추가. 예약 등록한 미래 정책이 즉시 적용되던 문제 해소 |
| 환불 수수료 환입 오차 | **원 거래에 실제로 기록된 분개**에서 환입액 산출로 변경. 환불 시점 요율로 재계산하던 방식은 요율 변경 시 원장이 영구히 틀어지고, 부분 환불에서 정액 수수료를 통째로 환입하는 문제가 있었습니다 |
| 정산 지급 검증 | `markSettlementPaid` 가 잠금 안에서 `APPROVED` 재확인 + 계좌 인증 재확인 + 잔액 재확인 |
| 정산 요청 페이지네이션 | 요청/원장 목록 페이지 파라미터 분리(`rpage`/`page`). 미처리 건 우선, 오래된 순 정렬로 변경 |
| 캘린더 집계 상수 불일치 | 정산 캘린더의 로컬 상수 제거 → 공용 `PAID_STATUSES`. 원천징수율도 서버 상수 공유 |
| `/api/health` 정보 노출 | 비로그인에는 `{ok}` 만, 관리자에게만 상세. 비정상 시 503 반환 |

---

## 4단계 — 외부 연동 준비 (완료)

계약 진행 중이므로 **서명·암호화·파싱 로직은 완성하고 단위 테스트로 검증**했으며, 규격서와 대조가 필요한 값은 한 곳에 모아 두었습니다.

### 4-1. 헥토파이낸셜 내통장결제 (`src/server/adapters/payment/hecto.ts`)

**가장 중요한 수정**: 기존 `HECTO_API_BASE="https://tbgw.settlebank.co.kr"` 는 **일반 PG 게이트웨이 주소**로, 내통장결제(EzAuth) 주소가 아니었습니다. 그대로 두면 연동 첫날 전건 실패합니다. 두 호스트로 분리했습니다.

- 결제창(UI): `HECTO_AUTH_UI_BASE` = `https://ezauth.settlebank.co.kr`
- 서버 API: `HECTO_AUTH_API_BASE` = `https://ezauthapi.settlebank.co.kr:8081`

구현·검증한 내용:

- 서버 API 서명 `SHA256(mercntId+authNo+reqDay+reqTime+hashKey)` — 재료 순서 회귀 테스트 포함
- 결제창 해시 `SHA256(mercntId+ordNo+trDay+trTime+**평문금액**+callbackUrl호스트+hashKey)` — 암호문을 넣으면 검증 실패하므로 평문 사용을 테스트로 고정
- AES-256 암복호화 왕복 + 키 길이(32byte) 미달 시 즉시 실패
- KST 기준 `yyyyMMdd`/`HHmmss` (자정 경계 포함 검증)
- 결제인증 10분 제한을 세션 만료에 반영
- 승인/조회/취소/빌키 발급·해지 전 구간
- **fail-closed**: 키가 하나라도 없으면 mock 으로 조용히 대체하지 않고 `AdapterNotConfiguredError`

### 4-2. MTONET 050 MO (`src/server/adapters/mo/mtonet.ts`)

국내 050 MO 표준인 "URL 방식"(수신 즉시 HTTP 푸시)을 전제로 구현했습니다. 현재 문자페이 MO 구조가 정확히 그 방식이라 아키텍처 변경은 없었습니다.

- 필드명 후보를 `FIELD_ALIASES` 한 표에 모음 → 규격서 수령 시 그 표만 수정
- `data`/`result` 래핑 payload 대응
- 하이픈·공백 제거, KST 시각 3종 표기 해석
- **필수값 누락 시 기본값으로 때우지 않고 실패** (관대하게 처리하면 엉뚱한 크리에이터에게 후원이 꽂힘)
- **050 이 아닌 수신번호는 거절** (라우팅 사고 차단)
- SMS/LMS/MMS 판정(표기 없으면 90바이트 기준 추정), LMS 제목 병합
- 공용 fail-closed 검증 통과 필수

### 4-3. 계약 후 필요한 자료

1. **헥토**: 상점 연동규격서(엔드포인트 경로·필드명·응답코드표), 테스트베드 MID/HASH/AES 키, 콜백 URL 등록
2. **MTONET**: MO 연동규격서(푸시 payload 필드명·인증 방식·성공 응답 형식), 발신 IP 대역(→ `MO_ALLOWED_IPS`), 계정 정보
3. 확보 후 `HECTO_SPEC` / `FIELD_ALIASES` 두 블록만 대조·수정하면 실연동 전환됩니다.

---

## 5단계 — 문의 채널 일원화 (완료)

**핵심 문제**: 위젯 문의(SupportInquiry)와 `/support` 접수 폼(Report)이 서로 다른 테이블이라, `/support` 로 접수하면 **답변 필드도 조회 화면도 없어 비회원은 영영 답을 못 받는** 구조였습니다.

- `/support` 접수도 **SupportInquiry 스레드**로 저장 → 우측 하단 문의 창에서 그대로 답변 확인
- 비회원도 게스트 쿠키로 답변 확인 가능
- `category`(죽은 컬럼) 활용 + `transactionNo`/`donationId`/`creatorId`/`source` 컬럼 추가
- **신고(ABUSE) 유형만** Report 를 추가 생성해 신고 처리 큐에도 등록
- 관리자 상세에 거래번호로 연결된 후원 요약(금액·상태·크리에이터·수신 시각) 표시
- 관리자 목록에 **검색(이름·이메일·연락처·거래번호·본문·접수번호) + 유형 필터 + 접수 경로 필터** 추가
- 답변 등록 시 회원에게 **알림 생성**, 문의 버튼에 **미읽음 배지** 표시
- 메시지 조회를 `asc + take 200` → **최신 200건 후 역순**으로 변경 (스레드가 길어져도 최신 답변이 잘리지 않음)
- 배경 폴링은 **문의 이력이 있는 방문자만** 수행 (모든 방문자가 15초마다 DB 를 두드리던 부하 제거)

---

## 6단계 — 정합·최종 검수 (완료)

- 마이그레이션 2건 추가 적용 (`report_reporter_user`, `inquiry_unify`)
- `.env` / `.env.example` 정리: 헥토 호스트 분리, MTONET 항목 추가, `MO_PROVIDER=mtonet` 명시, `APP_ENV` 주석 정정
- 시드 재적용 후 전 화면 정상 동작 확인
- 검수 과정에서 만든 문의·신고 데이터 전부 정리 완료

---

## 참고: 이번에 확인된 사항

`preview.bat` 처럼 로컬에서 프로덕션 빌드를 띄우는 방식은 그대로 동작합니다. 처음에 `NODE_ENV=production` 만으로 운영 환경으로 단정하도록 만들었더니 로컬 미리보기가 기동을 거부해서, **명시적으로 지정한 `APP_ENV` 를 우선 존중**하고 미설정·오타일 때만 안전한 쪽으로 판정하도록 조정했습니다. `.env` 에 `APP_ENV="local"` 이 있으므로 PC 미리보기는 지금과 동일하게 동작합니다.

배포 시에는 `APP_ENV=prod` 와 함께 아래가 모두 갖춰져야 서버가 뜹니다(하나라도 빠지면 기동 중단).

```
CRYPTO_PROVIDER=aws-kms
SESSION_SECRET / PHONE_HASH_SECRET   (기본값 금지)
ALLOW_INMEMORY_FALLBACK=false
MO_ALLOWED_IPS / MO_WEBHOOK_SECRET
PAYMENT_PROVIDER=hecto + HECTO_MID / HECTO_HASH_KEY / HECTO_AES_KEY
APP_BASE_URL=https://...
```

## 남은 항목

2차 감사의 중간(P2)·낮음(P3) 항목과 문의 로직 중간/낮음 항목(연락처 복호화 경로, 전송 실패 시 본문 유지, 담당자 배정·답변 템플릿, 파기 정책, 상태 전이 강제 등)은 이번 범위에 넣지 않았습니다. 운영 리스크가 큰 순서대로 정리되어 있으니, 다음 진행 여부를 지시해 주시면 이어서 처리하겠습니다.


---

# 부록 — PC 작업분 유실과 복구 (중요)

## 무슨 일이 있었나

전달 후 PC를 점검하다가, **제 클라우드 사본에 없던 PC 작업분**을 발견했습니다. 제 클라우드 클론이 PC의 최신 커밋(`56d90bb`)보다 앞선 시점에서 갈라져 나온 상태였고, 그 사이 PC에는 아래 작업이 들어와 있었습니다.

- 알림 시스템 (종 아이콘 + `/api/notifications` + 알림 서비스)
- 프로필 캐릭터 자동 생성 컴포넌트 (`ProfileAvatar`)
- 후원 설정 탭 UI (후원금 / 결제 모드 / 문자번호 / 후원페이지)
- 후원페이지 공유 카드 + QR (`DonationPageShare`, `qrcode` 의존성)
- 공개 주소 헬퍼 (`getPublicBaseUrl`)
- 관리자 사이드바 아이콘
- 라이브 플랫폼(유튜브·인스타그램·틱톡) 선택 — **미완성 상태**

제가 파일을 덮어쓰면서 이 중 일부가 사라졌습니다. **모두 복구했습니다.**

## 복구 내역

| 항목 | 처리 |
|------|------|
| `schema.prisma` 라이브 컬럼 4개 | 복원 (`livePlatform`, `youtubeLiveUrl`, `instagramLiveUrl`, `tiktokLiveUrl`) |
| 알림 시스템 5개 파일 | 클라우드로 복원 후 유지 |
| 크리에이터 승인 알림 (`notifyUser`) | `accounts.ts` 에 복원 |
| 문의 답변 알림 | 제가 직접 넣었던 코드를 공용 `notifyUser` 로 통일 |
| 새 문의 관리자 알림 (`notifySuperAdmins`) | `inquiry.ts` 에 복원 |
| 아바타·탭 UI·공유 카드·사이드바 아이콘 | PC 버전을 그대로 채택 |
| `qrcode` 의존성 | 클라우드에도 설치 |

## 함께 고친 것

**라이브 플랫폼 기능이 PC에서도 동작하지 않는 상태였습니다.** 화면에는 유튜브·인스타그램·틱톡 선택 UI가 있었지만, 저장 액션(`updateDonationPageAction`)이 그 값을 저장하지 않아 무엇을 골라도 반영되지 않았습니다. 마저 붙였습니다.

- 선택한 플랫폼과 3개 주소를 모두 저장
- **플랫폼별 호스트 검증** — 인스타그램 칸에 다른 사이트 주소를 넣으면 거절 (엉뚱한 주소가 저장되면 후원자가 빈 페이지로 이동)
- 스위치를 켤 때 선택한 플랫폼의 주소가 비어 있으면 안내 후 차단
- 후원샵 온에어 버튼이 **선택한 플랫폼의 라이브로 연결**

E2E 7건으로 검증했습니다(선택 저장 · 재진입 유지 · 잘못된 호스트 거부 · 후원샵 링크 연결 등).

## 추가로 발견한 것

**고객센터 폼(`/support`) 접수는 관리자에게 알림이 가지 않았습니다.** 위젯 문의만 알림이 발송되어, 폼으로 들어온 문의는 관리자가 목록을 직접 열어보기 전까지 알 수 없었습니다. `notifySuperAdmins` 를 추가했습니다.

## 남은 참고 사항

후원페이지 설정 폼은 **검증 실패 시 입력값이 DB 값으로 되돌아갑니다.** (플랫폼 선택·주소를 다시 입력해야 합니다) 문의 위젯의 전송 실패 시 본문 유실과 같은 원인이며, 2차 감사의 중간 등급 항목입니다. 다음 진행 시 함께 고치겠습니다.

## 앞으로의 권장 사항

이번 유실의 원인은 **PC와 클라우드 두 곳에서 각각 작업이 진행된 것**입니다. 재발을 막으려면 작업 시작 전에 PC의 최신 상태를 클라우드로 한 번 내려받는 절차가 필요합니다. 다음 작업 시작 시 제가 먼저 PC 상태를 확인하고 시작하겠습니다.
