# 전체 검수 결함 수정 — 완료 보고서

- 일자: 2026-08-21
- 대상: 2026-08-21 전체 검수 보고서의 **치명 4 / 높음 11 / 중간 7 / AWS 6 = 총 28건**
- 결과: **전 항목 수정 + 실행 검증 완료**

| 검증 | 결과 |
|---|---|
| 타입검사 | 오류 0 |
| 린트 | 오류 0 (경고 19, 기존 미사용 변수) |
| 유닛 테스트 | **93건 통과** (기존 77 + 신규 회귀 16) |
| E2E (8개 스위트) | **187건 통과** (신규 `round9-e2e.mjs` 15건) |
| 프로덕션 빌드 | 성공 |
| 빈 DB 마이그레이션 재생 | 13개 전량 성공 |
| 스키마 드리프트 | **No difference detected** (검수 전 3건 → 0건) |

---

## 1. 원천징수 계산 — 국내 표준 방식 적용

지시하신 대로 실무 표준으로 바꿨습니다. 3.3% 를 한 번에 곱해 절사하던 것을 **2단계**로 나눴습니다.

```
1) 소득세     = 지급액 × 3%   → 10원 미만 절사   (국고금관리법 제47조)
2) 지방소득세 = 소득세 × 10%  → 10원 미만 절사
3) 합계       = 소득세 + 지방소득세
4) 소액부징수 : 소득세가 1,000원 미만이면 전액 미징수 (소득세법 제86조)
```

**예시**

| 정산액 | 소득세 | 지방소득세 | 원천징수 | 실지급 | (기존 3.3%) |
|---|---|---|---|---|---|
| 333,333원 | 9,990 | 990 | **10,980** | 322,353 | 10,999 |
| 1,000,000원 | 30,000 | 3,000 | **33,000** | 967,000 | 33,000 |
| 30,000원 | 0 | 0 | **0** (소액부징수) | 30,000 | 990 |

**실무상 바뀌는 점 하나**: 소액부징수 때문에 **정산액 33,334원 미만은 원천징수 없이 전액 지급**됩니다. 법대로입니다.

소득세·지방소득세를 **각각 DB 컬럼에 저장**하도록 바꿨습니다(`income_tax`, `local_tax`). 지급명세서는 둘을 따로 적어야 해서 합계만으로는 신고서를 만들 수 없습니다. 원천징수 CSV에도 두 열이 분리돼 나갑니다.

> 세액 최종 확정은 세무 자문을 거치시길 권합니다. 저는 세무 전문가가 아닙니다.

---

## 2. 치명 4건

### CRIT-1. 결제 결과 미확인(UNKNOWN) 보존
`donation-flow.ts` — 승인 여부를 확인 못 한 건을 즉시 `FAILED` 로 덮어쓰던 것을 **UNKNOWN 그대로 유지**하도록 바꿨습니다. 함께 처리한 것:

- 실패 문자를 보내지 않습니다 (돈은 나갔는데 "실패" 안내가 가면 이중청구 오해를 부릅니다)
- 실패 카운터를 올리지 않습니다 (정상 후원자가 잠기던 문제)
- 후원 상태를 `PENDING_PAYMENT` 로 유지하고 사유만 남깁니다
- **`RiskDetection(PAYMENT_UNKNOWN, CRITICAL)` 을 생성**해 관리자 큐에 올립니다. 같은 건이 중복 등록되지 않도록 미해결 건이 있으면 건너뜁니다
- 후원자에게는 "결제 결과를 확인하는 중입니다"로 안내합니다

이제 `/admin/payments` 의 "확인 필요" 큐가 실제로 채워져 수동 대사가 가능합니다.

### CRIT-2·3. 민감정보 CSV 접근통제
- 주민등록번호 CSV / 계좌·실명 CSV 모두 **화이트리스트(`SUPER_ADMIN`·`FINANCE`)** 로 전환. 기존 블랙리스트 방식은 `AdminPermission` 기본값이 `READ_ONLY` 라 그대로 뚫려 있었습니다
- 다운로드마다 **감사로그 기록**: 누가·언제·몇 건·주민번호 원문이 몇 건 포함됐는지
- 미인증 요청은 500 대신 **401** 로 정리

### CRIT-4. MO 번호 라우팅 충돌
세 겹으로 막았습니다.

1. **DB**: `CREATE UNIQUE INDEX ... ON creator_mo_number(phone_number) WHERE keyword IS NULL` 부분 유니크 인덱스 — PostgreSQL 이 NULL 을 서로 다른 값으로 취급해 전용번호가 무제한 중복되던 구멍을 막습니다(같은 파일의 `banned_word` 가 이미 쓰던 방식과 동일)
2. **등록**: 같은 번호에 전용/대표번호 모드를 섞어 등록할 수 없습니다. 전용번호는 키워드를 붙이지 않도록 정규화
3. **라우팅**: 그래도 충돌 상태가 남아 있으면 **아무에게도 배달하지 않고** 관리자 로그를 남깁니다. 엉뚱한 크리에이터에게 돈이 흘러가는 것보다 접수 실패가 낫습니다

---

## 3. 높음 11건

| # | 수정 내용 |
|---|---|
| H-1 | 결제 승인 기록과 **정산 원장 분개를 같은 트랜잭션**으로 묶었습니다. 중복 분개 방지 가드도 추가해 재시도에도 안전합니다 |
| H-2 | 거래결과조회(`inquire`)를 try/catch 로 감쌌습니다. 조회 실패 시 예외를 밖으로 내보내지 않고 UNKNOWN 으로 확정합니다 (후원이 영구히 멈추던 경로) |
| H-3 | 방송 송출(`dispatchBroadcast`)을 try/catch 로 격리. 송출 실패가 결제 결과를 뒤집지 않습니다 |
| H-4 | **검증 시점을 이체 전으로 이동.** 새 함수 `assertPayable()` 이 이체파일 생성 시 계좌 인증·잔액을 검사해 부적격 건을 제외합니다. 이체가 끝난 뒤 호출되는 `markSettlementPaid()` 는 **어떤 경우에도 분개를 남기고** 이상은 경고 메모(`⚠`)로만 표시합니다 |
| H-5 | advisory lock 키를 `settlement:paid:{요청ID}` → **`settlement:creator:{크리에이터ID}`** 로 변경. 보호 대상이 크리에이터 잔액이므로 같은 크리에이터의 서로 다른 요청이 동시 통과하던 구멍이 막힙니다 |
| H-6 | 유튜브 입력칸 폴백을 `livePlatform` 이 YOUTUBE 이거나 미지정일 때로 한정. 인스타/틱톡 선택 후 설정이 영구 잠기던 문제 해소 |
| H-7 | 채널 주소를 소개글에 이어 붙이지 않고 **`channel_url` 별도 컬럼**으로 분리. 기존 데이터도 마이그레이션에서 자동 분리·정리 |
| H-8 | 번호 미배정 시 **PC 웹 후원 패널은 그대로 노출**하고, 문자후원 영역만 안내로 대체 |
| H-9 | 후원샵 조회 조건에 `user.status = ACTIVE` 추가. 계정 정지가 실제로 샵을 닫습니다 |
| H-10 | `purgeResidentIfNotFilable()` 추가 — 반려·지급실패 건은 신고 대상이 아니므로 주민번호를 즉시 파기. 상태 전이 시점에 자동 호출 |
| H-11 | **지급대행 배치번호** 발급 (`payout_batch_no`, `payout_issued_at`). 재발급 시 목록으로 경고, 파일명에 배치번호 포함, 결과 반영 시 배치번호로 대조 |

**보너스 — 지난 결과 파일 재반영 차단**: 결과 붙여넣기 화면에 배치번호 입력칸을 추가했습니다. 예전 파일을 잘못 다시 붙여넣어 **정상 지급건이 '실패'로 되돌아가 원장이 환입되는** 사고를 막습니다. 이체파일이 나간 적 없는 건도 거부합니다.

---

## 4. 중간 7건

| # | 수정 내용 |
|---|---|
| M-1 | 위 1장 참조 |
| M-2 | `kstMonthEndKey()` 추가. 월말이 실제 월말로 계산됩니다. 추가로 **`2026-02-31` 같은 달력에 없는 날짜는 400 으로 거부** (JS 는 이를 3월 3일로 굴려버립니다) |
| M-3 | 웹 후원 인증 세션을 hidden 필드 → **HttpOnly 쿠키**로 이전. 토큰이 DOM·화면공유에 노출되지 않습니다. 서버는 폼 값을 신뢰하지 않고 쿠키만 봅니다 |
| M-4 | `moRoutes` 에 `orderBy: { assignedAt: 'desc' }` 추가 |
| M-5 | 후원 금액 검증이 **플랫폼 정책 ∩ 크리에이터 설정** 교집합을 쓰도록 변경 |
| M-6 | `getSettlementSummary(creatorId, tx)` — 잠금 안에서 트랜잭션 클라이언트로 읽습니다 |
| M-7 | `timeZone: 'Asia/Seoul'` 명시 |

---

## 5. AWS 이관 6건

| # | 수정 내용 |
|---|---|
| A-1 | `schema.prisma` 에 누락됐던 인덱스 3개 선언 + 인덱스명을 Prisma 규약으로 통일. **드리프트 0건** 달성 |
| A-2 | `prisma.config.mjs` 가 `DIRECT_DATABASE_URL` 을 우선 사용. 드리프트 검사용 `SHADOW_DATABASE_URL` 도 연결 |
| A-3 | **저장소 드라이버 분리** — `STORAGE_DRIVER=local\|s3`. S3 드라이버(`@aws-sdk/client-s3`, 지연 로딩) 구현 완료. `S3_PUBLIC_BASE` 로 CloudFront 직결도 지원. 운영에서 `local` 이면 **부팅 중단** |
| A-4 | `docs/AWS-배포-가이드.md` 에 DB 롤 분리 SQL 수록 (마이그레이션 소유자 / 앱 DML 롤 / 원장 UPDATE·DELETE 회수) |
| A-5 | `output: 'standalone'` 을 **`NEXT_OUTPUT=standalone` 일 때만** 켜도록 조건화. 로컬 `preview.bat`(npm run start)은 그대로 동작합니다 — standalone 빌드는 `next start` 로 못 띄웁니다 |
| A-6 | 헬스체크를 읽기 전용 점검으로 변경 (ALB 가 30초마다 여러 인스턴스에서 호출하는데 매번 Redis 에 쓰던 것) |

신규 문서 **`docs/AWS-배포-가이드.md`** — 마이그레이션 실행법, 드리프트 CI 검사, DB 롤 분리 SQL, S3 설정, Dockerfile, 환경변수 점검표, 배포 순서를 담았습니다.

---

## 6. 마이그레이션 2건

| 파일 | 내용 |
|---|---|
| `20260821090000_audit_fixes` | 소득세/지방소득세 컬럼(+ 기존 행 역산 보정), 지급 배치 컬럼, `PAYMENT_UNKNOWN` 위험유형, MO 부분 유니크 인덱스, 드리프트 인덱스 정리 |
| `20260821091000_creator_channel_url` | `channel_url` 컬럼 + 소개글에 섞여 있던 "채널: …" 자동 분리 + 300자 초과분 정리 |

둘 다 `IF NOT EXISTS` / `IF EXISTS` 를 써서 재실행 안전하고, 빈 DB 재생과 기존 DB 적용 모두 검증했습니다. RDS/Aurora 표준 SQL 만 사용합니다.

---

## 7. 신규 테스트

**유닛 16건** (`tests/audit-fixes.test.ts`) — 각 테스트는 "고치기 전이었다면 실패했을" 조건을 검사합니다.
원천징수 2단계·10원 단위·소액부징수·경계값 / 월말 날짜 12개월 전수 / MO 모드 혼재 차단 / 부분 유니크 인덱스 / 결제 성공 시 원장 동시 기록 / 배치번호 발급·재발급 / **이체 후 이상 시에도 분개 기록** / 반려 건 주민번호 파기 / 소득세·지방소득세 분리 저장

**E2E 15건** (`round9-e2e.mjs`) — CSV 권한·감사로그·잘못된 날짜 거부·비로그인 401 / MO 혼재 등록 차단 / PC 웹 후원 패널 노출 / **라이브 플랫폼 전환 후 재저장 성공** / 지급대행 배치번호 UI

기존 테스트 중 3건은 새 설계에 맞게 갱신했습니다(원천징수 기대값 2건, 지급 검증 시점 이동 1건). 검증이 약해지지 않도록 `assertPayable()` 검사로 대체했습니다.

또한 검수 중 **기존 E2E의 잠재 실패 1건**을 발견해 고쳤습니다 — `round6-e2e.mjs` 가 이름이 바뀐 옛 배너 파일명(`donaido-banner-02.png`)을 찾고 있었습니다.

---

## 8. 남은 판단 사항

1. **세무 자문** — 원천징수 2단계 계산과 소액부징수 적용은 국내 실무 표준을 따랐지만, 최종 확정은 세무사 확인을 권합니다. 특히 소액부징수로 33,334원 미만 정산이 전액 지급되는 점을 확인받으시면 좋겠습니다.
2. **S3 버킷 준비** — 코드는 완료됐고 `STORAGE_DRIVER=s3` + `S3_BUCKET` 만 주면 전환됩니다. 이관 전 버킷·IAM 역할 생성이 필요합니다.
3. **DB 롤 분리** — SQL은 가이드에 넣었지만 실제 실행은 RDS 생성 시점에 해야 합니다.
4. **쿠콘 연동규격서** — 이체파일 열 순서는 표준형입니다. 규격서를 받으시면 헤더만 대조하면 됩니다.
