# E2E 브라우저 검증 복구 보고서

- 작성일: 2026-08-25
- 대상: 커밋 `99da66b` ("chore: 사용하지 않는 e2e 스크립트, 임시 배치파일, 보고서 문서 정리") 로 삭제된 24개 파일
- 결과: **E2E 9종 복구·재작성 완료, 417건 전건 통과 / 검수 보고서 10건 복구 완료 / 원천징수 표시 결함 1건 수정**

---

## 1. 왜 복구했는가

`99da66b` 는 24개 파일 3,491줄을 삭제했다. 이 중 **E2E 스크립트 9개(약 220건 검증)** 는
메시지페이에서 **화면이 깨졌는지 확인하는 유일한 수단**이었다.

- `tests/*.test.ts` (Vitest 162건) — 서버 로직만 본다. 화면은 보지 않는다.
- `e2e/*.mjs` — 실제 크로미움을 띄워 로그인하고 클릭·입력한다.

삭제 이후 화면 검증은 0건이었다. 특히 그 직후 Codex 가 PC 웹 후원을 PIN 인증 흐름으로
전면 교체(`78925cd`, `6a042a9`)했기 때문에, **가장 크게 바뀐 결제 화면이 검증 없이 배포되는 상태**였다.

## 2. 어떻게 복구했는가

옛 파일을 그대로 되돌리지 않았다. 되돌렸다면 상당수가 FAIL 이었다.
예: `gap-e2e.mjs` 가 검사하던 문구 `"본인 인증 후 바로 결제"` 는 현행 코드에 **0개**다(PIN 전환으로 사라짐).

그래서 **현행 화면 기준으로 다시 썼다.**

| 조치 | 내용 |
|---|---|
| 위치 이동 | 저장소 루트 → `e2e/` 디렉터리 (루트 산재 → "안 쓰는 파일" 오해의 원인이었음) |
| 실행 경로 부여 | `npm run test:e2e` 추가 + `e2e/run-all.mjs` 실행기 |
| 재현성 확보 | 스크립트마다 DB 초기화·재시드 → 실행 순서에 결과가 좌우되지 않음 |
| 이식성 확보 | 크로미움 경로를 하드코딩하지 않음 (`E2E_CHROMIUM`, `E2E_BASE` 환경변수) |
| 기능 변화 대응 | 관리자 화면 목록을 **사이드바 링크에서 자동으로 뽑는다**. 메뉴가 늘거나 줄어도 스크립트를 고칠 필요가 없다. 자체방송(RTMPS)처럼 통째로 정리된 기능은 "있으면 검증, 없으면 잔재 없음 확인"으로 처리한다 |
| 문서화 | `e2e/README.md` — 실행법·구성·삭제 금지 사유 |

## 3. 검증 결과

전량을 실제 크로미움으로 실행했다.

| 스크립트 | 검증 | 결과 |
|---|---:|---|
| `round5` 스튜디오 셸·내비게이션 | 37 | 전건 통과 |
| `round6` 후원샵 · **PC PIN 후원 전 구간** | 47 | 전건 통과 |
| `round7` 문의 채널 · 관리자 문의 · 보안 | 39 | 전건 통과 |
| `round8` 정산 탭 · 원천징수 · 금칙어 | 40 | 전건 통과 |
| `round9` 민감자료 권한 · 지급대행 · 관리자 권한 | 38 | 전건 통과 |
| `round10` 알림 · 정산주기 · 공휴일 · 오버레이 | 64 | 전건 통과 |
| `gap` 기기별 안내 · 모의 고지 · 공개 페이지 | 30 | 전건 통과 |
| `studio` 유튜브 · 오버레이 · 스트림 · 설정 | 57 | 전건 통과 |
| `admin` 관리자 전 메뉴 스모크 · 한도 정책 | 66 | 전건 통과 |
| **합계** | **417** | **9종 9종 통과 · 실패 0** |

> PC 로컬에 아직 푸시되지 않은 Codex 커밋 `0d76366`("자체방송 미사용 코드 제거")을 확인하고,
> `/admin/streams` 삭제와 스튜디오 스트림 키 섹션 제거에 맞춰 `admin`·`studio` 스크립트를
> 조건부로 다시 손봤습니다. 그래서 자체방송이 있는 상태에서도, 정리된 상태에서도 둘 다 통과합니다.

단위 테스트(Vitest) 13파일 162건도 전건 통과. `typecheck` 오류 0건, `lint` 오류 0건(경고 19건은 기존분).

앱 코드 변경은 아래 5장의 원천징수 표시 수정 1곳뿐이다.
(그 밖의 변경분: `package.json` 에 `test:e2e` 스크립트 1줄 + `e2e/` 신규 + 문서 복구)

### 이번 복구로 브라우저에서 실제 확인된 것

1. **PC 웹 후원 PIN 전 구간** — 금액·메시지 선택 → 번호 입력 → PIN 링크 문자 발송 →
   문자의 링크에서 PIN 6자리 입력 → 결제 완료 → 후원샵 화면이 폴링으로 완료 감지 →
   최근 후원 목록 노출. **PIN 링크 1회용 차단**까지 확인.
2. **정산 요청 → 최고관리자 알림 도착** — 크리에이터가 정산을 요청하면 최고관리자
   알림 버튼에 미읽음 배지가 붙고, 알림함에 `새 정산 요청이 접수되었습니다` 가 들어오며
   `/admin/settlements` 목록에도 올라온다. 클릭만으로 전 구간 확인.
3. **원천징수 계산** — 정산 가능금 7,488원 구간에서 소득세 340원 < 1,000원 →
   `0원 (소액부징수)` 로 표시. 옛 3.3% 일괄 계산 흔적 없음.
4. **오버레이 파이프라인** — `/overlay/{id}?preview=1&debug=1` 에서 SSE `연결됨` 확인 후
   스튜디오에서 테스트 후원 발사 → 오버레이 배너에 금액·메시지·`테스트` 배지 표시.
   토큰 없는 접근은 `접근 권한이 없습니다 (401)`.
5. **민감자료 권한** — 지급대행/원천징수 CSV 는 비로그인 401, 대상 미선택 400,
   달력에 없는 날짜(2026-02-31) 400. `no-store` 헤더 확인.

## 4. 복구하지 않은 것 (판단 근거)

| 대상 | 판단 |
|---|---|
| 아바타 스프라이트 PNG 2개 (4.7MB) | **복구 불필요.** 낱장 50개(`public/avatars/messagepay/avatar-01~50.png`)로 이미 대체됨. `generated-avatar.tsx` 의 `CHARACTER_SHEETS` 는 옛 데이터 판별용 문자열일 뿐 이미지를 불러오지 않음(확인 완료). |
| `diag-preview.bat` | 현재 `diag.bat` 이 대체 |
| `preview-reset.bat` | 현재 `db-reset.bat` + `preview.bat` 이 대체 |
| `git-push.bat` | 현재 터미널에서 직접 푸시하므로 불필요 |

## 5. 추가 수정 (지시받아 반영 완료)

- **정산 요청 내역의 원천징수 열이 `-0원` 으로 표시되던 문제 수정.**
  공제액을 음수로 보이려는 의도였으나(`-{formatWon(r.withholding)}`), 소액부징수로 0원일 때
  `-0원` 이 되어 어색했다. 원천징수가 있을 때만 `-` 를 붙이고 색도 빨간색으로 두며,
  0원이면 `0원` 을 흐린 색으로 표시하도록 바꿨다. (`src/app/studio/settlement/page.tsx`)
- 이 회귀를 막기 위해 `round10-e2e.mjs` 에 검사 1건을 추가했다:
  정산 요청 직후 화면에 `-0원` 문자열이 없어야 한다. → **전체 417건 전건 통과.**

## 6. 남은 확인 사항

- 실제 헥토/엠투넷 연동 후에는 `round6` 의 결제·문자 구간을 연동 규격에 맞게 다시 손봐야 한다.
  (현재는 `mock` 어댑터 기준, PIN 은 6자리면 무엇이든 통과)
- 2026년 공휴일 시드 날짜 확정, 근로자의 날(5/1)을 영업일에서 제외할지 여부는 여전히 결정 대기.
