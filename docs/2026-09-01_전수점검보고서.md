# 메시지페이 전수 점검 보고서 (2026-09-01)

대상: `E:\프로젝트\메시지페이` / 커밋 `9bde878` 기준 작업 트리
범위: `src/**` 42,073줄(생성 코드 제외), `prisma/**`(스키마 1,625줄 + 마이그레이션 41개), `tests/**` 22개 파일 271 케이스, `tools/**`

## 요약

정적 검사는 깨끗하다(`tsc --noEmit` 통과, eslint 오류 2건은 규칙 위반이고 실행 결함은 아니다). 스키마와 마이그레이션 사이 드리프트도 사실상 0이었다. 반면 **돈이 나가는 경로 — 지급대행·정산 회차 처리 — 에 실제 손실로 이어지는 결함이 모여 있다.** 결제 수신 쪽(멱등성, 상태 선점, 한도 예약)은 설계가 정확하고 DB 제약으로 방어선이 이중화되어 있는데, 그 뒤 정산·지급 단계가 같은 수준으로 방어되어 있지 않다.

| 심각도 | 건수 | 성격 |
|---|---|---|
| 치명 | 3 | 실연동 전환 시 즉시 금전 손실 또는 무결제 성공 처리 |
| 높음 | 9 | 이중 지급·장부 오류·권한 우회·개인정보 대량 반출 |
| 중간 | 20 | 상태 오염, fail-open, 표시 오류, 운영 가시성 |
| 낮음 | 7 | 규격·정리 항목 |

치명·높음 12건은 보고서 작성자가 해당 코드를 직접 다시 열어 확인했다(아래 ✔ 표시).

---

## 치명 — 실연동 전환 전 반드시

### C-1 ✔ 지급대행 어댑터가 항상 mock 을 반환하고, 그 성공으로 정산이 확정된다
`src/server/adapters/payout/index.ts:104-110`

```ts
export function getPayoutAdapter(): PayoutAdapter {
  if (env.payout.provider !== 'mock') {
    logger.warn('지급대행 실연동 어댑터가 아직 없어 mock 으로 처리합니다.', ...);
  }
  return mockPayoutAdapter;   // env 값과 무관하게 항상 mock
}
```

`PAYOUT_PROVIDER` 를 실사업자로 설정해도 mock 이 `{ok:true}` 를 돌려주고, `auto-settlement.ts:193` 이 `markSettlementPaid()` 를, :196 이 `charge.status='SETTLED'` 를 확정한다. 실제 이체는 0원인데 append-only 원장에 지급 분개가 남아 반대분개 없이는 되돌릴 수 없다. `assertProductionSafety()` 에 `PAYOUT_PROVIDER` 검사도 없어 부팅이 막히지 않는다.

**조치**: 실연동 어댑터가 없는 동안에는 `getPayoutAdapter()` 가 `provider !== 'mock'` 일 때 **throw** 해야 한다. 그리고 `assertProductionSafety()` 에 `PAYOUT_PROVIDER === 'mock'` 검사를 추가한다.

### C-2 ✔ 지급 실패 시 멱등키를 삭제해, 같은 정산분이 두 번 이체될 수 있다
`src/server/services/auto-settlement.ts:186-190`, `:206-212`

```ts
if (!result.ok) {
  await prisma.settlementRequest.update({ ... status: 'PAYOUT_FAILED' ... });
  await prisma.idempotencyKey
    .delete({ where: { scope_key: { scope: 'payout', key: `${merchantId}:${dateKey}` } } })
    .catch(() => undefined);
```

지급대행 계약은 "`requestId` 를 멱등키로 쓰고 결과를 못 받으면 `inquire()` 로 확정하며 재이체하지 않는다"(`adapters/payout/index.ts:11-14`)인데, 실패 처리에서 그날의 멱등키를 스스로 지운다. `settledAt` 도 채우지 않고 `PAYOUT_FAILED` 는 pending 대상이 아니라 가용 잔액이 그대로 복구된다.

**재현**: 실이체 967,000원 → `transfer()` 타임아웃 → `inquire()` 도 미확정(`ok:false`) → `PAYOUT_FAILED` + 멱등키 삭제. 관리자가 `runPayoutBatchAction` 으로 배치를 재실행하면 새 `SettlementRequest`(= 대행사 입장의 새 멱등키)가 만들어져 **967,000원이 한 번 더 나간다.** 원장에는 PAYOUT 1건뿐이라 967,000원 손실.

같은 결함의 다른 창: `transfer()` 성공 직후 `markSettlementPaid()` 전에 프로세스가 죽으면 회차는 `APPROVED` 로 남고 `settledAt` 도 비어 다음 배치가 또 이체한다. **`transfer()` 호출 직전에 "이체 시도함" 을 영속화하는 지점이 코드 어디에도 없다.**

**조치**: 실패 경로에서 멱등키를 지우지 않는다(상태만 `FAILED` 로 표시). 재시도는 `retryPayout` 으로만 하고, `transfer()` 직전에 회차를 `PAYOUT_ISSUED` 같은 중간 상태로 커밋한다.

참고: `auto-settlement.ts:1` 이 `withAdvisoryLock` 을 import 하지만 파일 전체에서 한 번도 쓰지 않는다(lint 경고로도 잡혔다). 즉 이 경로의 유일한 방어가 그 멱등키다.

### C-3 ✔ `SAFE_MODE` 기본값이 `true` 인데 운영 부팅 점검이 검사하지 않는다
`src/lib/env.ts:198` (`safeMode: bool('SAFE_MODE', true)`), `:231-262` (`assertProductionSafety`)

부팅 점검은 `PAYMENT_PROVIDER=mock` 은 막지만 `safeMode` 는 보지 않는다. `APP_ENV=prod` + `PAYMENT_PROVIDER=hecto` + 헥토 키 4종을 모두 넣고 **`SAFE_MODE=false` 만 빠뜨리면** 점검을 통과한 뒤 `adapters/payment/index.ts:270` 이 `mockPaymentAdapter` 를, `adapters/mt/index.ts:72` 가 `mockMtAdapter` 를 반환한다. 신호는 `logger.warn` 한 줄뿐이다.

여기에 `ALLOW_LEGACY_WEB_INSTANT_PAY=true` 가 겹치면 mock `approve()` 가 `ok:true` 를 주고 → 원장 분개 생성 → "결제가 완료되었습니다" 문자 → 자동정산이 가맹점에 실제 지급까지 간다. **이용자 통장에서는 1원도 빠지지 않는다.** 절대규칙 2 위반이 운영에서 성립한다.

**조치**: `assertProductionSafety()` 에 `if (env.appEnv === 'prod' && env.payment.safeMode) problems.push(...)` 를 추가한다.

---

## 높음

### H-1 ✔ 일괄 "지급완료"에 상태 검사가 없어 원장이 이중 차감된다
`src/app/actions/admin/settlement.ts:159-163`

```ts
} else {
  // PAY: 승인 건만 지급 완료. 잠금·재검증은 markSettlementPaid 내부에서 처리.
  await markSettlementPaid(id, admin.id);
```

주석과 달리 `req.status` 를 전혀 보지 않는다. 단건 액션(`:65`)은 `if (before.status !== 'APPROVED') throw` 로 막는데 일괄 경로만 뚫려 있고, `markSettlementPaid`(`settlement.ts:575`)는 `APPROVED` 와 **`PAYOUT_FAILED` 를 모두 통과시킨다**(재시도용으로 의도된 허용).

**재현**: R1 이 이체 실패로 `PAYOUT_FAILED`(원장 환입) → 다음 배치가 R2 로 실제 483,500원 이체 완료 → 관리자가 목록에 남은 R1 을 함께 체크해 "일괄 지급완료" 실행 → `PAYOUT −483,500` + `PAYOUT_WITHHOLDING −16,500` 이 **한 번 더** 기록된다. `assertPayable` 의 잔액 부족은 `warnings` 로만 남고 분개는 무조건 쌓인다(`settlement.ts:579-604`, 의도된 설계). 결과: 실제 지급 1회, 장부 차감 2회 → 가맹점이 500,000원을 영구히 덜 받는다.

### H-2 ✔ `retryPayout` 이 지급액보다 많은 결제 건을 SETTLED 로 잠근다
`src/server/services/auto-settlement.ts:343-356`

```ts
for (const c of charges) {
  if (remaining <= 0n) break;
  ids.push(c.id);          // 검사 전에 먼저 담는다
  remaining -= c.netAmount;
}
```

정상 배치 루프(`:120-125`)는 `if (amount + c.netAmount > available) break` 로 넘치면 안 담는데, 재시도 루프만 담고 나서 뺀다. 실패~재시도 사이 대상 목록이 바뀌면 실제 지급액보다 많은 건이 잠기고, `findDueCharges` 는 `settledAt: null` 만 보므로(`:67`) 한 번 잠기면 다시는 지급 대상이 되지 않는다.

**재현**: R1(A 60,000 + B 40,000 = 100,000) 실패 → A 전액 환불 → 재시도 성공(100,000 기준) → 대상이 B(40,000) + C(70,000) 로 바뀜 → B 담고 C 도 담아 remaining −10,000 → C 전액 70,000 이 SETTLED. 실제로 C 몫으로 나간 돈은 60,000뿐, **10,000원이 영원히 지급되지 않는다.**

### H-3 ✔ 승인 성공 후 정산 트랜잭션이 실패하면 "출금됐는데 기록 없음"에서 복구 경로가 없다
`src/server/services/charge-flow.ts:1114-1160`

`adapter.approve()` 로 실제 출금이 끝난 뒤 여는 정산 트랜잭션에 `maxWait`/`timeout` 이 지정되어 있지 않다(바로 위 판정 트랜잭션은 `:945-951` 에서 명시적으로 지정한 것과 대조). 여기서 예외가 나면 `paymentTransaction.status` 는 기본값 `REQUESTED`, `charge.status` 는 `PENDING_PAYMENT` 로 남는다.

그런데 수동 대사는 `RECONCILABLE = ['UNKNOWN','TIMEOUT']` 만 받는다(`payment-reconcile.ts:36, 59`). 관리자 대사 큐도 `UNKNOWN + TIMEOUT` 만 집계한다(`admin/payments/page.tsx:205`). 결제사 재전송이 와도 PIN 세션이 이미 COMPLETED 라 DUPLICATE 로 끝난다. **자동·수동 어느 쪽으로도 회수할 수 없다.**

**조치**: 이 트랜잭션에 명시적 timeout 을 주고, 실패 시 거래를 `UNKNOWN` 으로 표시해 대사 큐로 넘긴다.

### H-4 ✔ 인증 없는 서버 액션으로 남의 닉네임을 바꿀 수 있다 (IDOR)
`src/app/actions/confirm.ts:62-84`

`updatePayerNicknameAction(payerId, nickname, snsPlatform)` 는 `'use server'` export 인데 세션 확인도 `payerId` 소유권 확인도 없다. 같은 파일의 `confirmChargeAction` 은 1회용 토큰으로 방어하는데 이 액션만 빠졌다. `payerId` 원문은 이미 화면에 있다 — `studio/messages/page.tsx:207`, `studio/charges/[id]/page.tsx:144`, `studio/moderation/page.tsx:197` 이 hidden input 으로 렌더한다.

**재현**: 가맹점 계정으로 `/studio/moderation` 에서 타인의 `payerId` 를 읽고 그 값으로 액션을 호출하면 그 이용자의 표시명이 플랫폼 전역에서 바뀐다. 이후 결제 건에 그 이름이 박제되어(`charge-flow.ts:527`) **다른 가맹점 화면에도** 노출된다. 피해자에게 알림은 없다.

### H-5 ✔ 파트너 조회 API 가 서명 없이 휴대폰 평문·배송지 실명/주소를 대량 반출한다
`src/app/api/partner/v1/charges/route.ts:116, 152-155` / `src/server/services/partner-auth.ts:133-139`

"해시+암호화+마스킹 3분리" 원칙에서 GET 조회만 예외다. GET 은 `writeMethod === false` 라 타임스탬프·서명·재전송 방어가 전부 선택이고, 응답에 `decrypt(phoneEnc)` 와 `shipping.receiver / phone / address` 평문이 함께 실린다. 별도 감사 로그도 없다(`lastUsedAt` 만 갱신).

**재현**: 유출된 API 키 하나로 `GET /charges?status=all&limit=500` 을 커서로 반복 호출 → 해당 가맹점 전 이용자의 휴대폰 원문·수령인 실명·주소를 시간당 최대 15만 건(300req/분 × 500) 수집. 응답에 이미 `payerPhoneMasked` 와 `payerRef`(phoneHash)가 함께 내려가므로 평문 필드는 옵트인으로 뺄 수 있다.

### H-6 ✔ READ_ONLY 를 제외한 모든 등급의 관리자가 계정을 탈취할 수 있다
`src/app/actions/admin/accounts.ts:64-86`

`issueTemporaryPasswordAction` 은 대상이 **ADMIN 일 때만** SUPER_ADMIN 을 요구하고, 그 외에는 READ_ONLY 차단만 통과하면 된다. 발급된 임시 비밀번호는 응답 `detail.tempPassword` 로 화면에 그대로 표시된다. (참고: 같은 파일의 정산 일괄 액션은 SUPPORT 를 명시적으로 막는데 여기만 빠졌다.)

**재현**: SUPPORT 등급 관리자가 임의 가맹점 회원의 `userId` 로 임시 비밀번호를 발급 → 직접 로그인 → `/studio` 에서 정산 요청·상품 변경·연동 API 키 신규 발급까지 수행. 감사로그에는 `USER_TEMP_PASSWORD_ISSUE` 한 줄만 남고 이후 행위는 전부 그 가맹점이 한 것으로 기록된다.

### H-7 헥토 승인 응답에서 HTTP 상태를 버리고 본문 코드만으로 "확정 실패"를 판정한다
`src/server/adapters/payment/hecto.ts:365-393` (`postJson` 은 `:187` 에서 `ok`/`status` 를 돌려주지만 `approve` 가 쓰지 않는다)

5xx·게이트웨이 오류처럼 "승인 여부 불확실"인 응답이 전부 확정 실패가 된다. UNKNOWN 안전망은 본문 파싱이 실패해 코드가 우연히 `'UNKNOWN'` 이 될 때만 작동한다.

**재현**: 결제사가 승인은 원장에 올린 뒤 HTTP 500 + `{"outRsltCd":"9999"}` 반환 → `charge-flow.ts:1093-1106` 하드 실패 경로 → 트랜잭션 FAILED, 한도 되돌림, 실패 카운터 증가, 이용자에게 "결제 실패" 문자. **실제 출금은 유지되는데 UNKNOWN 큐에도 안 잡힌다.**

### H-8 정산 원장 append-only 트리거가 TRUNCATE / DISABLE TRIGGER 로 뚫린다
`prisma/migrations/20260831200000_rename_domain_models/migration.sql:82-85`

트리거가 `BEFORE UPDATE OR DELETE ... FOR EACH ROW` 로만 걸려 있고 `ENABLE ALWAYS` 가 아니다. 행 트리거는 TRUNCATE 를 가로채지 못한다. 앱 롤이 트리거를 끌 권한을 가지고 있다는 것은 `tests/helpers.ts:21-23` 이 매 테스트마다 `DISABLE TRIGGER` 를 실행하는 것으로 코드상 증명된다.

**조치**: ① `BEFORE TRUNCATE ... FOR EACH STATEMENT` 트리거 추가 ② `ENABLE ALWAYS TRIGGER` ③ 런타임 롤에서 `REVOKE UPDATE, DELETE, TRUNCATE ON settlement_ledger`(테스트는 소유자 롤 사용). 기존 SQL 은 고치지 말고 **새 마이그레이션**으로 추가한다.

### H-9 부분 유니크 인덱스 6개가 schema.prisma 에 없어 다음 `db:migrate` 가 DROP 을 만들 수 있다
`prisma/migrations/20260819100100_guards_and_indexes/migration.sql:24-43`, `20260821090000_audit_fixes/migration.sql:38-39`

해당 인덱스: `payment_transaction_approved_uniq`(결제 1건당 APPROVED 1건 — **중복 결제 최종 방어선**), `payment_method_token_active_uniq`, `merchant_mo_number_dedicated_uniq`, `merchant_code_active_uniq`, `banned_word_global_uniq`, `banned_word_merchant_uniq`.

Prisma 는 `WHERE` 절 인덱스를 데이터 모델로 표현하지 못한다. **이 저장소는 같은 사고를 이미 한 번 겪었다** — `20260821090000` 주석이 "raw SQL 인덱스 3개가 schema.prisma 에 없어 다음 migrate 때 DROP 이 자동 생성되는 상태였다"고 적고 `@@index` 선언을 추가해 막았다. 부분 유니크는 그 대책 자체를 쓸 수 없다.

**조치**: `tools/` 에 부분 인덱스 존재 확인 스크립트를 두고 배포 게이트로 건다. `npm run db:migrate` 로 생성된 SQL 에 `DROP INDEX` 가 들어가는지 매번 확인한다.

---

## 중간

| # | 위치 | 내용 |
|---|---|---|
| M-1 ✔ | `api/partner/v1/charges/shipment/route.ts:76-86` | 상태만 보내도 `carrier`/`trackingNo`/`memo` 가 null 로 덮이고 `deliveredAt` 이 초기화된다. 주석의 "멱등" 계약과 어긋난다 |
| M-2 ✔ | `actions/studio.ts:1259` | `deliveredAt: status==='DELIVERED' ? now : null` — 같은 주문을 다시 저장할 때마다 배송 완료 시각이 갱신된다(바로 윗줄 `shippedAt` 은 최초값을 보존) |
| M-3 ✔ | `services/payment-reconcile.ts:235-278` | 대사에서 "취소"로 확정할 때 `rollbackCounters` 는 하는데 재고 복구가 없다. 승인 실패 경로(`charge-flow.ts:1101`)와 환불 경로는 모두 복구한다 |
| M-4 | `services/settlement.ts:151-152, 177` | `vatIncluded=true`(기본값)면 `feeVat`가 0으로 저장되어 **수수료에 포함된 부가세 금액이 장부 어디에도 남지 않는다.** 3,000원/15% 기준 건당 41원, 월 10만 건이면 410만원의 매출 부가세 근거가 없다 |
| M-5 | `services/auto-settlement.ts:120-125` | 지급 대상 선정이 `break` 라, 앞의 큰 건 하나가 잔액에 안 들어가면 뒤의 작은 건 전부가 무기한 막힌다(잔액 100,000 / A 150,000 → B·C 60,000 이 한 푼도 안 나감) |
| M-6 ✔ | `rate-limit.ts:19-27, 54, 57` | XFF 마지막 홉을 무조건 신뢰 → 헤더를 바꿔가며 로그인·가입·재설정 제한을 전부 우회. 헤더를 아예 안 보내면 `key=null` 이라 **무조건 통과**. Redis 오류 시에도 `catch → ok:true` |
| M-7 | `partner-auth.ts:124,155`, `cron/*.ts` | Redis 장애 시 파트너 API nonce(재전송 방어)와 cron 실행 잠금이 `.catch(() => true)` 로 열린다. 운영은 `ALLOW_INMEMORY_FALLBACK=false` 라 예외가 나는데 그 예외를 삼킨다 |
| M-8 ✔ | `services/idempotency.ts:48-51` | `create` 의 **모든** 예외를 유니크 위반으로 간주해 DUPLICATE 반환. 커넥션 풀 고갈 시점의 MO 1건이 결제도, 링크도, 안내 문자도 없이 영구 유실된다(재전송해도 DUPLICATE 로 차단) |
| M-9 | `services/limits.ts:355-369` | `failCount` 가 시간 경과로 감쇠하지 않아, 1월·4월·8월의 잔액부족 3건으로 이용자가 **1년** 잠긴다 |
| M-10 | `studio/settlement/page.tsx:90-93` | `paidAt` 만 보고 status 를 안 걸러, 지급 실패로 환입된 회차가 가맹점 캘린더에 "지급완료"로 표시되고 "지급 예정"에도 이중으로 잡힌다 |
| M-11 ✔ | `prisma/schema.prisma:531` | 사업자등록번호만 3분리에서 빠져 평문 저장이고, `admin/merchants/[id]/page.tsx:96` 이 10자리 전체를 마스킹 없이 출력한다 |
| M-12 | `services/charge-select.ts:126` | 전역 한도 정책을 좁히면 범위 밖 상품이 목록에서 사라지는데, 상품 비활성화도 안 되고 "고를 게 없음" 안내도 없다 → 금액 선택 화면이 빈 화면이 되어 결제를 끝낼 수 없다 |
| M-13 | `charge-flow.ts:170` + `studio/moderation/page.tsx:56-92` | 금칙어 BLOCK 을 전부 MASK 로 내리는 정책 변경이 화면 안내에 반영되지 않았다("결제로 접수되지 않으며", "차단으로 바꿀 수 있습니다" — 선택지에 BLOCK 이 없다). `studio.ts:734, 799, 930` 은 도달 불가 죽은 코드 |
| M-14 | `lib/labels.ts:45-47` | 도달 불가 상태 `송출대기/송출완료/일부 송출실패` 가 `/studio/charges` 필터에 그대로 노출된다. 옛 "방송 노출" 용어이기도 하다(규칙상 **충전 반영**) |
| M-15 | `api/cron/cleanup/route.ts:85-97` | 부분 실패가 항상 HTTP 200 이라 스케줄러 알람이 안 울린다. `expireStalePinSessions()` 가 매 분 throw 해도 성공으로 집계된다 |
| M-16 | `services/auto-settlement.ts:378-409` | 관리자 자동지급 대시보드가 `take: 200` 으로 잘리고(배치 자체엔 take 없음) `scheduledAmount` 에 잔액 상한을 안 걸어, 표시 금액과 실제 이체가 어긋난다 |
| M-17 | `prisma/schema.prisma:947-956` | `charge` 에 `(merchant_id, paid_at)` 인덱스가 없어 파트너 폴링 API 가 매 호출마다 그 가맹점 전 결제를 읽고 Sort 한다 |
| M-18 | `services/mt-templates.ts:501-524` | 커스텀 본문 길이 검증이 **치환 전** 400자만 본다. `{메시지}` 80회면 치환 후 6,000자가 되는데 `decideMessageType` 은 LMS 2,000바이트 상한을 확인하지 않는다(실사업자 연동 후 전건 거절) |
| M-19 | `services/charge-confirm.ts:107-121` | `PENDING_AMOUNT` 결제 건 만료 정리 경로가 없다. PIN 대기·확인 대기에는 있는 처리가 이 단계에만 없어 "금액선택대기"가 무한히 쌓인다 |
| M-20 | `lib/env.ts:45-58` | 주석은 "미설정·오타면 안전한 쪽(prod)으로 판정"이라고 하는데 코드는 `NODE_ENV !== 'production'` 이면 **local** 로 떨어진다. 정식 배포는 NODE_ENV=production 이라 안전하지만, 개발 서버를 노출하면 테스트 로그인 버튼·`/api/dev/outbox`(타인의 보안링크·OTP 평문)·MO 시뮬레이터가 함께 열린다 |
| M-21 | `prisma/seed.ts:213-222` | 시드 보충 실행 시 MO 수신번호가 **옛 가맹점에 묶인 채** 남는다. 브랜드 전환으로 코드·이메일이 바뀐 지금 preview DB 를 재사용하면 새 가맹점에 수신번호가 하나도 배정되지 않는다 → `npm run db:reset` 필수 |

---

## 낮음 / 정리

- `actions/studio.ts:383` — 충전 상품 `imageUrl` 만 URL 스킴 검증이 빠졌다(프로필·배너는 모두 검증). 결제 화면을 여는 모든 이용자의 IP/UA 가 제3자 도메인에 전달된다.
- `api/media/[name]/route.ts:34-39` — `X-Content-Type-Options: nosniff` 가 없다. 업로드는 매직바이트로 검사하지만 `GIF89a` 폴리글롯은 통과한다.
- lucide 아이콘 규격 위반: `strokeWidth` 82곳(1.8×54, 2×11, 2.2×5 등), `size` 90여 곳(12~15, 21~42). `components/studio/save-feedback.tsx:129,159,167` 은 한 컴포넌트 안에서 2.2/2/1.7 을 섞어 쓴다. (이모지는 전 코드베이스 0건 — 규칙 준수)
- `app/c/[code]/page.tsx:451` — 아바타 기본 이니셜에 `'후'`(후원자) 잔재. 앱 코드에 남은 유일한 옛 용어 표시 문자열이다.
- `studio/settings/page.tsx:559` — 이 화면만 `toLocaleString('ko-KR')` 을 쓴다(다른 화면은 `formatKst`). 컨테이너 TZ 가 UTC 라 "마지막 사용"이 9시간 이르게 표시된다.
- `prisma/schema.prisma:776, 1163-1164` — `MtOutboundMessage.merchantId`, `RiskDetection.merchantId/chargeId` 에 FK 가 없어 가맹점 삭제 시 고아 레코드가 남는다.
- `charge` 하위 6개 테이블이 전부 `onDelete: Cascade` 라, 오래된 실패 건을 지우면 `payment_transaction`(providerTid, resultCode)과 `payment_attempt`(PG 요청·응답 스냅샷)까지 사라져 결제사 대사 근거가 없어진다. `Restrict` 가 맞다.
- eslint 오류 2건: `admin/products/page.tsx:50`(서버 컴포넌트의 `Date.now` — 규칙 오탐에 가깝다), `r/[token]/select-amount-panel.tsx:103`(effect 내 setState 연쇄).
- 테스트 11개 파일의 `handleMoInbound`/`mockMoAdapter` 미사용 import — MO 테스트가 삭제된 흔적이 **아니다.** `tests/helpers.ts:181-183` 의 `inboundAndSelect()`/`inboundAndPay()` 헬퍼로 리팩터링한 뒤 상단 import 만 남은 것이다. import 문 삭제로 끝난다.

---

## 테스트 커버리지

22개 파일 / 271 케이스 / 실 DB 통합 방식.

**덮고 있다**: MO 수신 → 금액 선택 → PIN 인증 → 결제 → 원장 반영 전 구간, 멱등·중복 방어, 대표번호+키워드 라우팅, 보안링크, 한도·속도제한·쿨다운, 금칙어(우회 표기·ReDoS), 수수료·부가세·원천징수 3.3%, 정산 요청→승인→지급→실패 재시도→지급일 정책, 파트너 API 서명/ack, 어댑터 fail-closed, 로그 마스킹, UNKNOWN 수동 대사, 실물 상품(배송비·재고·옵션·환불 시 재고 복원).

**비어 있다**: `src/app/actions/**` 26개 중 테스트가 직접 import 하는 것은 `web-charge.ts`·`web-charge-pin.ts` **둘뿐**이다. `studio.ts`(1,276줄)와 `admin/*`(2,100여 줄)에 대한 직접 테스트는 0건 — 즉 **H-4, H-6, M-2 같은 소유권 검증·권한 분리·폼 검증 결함은 테스트 그물에 아예 걸리지 않는다.** RSC 화면 렌더 테스트도 없어 M-10·M-12·M-13·M-14 같은 화면-동작 불일치도 잡히지 않는다.

가장 비용 대비 효과가 큰 보강은 서버 액션의 **권한 경계 테스트**다(각 액션을 비인증·타 가맹점·READ_ONLY/SUPPORT 세션으로 호출해 거절되는지).

---

## 점검 방법과 한계

- `npx tsc --noEmit` 통과. `npx eslint .` 오류 2 / 경고 63.
- 마이그레이션 41개를 순서대로 파싱해 각 문장이 참조하는 객체의 존재를 추적했고, 최종 47개 테이블이 schema.prisma 의 47개 모델과 일치함을 확인했다(매핑 컬럼 304개, 인덱스·유니크 이름, FK `onDelete` 전부 대조). **빈 DB 순차 적용과 드리프트는 문제 없다.**
- **통합 테스트(`npm test`)와 프로덕션 빌드는 실행하지 못했다.** node_modules 네이티브 바인딩이 win32-x64 이고 리눅스 셸에서 PostgreSQL 에 닿지 않는다. Windows 에서 `도구_테스트실행.bat` 으로 271개 케이스를 직접 돌려 확인해야 한다.
- 위 발견 중 치명·높음 12건은 코드를 다시 열어 인용 줄을 직접 확인했다(✔). 중간 이하는 리뷰 단계에서 확인한 내용으로, 조치 전에 해당 줄을 한 번 더 보는 것을 권한다.

---

## 잘 되어 있는 점

- **중복 승인의 최종 방어선이 애플리케이션이 아니라 DB 에 있다.** `mo_inbound_message.provider_message_id` UNIQUE, `idempotency_key(scope,key)` UNIQUE, `payment_pin_session.charge_id`/`session_id` UNIQUE, 부분 유니크 `payment_transaction_approved_uniq`. 개명 마이그레이션이 이것들을 온전히 보존했다.
- 상태 선점을 전부 조건부 `updateMany` 로 처리해(보안링크 1회 사용, PIN 세션 PENDING→COMPLETED, 환불 REQUESTED→APPROVED, 대사 UNKNOWN→APPROVED) 동시 클릭·중복 콜백·관리자 동시 처리가 승인 단계로 두 번 넘어가지 못한다.
- 결과 미확인(UNKNOWN)을 실패로 덮지 않고 관리자 대사 큐로 넘기는 판단, 한도 재검사부터 집계 예약까지를 이용자 행 `FOR UPDATE` 로 직렬화한 설계, 재고를 승인 요청 **전에** 잠금 안에서 선점하는 순서는 정확하다.
- 충전 반영을 가맹점 pull(파트너 조회 API + `pointStatus` 재노출)로 완전히 분리해, 반영 실패가 결제 상태를 오염시키지 않고 재시도가 자연스럽게 큐 역할을 한다.
- 원천징수를 소득세·지방소득세 각각 산출 후 각각 10원 절사하는 국내 실무 방식으로 정확히 구현했고, 환불 수수료 환입을 재계산이 아니라 **원 거래의 실제 분개 비율**로 산출해 요율 변경·부분환불에도 총 환입액이 원 차감액을 넘지 않는다.
- 세션·보안링크·재설정 토큰·파트너 API 키를 전부 HMAC 해시로만 저장하고, 재설정 성공 시 전 세션 폐기 + 잔여 링크 무효화를 한 트랜잭션으로 처리한다. 관리자 액션 25종이 예외 없이 `requireWriteAdmin` 을 거치고, 스튜디오 액션과 CSV 내보내기는 모두 `merchantId` 조건을 직접 걸어 IDOR 을 닫았다.
- 업로드는 확장자·MIME 대신 매직바이트로 판정하고 파일명을 32바이트 난수로 재생성하며, 로거가 메시지와 meta 양쪽에서 URL·전화번호·비밀키를 지운다.
