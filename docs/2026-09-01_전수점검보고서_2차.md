# 메시지페이 2차 전수 점검 보고서 (2026-09-01)

1차 보고서(`docs/2026-09-01_전수점검보고서.md`)에 이어 **전체를 다시** 점검했다. 1차와 다른 점은 두 가지다.

1. **이번에는 실제로 돌렸다.** 리눅스 컨테이너에 의존성을 새로 설치하고, PostgreSQL 16 을 띄우고, 마이그레이션 41개를 빈 DB 에 적용하고, 테스트 270개를 실행했다. 1차 때는 정적 읽기만 가능했다.
2. **1차가 덜 본 축을 팠다.** 성능·확장성, 장애 시 오작동, 비즈니스 규칙 정합성, 테스트가 실제로 보장하는 것, 접근성·결제 완주율, 관리자 등급 경계.

그 결과 **1차 보고서의 지적 1건을 철회**하고(아래 "정정"), 신규로 치명 3 · 높음 7 · 중간 10건을 찾았다.

---

## 실행 결과 (사실)

| 검사 | 결과 |
|---|---|
| `tsc --noEmit` | **통과** (오류 0) |
| `eslint .` | 오류 2 / 경고 62 — 1차와 동일, 실행 결함 아님 |
| `prisma migrate deploy` (빈 DB) | **41개 전부 성공** — 1차의 정적 분석 결론이 실측으로 확인됨 |
| `vitest run` | **270개 중 268 통과, 2 실패** |

실패 2건 중 `profile-avatar.test.ts` 는 점검 환경에 `public/` 이미지를 복사하지 않아 생긴 것으로 **실제 결함이 아니다.** 나머지 1건은 진짜다 → 아래 N-1.

DB 실측으로 확인한 것:
- `public_holiday` 31행, 2026-01-01 ~ **2027-12-25** (마이그레이션이 시드한다)
- 부분 유니크 인덱스 6개 모두 생성됨 (`payment_transaction_approved_uniq` 포함)
- `charge` 테이블 인덱스 10개 — `(merchant_id, paid_at)` **없음**

---

## 1차 보고서 정정

### 철회: H-9 "부분 유니크 인덱스 6개가 다음 `db:migrate` 때 DROP 될 수 있다"

**재현되지 않는다.** 실제로 확인한 절차:

```
$ npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script
-- This is an empty migration.

# 스키마에 필드를 하나 추가한 뒤 다시:
$ npx prisma migrate diff --from-config-datasource --to-schema <patched> --script
-- AlterTable
ALTER TABLE "system_setting" ADD COLUMN     "audit_probe" TEXT;
```

Prisma 7.10 은 데이터모델에 없는 부분 인덱스를 DROP 대상으로 잡지 않는다. `20260821090000_audit_fixes` 주석이 기록한 과거 사고는 이전 Prisma 버전의 동작이다. **조치 불필요.** (다만 Prisma 메이저 업그레이드 시에는 이 diff 를 한 번 다시 확인하는 게 좋다.)

### 해소: `generated-avatar.tsx` 의 없는 파일 참조

사용자가 이미 고쳤다. 현재 `CHARACTER_SHEETS` 는 `/avatars-donaido-*.png` 로 되돌아가 있고, "파일 자체는 더 이상 없고 이 목록은 잘못 저장된 avatarUrl 을 걸러내는 용도"라는 주석이 붙었다. 올바른 처리다.

### 유효 확인: 1차의 나머지 치명·높음 12건은 이번에도 코드에서 그대로 확인됐다.

---

## 신규 — 치명

### N-1 ✔실행입증 동시 MO 문자로 이용자 생성이 충돌한다 — 저장소의 테스트가 실제로 실패한다
`src/server/services/charge-flow.ts:243-255`

```ts
async function getOrCreatePayer(phone: string) {
  const ph = hashPhone(phone);
  return prisma.payerProfile.upsert({ where: { phoneHash: ph }, update: {}, create: {...} });
}
```

Prisma 의 `upsert` 는 이 형태에서 원자적이지 않다. 같은 새 번호로 MO 가 동시에 들어오면 두 요청이 모두 "행 없음"을 보고 INSERT 해 `payer_profile_phone_hash_key` 유니크 위반이 난다.

**실행 결과:** `tests/payer-onboarding.test.ts > 가입 전 MO가 겹쳐도 가입 링크 MT는 최초 1건만 발송한다` 를 3회 돌려 **1회 실패**했다(약 1/3 확률).

```
PrismaClientKnownRequestError:
Invalid `prisma.payerProfile.upsert()` invocation in charge-flow.ts:245
Unique constraint failed on the constraint: `payer_profile_phone_hash_key`
 ❯ processMoRow src/server/services/charge-flow.ts:420
```

바로 아래 `claimRegistrationGuide`(`:258-264`)는 "같은 전화번호의 동시 MO 중 한 요청만 발송권을 얻는다"고 조건부 `updateMany` 로 정교하게 막아 놨는데, **그 앞 단계인 이용자 행 생성 자체가 경합에 열려 있다.** 운영에서는 그 문자가 `result=ERROR` 로 기록되고 이용자는 아무 응답을 못 받는다(재전송하면 복구되긴 한다 — `charge-flow.ts:313-315`).

**조치:** `create` 를 try/catch 로 감싸 P2002 이면 `findUnique` 로 재조회하거나, `$executeRaw` 로 `INSERT ... ON CONFLICT (phone_hash) DO NOTHING` 후 조회한다.

**부수 효과:** 지금 `도구_테스트실행.bat` 은 3번에 1번꼴로 빨간불이 뜬다. 이걸 "가끔 그러는 것"으로 넘기면 진짜 회귀도 같이 묻힌다.

### N-2 ✔ 정지시킨 회원·가맹점이 문자결제는 계속 받는다 (제재 우회)
`src/server/services/charge-flow.ts:411` · `src/server/services/limits.ts:132` ↔ `src/app/c/[code]/page.tsx:80-84`

공개 결제 페이지는 `user: { status: 'ACTIVE' }` 를 확인하고, **주석이 그 이유를 정확히 적어 놨다**: "merchantProfile.status 만 보면 User 를 SUSPENDED 로 제재해도 샵이 계속 열려 결제를 받고 정지 계정에 돈이 계속 쌓인다." 그런데 정작 결제가 일어나는 세 경로는 그 검사를 하지 않는다.

- 가맹점: `charge-flow.ts:411` 은 `merchant.status !== 'APPROVED'` 만 본다. `web-charge.ts:59-62` 도 같다.
- 이용자: `checkLimits` 의 입력 타입(`limits.ts:132`)에 연결된 `User.status` 가 아예 없다. `updateUserStatus`(`actions/admin/accounts.ts:33-40`)는 `User.status` 와 세션만 바꾼다.

**재현:** 부정 이용자를 `/admin/users` 에서 SUSPENDED 로 제재한다 → 화면은 "회원 상태를 변경했습니다" → 그 이용자는 가맹점 MO 번호로 문자를 보내 금액 선택·PIN 입력·출금까지 정상 진행한다. 막힌 것은 웹 로그인뿐이다. 실제 결제 차단은 `/admin/payers` 의 **별도 화면**에서 `setPayerBlock`(`accounts.ts:134`)을 눌러야만 걸린다. 가맹점 쪽도 같아서, 정지된 가맹점 계정에 결제가 계속 쌓이고 `runScheduledPayouts` 가 자동 지급까지 내보낸다.

### N-3 ✔ 관리자 등급 경계가 세 곳에서 뚫려 있다
`src/app/actions/admin/accounts.ts:20-52, 203` · `policy.ts:49,119` · `transactions.ts:22,72,108`

이 코드베이스는 등급 검사를 대체로 성실하게 한다 — 정산 처리·수수료 정책·환불 승인·결제 수동확정은 물론 공휴일 등록까지 SUPPORT 를 명시적으로 배제한다. 그래서 빠진 곳이 더 눈에 띈다.

| 액션 | 문제 | 결과 |
|---|---|---|
| `updateUserStatus:25-31` | 대상의 `role` 을 `select` 해놓고 **한 번도 쓰지 않는다** (바로 아래 `issueTemporaryPasswordAction:84` 는 `role==='ADMIN'` 이면 SUPER_ADMIN 을 요구한다) | SUPPORT 관리자가 SUPER_ADMIN 전원을 SUSPENDED 로 만들 수 있다. 세션이 즉시 폐기되고 재로그인도 막히며, 등급 복구 액션은 SUPER_ADMIN 전용이라 **DB 직접 수정 없이는 복구 불가** |
| `saveLimitPolicy:49` · `toggleLimitPolicy:119` | 등급 검사 없음. 금액 파서 `money()`(`shared.ts:69-75`)에 **상한이 없다** | 자기 결제 프로필에 PAYER 스코프 정책을 넣어 일·월 한도·속도제한·쿨다운·실패잠금을 통째로 해제할 수 있다. 반대로 GLOBAL 에 `payerDailyLimit=1` 을 넣으면 전 서비스 결제가 즉시 차단된다 |
| `updateMerchantStatus:203` + `createMoNumber/assignMoNumber`(`transactions.ts:22,72,108`) | KYC 게이트에 등급 검사 없음 | 자기 명의로 가맹점을 신청한 뒤 **스스로 승인**하고 MO 번호까지 배정해 실제 소비자 결제를 받을 수 있다. 역방향으로 정상 가맹점 번호를 `RECLAIMED` 로 회수하면 그 가맹점의 문자결제가 즉시 전면 중단된다 |

---

## 신규 — 높음

### N-4 ✔ 결제수단을 해지하면 안내대로 문자를 보내도 다시 등록할 수 없다
`src/server/services/payer-registration.ts:269` ↔ `src/server/services/charge-flow.ts:261, 478-493`

`/my/account` 는 세 군데에서 "가맹점의 결제 수신번호로 문자를 보내면 계좌 등록용 1회용 보안 링크가 문자로 발송됩니다"라고 안내한다. 그런데 해지는 `onboardingStatus` 를 `SUSPENDED` 로 만들고, 등록 링크를 발급하는 `claimRegistrationGuide` 는 `UNREGISTERED` 인 이용자만 선점한다. **`SUSPENDED → UNREGISTERED` 로 되돌리는 코드가 코드베이스 전체에 없다**(`onboardingStatus` 전수 grep 확인).

**재현:** 해지 → 안내대로 문자 전송 → `charge-flow.ts:478-493` 분기를 타서 등록 링크 대신 "이용 상태를 확인하거나 고객센터로 문의해 주세요" 문자만 온다. 몇 번을 보내도 같다. PC 결제 페이지의 전화번호 인증 경로(`web-charge.ts:216`)만 상태와 무관하게 링크를 주므로, 문자만 쓰는 이용자는 영구 차단된다.

### N-5 ✔ 한도 집계를 잡는 시각과 되돌리는 시각이 경로마다 다르고, 카운터가 음수까지 내려간다
`charge-flow.ts:1099`(`reservedAt`) · `payment-reconcile.ts:258`(`txn.requestedAt`) · `refund.ts:158`(`charge.paidAt`) · `limits.ts:339-352`

`chargeCounter` 의 키는 `kstDateKey(at)`/`kstMonthKey(at)` 로 정해지는데, 같은 결제 1건을 세 함수가 서로 다른 시각으로 더하고 뺀다. `rollbackCounters` 는 `updateMany` + `decrement` 라 하한이 없다.

**재현:** 1/31 결제(1월 카운터 예약) → PG 타임아웃으로 UNKNOWN → 2/2 관리자가 승인 확정(`paidAt` 이 **2/2** 로 덮인다, `payment-reconcile.ts:114,141`) → 2/5 환불 승인 → `rollbackCounters(..., charge.paidAt)` 가 **2월** 카운터에서 차감. 결과: 1월 한도는 환불했는데도 소진된 채 남고, 이용자는 2월에 환불액만큼 한도를 **더** 쓸 수 있다. 결제가 없던 달이면 카운터가 그대로 음수가 된다.

### N-6 ✔ PIN 인증창 생성이 실패하면 1회용 링크가 이미 소진돼 결제를 끝낼 수 없다
`src/server/services/charge-select.ts:335 → :393`

`confirmChargeAmount` 는 `consumeSecureLink()` 로 링크를 태운 **뒤에** `startPinAuthorization()` 을 호출하고, 실패하면 `{ok:false, message}` 만 돌려준다. 링크는 되돌리지 않는다.

**재현:** 문자로 받은 `/r/[token]` 에서 상품을 고르고 결제 버튼 → 결제사 PIN 창 생성 실패 → "PIN 인증창을 생성하지 못했습니다." 한 줄만 뜨고 버튼은 다시 활성화된다 → 다시 누르면 "이미 사용한 링크입니다." 새로고침해도 같다. 화면 어디에도 "가맹점 번호로 문자를 다시 보내세요"라는 다음 행동이 없다.

관련: 같은 함수가 `notify:false` 로 호출하는데(`:393`), `startPinAuthorization` 은 경합·기존 세션 시 "**이미 발송된 PIN 입력 링크가 있습니다. 받으신 문자에서 진행해 주세요**"(`charge-flow.ts:713-724`)를 돌려준다 — 보낸 적 없는 문자를 기다리게 만든다.

### N-7 ✔ 필수 약관을 세 곳이 다르게 조회한다 — 시행일이 미래인 약관을 등록하면 신규 가입이 통째로 막힌다
`payer-registration.ts:86`(`active && required`) · `app/r/[token]/page.tsx:184`(`active && effectiveFrom <= now`) · `app/terms/page.tsx:16`(`active` 만)

`createTermsVersion`(`actions/admin/policy.ts:173-176`)은 신 버전 등록 즉시 구 버전을 `active=false` 로 내린다.

**재현:** 관리자가 `E_FINANCE` 새 버전을 **시행일 다음 주**로 등록 → 그 순간부터 가입 화면의 동의 목록에서 그 약관 체크박스가 사라지고(시행일 미도래) → 서버 검증은 시행일을 무시하므로 "필수 동의 항목이 누락되었습니다: 전자금융거래 이용약관"으로 **무조건 거절** → 시행일까지 신규 가입 0건. 동시에 `/terms` 공개 페이지는 아직 시행되지 않은 신 버전을 현행 약관으로 노출한다.

덧붙여 **약관이 개정돼도 기존 이용자에게 재동의를 요구하는 코드가 결제 경로 어디에도 없다**(`checkLimits`·`executePayment`·`confirmChargeAmount` 중 `consentRecord` 를 조회하는 곳 0건).

### N-8 ✔ PC 결제 페이지에서 실물 상품이 배송지·배송비 없이 결제된다
`app/c/[code]/page.tsx:91-95, 143` ↔ `components/public/web-charge-pin-panel.tsx:224-243` ↔ `server/services/web-charge.ts:58-135`

`chargeProducts` 를 `kind` 필터 없이 전부 읽어 `{id,name,amount}` 만 웹 패널로 넘긴다. 웹 패널은 금액 칩만 그리고, `createWebCharge` 는 `amount` 만 받는다(`productId`·배송지·`shippingFee` 없음).

**재현:** 가맹점이 "굿즈 티셔츠 25,000원 / 배송비 3,000원"을 등록 → PC 에서 `/c/MSG-XXXX` 접속 → 그 칩을 눌러 결제 완료. 배송지를 한 번도 묻지 않았고 배송비 3,000원도 빠졌다. 가맹점은 보낼 주소가 없다. 모바일 `/r/[token]` 경로는 주소·배송비를 제대로 받으므로 **같은 상품이 화면에 따라 다르게 팔린다.**

### N-9 ✔ 지급일(영업일) 정책에 0 을 저장할 수 있다 — 결제 당일, 그것도 비영업일에 지급된다
`actions/admin/settlement.ts:319` (`settlementDaysRaw < 0` 만 거부) · `lib/business-day.ts:81-91`

`addBusinessDays(key, 0)` 은 `remaining=0` 이라 루프를 한 번도 돌지 않고 `from` 을 그대로 반환한다.

**재현:** 수수료 정책에 `settlementDays=0` 저장 → `settlementDateFor` 가 결제일 자체를 반환 → `findDueCharges` 의 `settlementDateFor(...) <= todayKey`(`auto-settlement.ts:80`)가 즉시 참 → **결제 당일 자동 지급**. 반환일이 토요일이어도 다음 영업일로 밀지 않아 같은 파일의 "정산일은 반드시 영업일" 불변식도 깨진다. PG 로부터 아직 정산받지 못한 자금을 플랫폼이 선지급하게 되고, 환불이 겹치면 회수 불능이다.

**더 나쁜 건** 이 동작이 테스트로 고정돼 있다는 점이다 — `tests/settlement-schedule.test.ts:78-80` 이 `addBusinessDays('2026-08-03', 0, H2026) === '2026-08-03'` 을 정답으로 단언한다.

### N-10 ✔ 전 가맹점 금액범위 일괄 적용에 `where` 가 없고, 되돌릴 기록도 없다
`actions/admin/accounts.ts:342, 354-360`

```ts
await tx.merchantProfile.updateMany({ data: { minAmount, maxAmount } });   // 필터 전무
```
게다가 감사로그에는 `after` 만 남고 가맹점별 `before` 가 없다(개별 액션 `updateMerchantAmountBounds:314-321` 은 남긴다). `money()` 에 상한이 없어 15자리까지 통과한다.

**재현:** 값을 잘못 넣고 실행하면 전 가맹점 범위가 덮이고, 범위를 벗어난 활성 상품이 일괄 `active=false` 되며, 이후 모든 결제가 `AMOUNT_RANGE` 로 실패한다. 원래 값이 어디에도 없어 가맹점마다 개별 문의해야 복구된다. (SUPPORT 등급도 실행 가능 — N-3 과 결합)

---

## 신규 — 중간

| # | 위치 | 내용 |
|---|---|---|
| N-11 ✔ | `app/login/page.tsx:40` | `ERROR_MESSAGES[sp.error] ?? decodeURIComponent(sp.error)` — 화이트리스트 밖 값이 공식 오류 배너에 그대로 렌더된다. ① `/login?error=계정이%20잠겼습니다.%20고객센터%20010-...` 로 **정품 도메인 위 피싱 문구** ② `?error=%` 같은 깨진 인코딩은 `URIError` 로 **로그인 페이지 렌더 자체가 죽는다** |
| N-12 ✔실측 | `prisma/migrations/20260821140000_public_holiday` | 공휴일 데이터가 **2027-12-25 까지만** 있다(31행 실측). 2028-01-01 부터는 공휴일이 빈 집합이 되어 설·추석 연휴가 정산일 계산에서 무시된다 — 지급일이 며칠 앞당겨진다. 갱신 절차나 알림이 없다 |
| N-13 ✔ | `services/auto-settlement.ts:393-397` + `app/admin/settlements/page.tsx:98` | 관리자 정산 페이지가 승인 가맹점 200곳을 **직렬 for 루프**로 돌며 각각 `findDueCharges`(내부 3쿼리, `take` 없음) → **600쿼리 직렬** + 이어서 `getSettlementSummary` 30개 동시. 커넥션 풀은 기본 10개(`server/db.ts:15`)라 **관리자 한 명이 이 페이지를 여는 것만으로 결제 승인이 커넥션을 못 잡는다** |
| N-14 ✔ | `services/settlement.ts:428-432` | `getSettlementSummary` 가 `where: { merchantId }` 만으로 원장 **전 기간**을 groupBy 한다. 스냅샷·이월 잔액 없음. 호출 지점 14곳이고, 그중 `createSettlementRequest:485`·`markSettlementPaid:584` 는 **advisory lock 을 쥔 트랜잭션 안에서** 부른다. 결제 1건당 원장 3행이므로 상위 가맹점은 1년에 550만 행 |
| N-15 ✔실측 | `prisma/schema.prisma:947-956` | `charge` 인덱스 10개를 DB 에서 직접 확인 — `(merchant_id, paid_at)` 이 **없다**. 공개 결제 페이지의 "최근 결제"(`c/[code]/page.tsx:121-129`, `orderBy paidAt desc`), 파트너 폴링 API, 자동정산 대상 조회가 모두 이 조합을 쓴다 |
| N-16 | `adapters/payout/index.ts:90-98` ↔ `tests/auto-settlement.test.ts:192` | mock 지급대행 `inquire()` 가 unknown 을 **항상 성공으로 승격**한다. 그래서 `auto-settlement.ts:174-189`(멱등키를 지우는 그 분기)는 **어떤 테스트도 밟지 않는다.** 실제 지급대행은 이체 직후 조회에서 전파 지연으로 NOT_FOUND 를 흔히 준다 |
| N-17 | `adapters/payment/index.ts:248-255` ↔ `tests/audit-fixes.test.ts:159` | mock 결제 `inquire()` 가 절대 실패하지 않아 UNKNOWN 분기(`charge-flow.ts:1069-1091`)가 한 줄도 실행되지 않는다. "UNKNOWN 으로 남긴다"는 제목의 테스트가 정작 UNKNOWN 단언을 0건 한다. `reconcileUnknownPayment(..., 'APPROVE')` — 원장에 돈을 넣는 쪽 — 은 호출 테스트가 0건 |
| N-18 | `services/refund.ts:94-102` | mock `cancel()` 은 `approvedOrders` 에 있는 주문을 절대 실패시키지 않는데 `approveRefund` 는 APPROVED 거래만 처리하므로 **`if (!res.ok)` 가 절대 참이 되지 않는다.** PG 가 취소를 거절했는데 원장에 반대분개가 쌓이는 회귀를 테스트가 못 잡는다 |
| N-19 | `components/public/web-charge-pin-panel.tsx:258,331` · `web-charge-panel.tsx:212,266,297` · `r/[token]/select-amount-panel.tsx:324-344, 388` | 결제 입력칸에 `<label>`·`aria-label` 이 없고 placeholder 뿐이다. 전화번호에 `type="tel"`·`autoComplete="tel"` 이 없어 모바일 자동완성이 안 뜨고, 배송지 6칸도 `autoComplete` 가 없어 주소를 전부 손으로 친다. 서버 오류 문구에 `role="alert"` 이 없어 스크린리더에는 아무 일도 안 일어난 것으로 들린다. (같은 코드베이스의 `charge-lookup-sheet.tsx:149-158` 은 제대로 하고 있다) |
| N-20 | `src/app/error.tsx:29-49` | `/r/[token]` 하위에 자체 error 바운더리가 없어 루트 경계가 잡는다. 그 화면에는 `1_미리보기실행.bat`·`도구_DB시작.bat`·`.env 의 DATABASE_URL` 안내와 `/api/health` 링크가 뜬다. **문자로 결제 링크를 연 이용자에게 개발자용 배치파일 안내가 노출되고**, 정작 "출금이 됐는지" 안내와 고객센터 경로는 없다. `/c/[code]/error.tsx` 는 제대로 되어 있다 |

---

## 확인 필요

- `actions/pin.ts:24` `submitMockPinAction(sessionId, pin)` — 세션·소유권 검사 없이 `sessionId` 만으로 PIN 인증을 완료 처리한다. `assertMockPaymentAllowed()` 가 `isLocal || PAYMENT_PROVIDER==='mock'` 이므로 **PAYMENT_PROVIDER 기본값이 mock 인 스테이징에서는 열려 있다**(운영은 부팅 점검이 막는다).
- `PENDING_CONFIRM` 을 세팅하는 코드가 없어 `charge-confirm.ts` 전체와 `resolveConfirmChannel` 이 사실상 죽은 코드다 → `ALLOW_LEGACY_CONFIRM_LINK` 되돌림 스위치가 실제로는 아무 것도 되돌리지 못한다.
- `resolveRefundFeeReturn`(`settlement.ts:325-359`)은 부분 환불을 전제로 정교하게 짜여 있는데 `requestRefund` 는 `amount: charge.amount` 로 전액만 만든다. 부분 환불 입력 경로가 없다.
- Redis 속도/연속 카운터(`limits.ts:264-284`)는 금액 선택 시 소진되는데 되돌리는 경로가 없다 — 결제가 실패·만료돼도 `streak` 이 쌓여 쿨다운이 걸린다.
- `/my/limits` 는 `resolvePolicy(null, payerId)` 로 한도를 보여주는데 결제 시엔 `resolvePolicy(merchantId, payerId)` 를 쓴다. MERCHANT 스코프 정책이 있으면 안내한 한도와 실제가 다르다.
- 배송비가 `charge.amount` 에 포함되므로 플랫폼 수수료·일월 한도가 배송비에도 걸린다. 의도된 정책인지 확인 필요.
- `notifySuperAdmins` 는 DB `notification` 행만 만든다(`notifications.ts:24-41`). 푸시·메일이 없어 "결제 결과 미확인"·"자동 정산 실패" 알림은 관리자가 콘솔을 열기 전까지 전달되지 않는다.
- `lib/logger.ts:13` 의 `PHONE_RE` 가 `+82` 표기를 매칭하지 못한다 — MO 웹훅 원문이 `webhookLog.bodyMasked` 에 평문으로 남을 수 있다.
- `redis.ts:72-81` 의 `degrade()` 는 한 번 폴백하면 **프로세스 재시작 전까지 해제되지 않는다.** Redis 가 복구돼도 영원히 인메모리이고, `/api/health` 는 폴백 성공을 `cache: 'ok'` 로 보고한다.
- `tests/helpers.ts:21-23` 이 `DISABLE TRIGGER` → TRUNCATE 를 try/finally 없이 실행한다. 중간 실패 시 원장 보호 트리거가 꺼진 채 남는다. (그리고 앱 롤이 트리거를 끌 수 있다는 사실 자체가 운영 권한 설계 과제다 — 1차 H-8)

---

## 테스트가 무엇을 보장하고 무엇을 못 하는가

270개가 **결제 수신 경로는 두텁게** 덮는다 — MO 수신부터 원장 반영까지, 멱등·중복 방어, 원천징수 금액, 파트너 API 서명, 로그 마스킹, PIN 콜백 중복·만료. `paymentTransaction.count()`·`settlementLedger.count()` 로 "출금이 일어나지 않았음"을 직접 확인하는 방식도 좋다.

못 하는 것이 구조적이다:

- **mock 이 실패하지 않아서** 지급대행 unknown 분기(N-16), 결제 UNKNOWN 분기(N-17), 환불 PG 취소 실패(N-18), MT 발송 실패 분기가 전부 도달 불가다. 하필 전부 **돈이 새는 경로**다.
- **서버 액션 26개 중 직접 테스트되는 것은 2개**(`web-charge`, `web-charge-pin`). `studio.ts`(1,276줄)와 `admin/*`(2,100여 줄)은 0건 — N-3·N-9·N-10 이 그 직접적 결과다.
- **화면 렌더 테스트 0건.** N-7·N-8·N-19·N-20 같은 화면-동작 불일치는 원리적으로 안 잡힌다.
- `vi.useFakeTimers` 사용 0건 — 시간 의존 테스트가 실제 `new Date()` 를 쓴다.

**가장 위험한 회귀 3가지** (테스트가 절대 못 잡고, 터지면 돈이 새는 것):
1. 지급대행 unknown 응답 처리 변경 → 같은 회차 이중 이체
2. 결제 UNKNOWN 처리의 강등(→ 정산 누락) 또는 승격(→ 받지 않은 돈 지급)
3. 지급일 계산 회귀 또는 `settlementDays` 과소값 → PG 정산 전 선지급

가장 비용 대비 효과가 큰 보강은 여전히 **서버 액션 권한 경계 테스트**(비인증·타 가맹점·READ_ONLY/SUPPORT 세션으로 호출해 거절되는지)이고, 그 다음이 **mock 어댑터에 실패 주입 스위치**를 넣는 것이다.

---

## 점검 방법과 한계

- 리눅스 컨테이너에 `npm install` 로 의존성을 새로 설치(win32 바인딩 문제 회피), `prisma generate`, PostgreSQL 16 기동, `prisma migrate deploy`, `vitest run` 을 실제 실행했다.
- 위 발견 중 ✔ 표시는 코드를 직접 열어 인용 줄을 확인한 것이다. "✔실행입증"·"✔실측"은 실행 결과나 DB 조회로 확인한 것이다.
- **프로덕션 빌드(`next build`)와 브라우저 E2E(`e2e/`)는 돌리지 않았다.** 화면 관련 발견(N-8·N-19·N-20)은 코드 판독이고, 실기기 확인이 남아 있다.
- 점검 환경에 `public/` 이미지와 `src/generated/prisma` 는 복사하지 않았다(전자는 테스트 1건 실패의 원인, 후자는 `prisma generate` 로 재생성).

---

## 잘 되어 있는 점

1차에서 적은 것에 더해, 이번 점검에서 새로 확인한 것:

- **마이그레이션 41개가 빈 DB 에서 실제로 전부 적용된다.** 개명 마이그레이션이 부분 유니크 인덱스 6개를 온전히 보존했고, Prisma 7 의 diff 도 깨끗하다. 스키마 관리가 정확하다.
- **결제 핫패스에 외부 호출이 없다.** PG 승인, Redis 속도제한, 지급대행 이체가 모두 트랜잭션 경계 밖이고 그 이유가 주석으로 남아 있다. 한도 판정도 charge 스캔이 아니라 `charge_counter` 집계 행 + 유니크 키로 O(1) 이다.
- **가맹점·이용자 격리는 빈틈이 없다.** 스튜디오 액션 25개와 `/studio/**` 전 페이지, CSV 내보내기까지 모든 DB 접근에 `merchantId` 가 걸려 있고, update 조건에 소유권을 함께 넣는 패턴이 일관된다. IDOR 누락 0건.
- **이중 결제 방어가 UI·서버·DB 3중이다.** 제출 중 버튼 비활성 + 클라이언트 멱등키 + `consumeSecureLink` + 조건부 `updateMany` + 부분 유니크 인덱스.
- 링크 오류 화면이 사유별(EXPIRED/USED/NOT_FOUND)로 갈리며 매번 "**결제는 진행되지 않았습니다**" 와 다음 행동을 함께 준다. 금융 서비스에서 가장 중요한 문구가 제자리에 있다.
