# 메시지페이 전체 검수 보고서 — 관리자 · 가맹점 콘솔 · 마이페이지

작성일 2026-09-02 · 대상 커밋 `5fa6f83` · 방식: 페이지별 코드 정독 + 연결된 서버액션/API 추적 + 정적 검증

---

## 1. 검수 개요

### 범위

| 영역 | 경로 | 페이지 수 |
|---|---|---|
| 운영자 관리자 | `/admin/**` | 29 |
| 가맹점 콘솔(사용자 관리자) | `/studio/**` | 15 |
| 결제자 마이페이지 | `/my/**` | 6 |
| 서버액션 | `src/app/actions/**` | 26개 파일 / 액션 65개 |
| API 라우트 | `src/app/api/**` | 23 |
| 교차 관심사 | `src/server/services/**`, `rate-limit`, `request-guard`, `mock-guard`, `uploads`, `crypto` | — |

### 정적 검증 결과

- `tsc --noEmit` — **통과 (오류 0)**
- `eslint src` — **오류 0 / 경고 1건** (`src/components/layout/public-shell-client.tsx:77` 미사용 인자 `aside`)
- 미해결 TODO/FIXME — `webhooks/pin-callback` 의 `TODO(계약 후)` 1건(의도된 표시)
- 이모지 — **전 범위 0건** (아이콘은 전부 lucide-react, 검출된 것은 화살표 문자 `→`)
- 깨진 내부 링크 — 정적 링크 96개 대조 결과 **404 링크 0건** (`/support` 등 전부 실재)
- 고아 페이지 — 0건

### 결함 집계

| 심각도 | 건수 | 성격 |
|---|---|---|
| 높음 | 15 | 기능이 실제로 동작하지 않거나, 데이터/장부가 어긋나거나, 개인정보가 새는 것 |
| 중간 | 24 | 숫자 신뢰 훼손, 권한 경계 누락, 복구 경로 없음 |
| 낮음 | 27 | UI 일관성, 문구 오류, 성능, 미사용 코드 |

### 전반 평가

인가(authorization) 골격은 견고합니다. 서버액션 65개 전부가 공용 래퍼(`run()` → `requireWriteAdmin`, `withMerchant()` → `requireMerchant`) 또는 1회용 링크 토큰·OTP 중 하나를 통과하며, **가드가 빠진 액션은 한 건도 없었습니다.** 가맹점 콘솔의 IDOR도 24개 액션 전수 확인 결과 없습니다(모두 `merchantId` 소유 재검증). 멱등 4중 방어, 정산 원장 append-only(앱 코드에 `settlementLedger.update/delete` 없음 + DB 트리거 설치 확인), 개인정보 3분리 저장·마스킹도 대부분 규칙대로입니다.

문제는 **다른 층에 몰려 있습니다.**

1. **폼과 액션 사이의 계약이 끊어진 곳** — 체크박스가 저장되지 않고, 폼에 없는 필드를 액션이 읽어 0으로 덮어씁니다. 코드는 컴파일되고 테스트도 통과하지만 사용자가 누른 것이 저장되지 않습니다.
2. **저장은 되는데 아무도 읽지 않는 설정** — 판매 설정의 직접 입력 범위·마케팅 수신 동의가 그렇습니다. 화면은 "지금 적용 중"이라고 단언합니다.
3. **요약 수치와 목록의 모집단 불일치** — `take: N` 으로 자른 배열의 `length` 를 건수로 쓰거나, 타일에만 `where` 를 빠뜨린 곳이 9군데입니다.
4. **실패 경로의 복구 수단 부재** — 환불 승인이 중간에 실패하면 관리자 화면에서 되살릴 방법이 없습니다.

---

## 2. 우선 조치 10선

| # | 결함 | 위치 | 왜 먼저인가 |
|---|---|---|---|
| 1 | 환불 승인 실패 시 결제 건 영구 고착 | `services/refund.ts:75-102` | 이용자 돈이 안 돌아간 채 DB 직접 수정 외 복구 불가 |
| 2 | 미래 시행일 약관 등록 → 현행 약관 즉시 소멸 | `actions/admin/policy.ts:178-184` | 전자금융거래 약관 미게시 상태로 결제 계속 진행 |
| 3 | 상품 "노출" 체크박스가 저장되지 않음 | `actions/studio.ts:569` | 숨기려 한 상품이 결제 화면에 계속 노출 |
| 4 | 상품 저장 시 노출 순서 0 초기화 | `actions/studio.ts:543,767` | 저장 한 번에 전체 진열 순서 파괴 |
| 5 | 수수료 상한 적용 시 부가세 미보정 | `services/settlement.ts:186-204` | append-only 원장에 틀린 부가세 확정 기록 |
| 6 | 결제내역 CSV "오늘"이 서버 로컬 자정 기준 | `api/studio/charges/export/route.ts:40` | 운영(UTC)에서 KST 00~09시 결제 누락 → 충전 미반영 |
| 7 | 탈퇴 프로필이 번호 재사용자에게 승계 | `actions/phone-link.ts:171-198` | 남의 결제 이력·메시지 원문 전체 열람 |
| 8 | 정산계좌·주민번호 변경에 감사로그 없음 | `actions/studio.ts:1366-1457` | 돈이 나가는 계좌 변경 추적 불가 |
| 9 | 무인증 SMS 발송 3종에 IP 속도제한 없음 | `charge-lookup.ts:84` 외 2 | 발신번호 도용 스팸·문자 비용 소진 |
| 10 | `/admin/users?role=constructor` → 500 | `admin/users/page.tsx:34` | URL 한 줄로 회원 관리 화면 다운 |

---

## 3. 높음 (15건)

### H-1. 환불 승인이 중간에 실패하면 결제 건이 복구 불가능한 상태로 고착된다

- **위치** `src/server/services/refund.ts:75-102`, `:22-24`, 화면 `src/app/admin/refunds/page.tsx:186-209`
- **현상** `approveRefund()` 는 PG 취소 호출 **전에** 환불을 `APPROVED` 로 선점한 뒤, 그다음에 승인 거래를 찾는다. 여기서 실패하거나 PG 취소가 실패하면 환불은 `APPROVED`/`FAILED`, 결제는 `REFUND_REQUESTED` 로 남는다.
- **근거**
  ```ts
  const claimed = await prisma.refund.updateMany({ where: { id: refundId, status: 'REQUESTED' }, data: { status: 'APPROVED' } }); // :75
  const txn = refund.charge.transactions.find((t) => t.status === 'APPROVED');
  if (!txn) throw new Error('승인된 결제 거래가 없습니다.');                                                                        // :84
  ```
  화면은 `r.status === 'REQUESTED'` 일 때만 승인/거절 버튼을 그린다. 재요청도 막힌다 — `requestRefund` 의 환불 가능 상태 목록(`refund.ts:22`)에 `REFUND_REQUESTED` 가 없고, 기존 refund 가 `APPROVED` 면 중복 차단에도 걸린다.
- **영향** 이용자 돈은 돌아가지 않고 결제 건은 정산 파이프라인에서도 빠진다. 관리자 화면에 되살릴 버튼이 없어 DB 직접 수정이 유일한 복구 수단. `refund.ts:95` 주석의 "관리자 화면에서 재시도(신규 환불 요청)로 이어갈 수 있도록"은 실제로 성립하지 않는다.
- **제안** ① 승인 거래 존재 확인을 선점 **이전으로** 옮긴다. ② PG 취소 실패 시 `charge.status` 를 `chargeStatusLog.fromStatus` 로 되돌린다(거절 경로와 동일). ③ `FAILED`/`APPROVED` 환불에 "재시도 / 요청 상태로 되돌리기" 액션을 화면에 추가한다.

### H-2. 미래 시행일로 약관을 등록하면 그 순간 현행 약관이 공개 화면에서 사라진다

- **위치** `src/app/actions/admin/policy.ts:178-184`, 화면 `src/app/admin/terms/page.tsx:83`
- **현상** 등록 폼은 "시행일 (KST)" 을 받는데, 액션은 시행일과 무관하게 같은 유형의 기존 버전을 **즉시** `active=false` 로 내린다.
  ```ts
  await tx.termsVersion.updateMany({ where: { type, active: true }, data: { active: false } });
  return tx.termsVersion.create({ data: { ..., effectiveFrom, active: true } });
  ```
  반면 공개 조회는 `active: true` **와** `effectiveFrom <= now` 를 동시에 요구한다 (`app/terms/page.tsx:16`, `terms/e-finance/page.tsx:17`, `r/[token]/page.tsx:182`).
- **영향** 10월 1일 시행 개정안을 9월에 미리 등록하면 9월 내내 `/terms`·`/terms/e-finance` 가 "현재 게시된 약관이 없습니다" 로 뜨고, **결제 링크에서 필수 동의 항목이 통째로 사라진다.** 전자금융거래 약관 미게시 상태로 결제가 계속 처리된다.
- **참고** `app/privacy/page.tsx:16` 은 `effectiveFrom` 필터가 아예 없어 세 공개 페이지의 기준이 서로 다르다.
- **제안** 기존 버전은 `active` 대신 `effectiveTo`(= 새 버전의 `effectiveFrom`)로 마감하고, 공개 조회는 `effectiveFrom <= now` 중 최신을 고르도록 통일한다.

### H-3. 상품 "결제 화면에 노출" 체크박스가 동작하지 않는다 — 항상 노출로 저장된다

- **위치** `src/app/actions/studio.ts:569`, 폼 `src/components/studio/product-form.tsx:122`
- **근거**
  ```ts
  active: !formData.has('active') || checked(formData, 'active'),
  ```
  폼에는 체크박스 하나뿐이고 짝이 되는 hidden 필드가 없다. HTML 체크박스는 해제 상태에서 FormData 에 담기지 않으므로 `formData.has('active') === false` → `!false = true` → **`active` 는 항상 true**.
- **영향** ① 등록 시 노출을 꺼도 즉시 결제 화면에 뜬다. ② 복제본(`studio.ts:901` `active:false`)·되살린 상품(`:849`)은 "값을 확인한 뒤 노출하라"고 안내하는데, 값을 고쳐 저장하는 순간 조용히 노출된다. ③ 목록 [숨기기] 로 숨긴 상품도 수정 폼에서 다른 값 하나만 고쳐 저장하면 다시 노출된다.
- **제안** `<input type="hidden" name="activePresent" value="1">` 를 두고 `active: formData.has('activePresent') ? checked(formData,'active') : true` 로 판정한다.

### H-4. 상품을 저장할 때마다 노출 순서(sortOrder)가 0으로 초기화된다

- **위치** `src/app/actions/studio.ts:543-544`, `:732`, `:767`
- **근거** 폼에 `sortOrder` 입력이 없는데(`grep` 확인) 액션이 그것을 읽는다.
  ```ts
  const sortOrderRaw = Number.parseInt(text(formData, 'sortOrder') || '0', 10);  // 항상 0
  ...
  const { id: _newId, merchantId: _m, kind: _k, ...rest } = parsed.data;
  await prisma.chargeProduct.update({ where: { id }, data: rest });              // sortOrder=0 이 함께 덮인다
  ```
  등록 경로의 `sortOrder: parsed.data.sortOrder ?? count`(`:732`)도 `parsed.data.sortOrder` 가 `number` 라 `?? count` 는 **도달하지 않는 죽은 코드**다. 신규 상품도 0으로 들어간다.
- **영향** `moveChargeProductAction` 이 `0..n` 으로 재정렬해 둔 값(`:951`)이 상품 저장 한 번에 파괴된다. 같은 값 0이 여러 개가 되면 결제 화면 순서가 `createdAt` 에 좌우되어 예측 불가능해진다 — 액션 주석(`:920`)이 막으려던 상황 그대로다.
- **제안** `parseProductForm` 이 `sortOrder` 를 반환하지 않게 하고(수정은 순서를 건드리지 않는다), 등록에서만 `sortOrder: count` 를 준다.

### H-5. 판매 설정의 "직접 입력 범위·단위"가 저장만 되고 결제에는 전혀 적용되지 않는다

- **위치** `src/app/studio/settings/page.tsx:230-235`, `src/server/services/charge-select.ts:121-124`
- **근거** 저장은 한다(`actions/studio.ts:355-357`). 화면은 "지금 적용되는 직접 입력 범위"라고 단언한다. 그러나 결제 확정 경로는 이 세 컬럼을 읽지 않는다.
  ```ts
  const minAmount = charge.merchant.minAmount > policy.minAmount ? charge.merchant.minAmount : policy.minAmount;
  const maxAmount = charge.merchant.maxAmount < policy.maxAmount ? charge.merchant.maxAmount : policy.maxAmount;
  ```
  `grep -rn "customMinAmount|customMaxAmount|customAmountStep" src` 결과 소비처는 설정 화면과 저장 액션뿐이다.
- **영향** 설정 화면이 거짓을 표시한다. 1,000원 단위·최대 5만원으로 좁혀 둔 가맹점도 이용자가 33,333원·한도 상한까지 결제할 수 있다. 저장 액션의 단위-범위 정합성 검증(`:339-349`)도 전부 무의미해진다.
- **제안** `loadSelectAmountContext` 에서 `customMinAmount ?? minAmount` 로 좁히고, `customAmountStep` 배수 검증을 `confirmChargeAmount` 의 직접 입력 분기(`charge-select.ts:296`)에 추가한다.

### H-6. 환불을 요청하는 순간 그 실물 주문이 주문·판매 목록에서 사라진다

- **위치** `src/app/studio/orders/page.tsx:213`, `:409-410`
- **근거** `requestRefund` 가 결제 상태를 `REFUND_REQUESTED` 로 바꾸는데(`refund.ts:31`), 목록·집계·CSV 가 모두 `PAID_STATUSES` 만 본다. `PAID_STATUSES`(`components/studio/shared.ts:9`)에는 `REFUND_REQUESTED`·`REFUNDED` 가 없다.
- **영향** 회수 송장 입력·반품 완료 처리 등 남은 배송 업무를 이 화면에서 할 수 없다. 같은 이유로 아래 두 뱃지는 **절대 렌더되지 않는 죽은 코드**다.
  ```tsx
  {s.charge.status === 'REFUNDED' ? <Badge tone="danger">환불됨</Badge> : null}
  {refund === 'REQUESTED' ? <Badge tone="warning">환불 요청됨</Badge> : null}
  ```
  상단 StatTile·발송 지연 카운트·주문서 CSV(`api/studio/orders/export/route.ts:53`)에서도 함께 빠진다.
- **제안** 배송 목록의 결제 상태 범위를 `[...PAID_STATUSES, 'REFUND_REQUESTED', 'REFUNDED']` 로 넓힌다(정산 집계용 `PAID_STATUSES` 와는 분리).

### H-7. 수수료 상한(cap) 적용 시 공급가액·부가세가 보정되지 않아 원장에 틀린 부가세가 확정 기록된다

- **위치** `src/server/services/settlement.ts:186-204`
- **근거** `pgFee`/`platformFee` 는 결제 금액을 넘지 않도록 잘라내지만, 함께 반환되는 `pgFeeSupply`/`pgFeeVat`/`platformFeeSupply`/`platformFeeVat`/`vat` 는 **보정 전 값 그대로**다.
  ```ts
  const pgFeeCapped = pgFee > amount ? amount : pgFee;
  const platformFee = platformFeeRaw > room ? room : platformFeeRaw;
  return { pgFee: pgFeeCapped, pgFeeSupply, pgFeeVat, platformFee, platformFeeSupply, platformFeeVat, vat: pgFeeVat + platformFeeVat, ... };
  ```
  재현: 1,000원 결제 / 부가세 별도 49.9%+49.9%(`createFeePolicy` 의 합계 검사를 통과) → `platformFee=452` 인데 `platformFeeSupply(499) + platformFeeVat(49) = 548`.
- **영향** 이 값들은 화면용이 아니라 **저장된다**(`charge-flow.ts:1163`, `payment-reconcile.ts:149` 가 `feeVat` 로 Charge 에 기록, `settlement.ts:289` 가 원장 메모에 "(부가세 N원 포함)"). "공급가액 + 부가세 = 차감액" 불변식이 깨진 채 **append-only 원장에 확정**되고, 원장은 정정이 불가능하다. CLAUDE.md 절대 규칙 8 위반이며 세금계산서 근거로 쓸 수 없다.
- **제안** 잘린 뒤 `supply = capped*10/11`, `vat = capped - supply` 로 재산출하고, 잘림 발생 시 경고 로그를 남긴다. 부수적으로 `pgFixedFee` 에 상한이 없다(`actions/admin/shared.ts:69`).

### H-8. 결제 내역 CSV의 "오늘" 필터가 KST가 아닌 서버 로컬 타임존으로 계산된다

- **위치** `src/app/api/studio/charges/export/route.ts:40`
- **근거**
  ```ts
  if (period === 'today') where.receivedAt = { gte: new Date(new Date().setHours(0, 0, 0, 0)) };
  ```
  화면 목록은 `kstStartOfDay()` 로 자른다(`studio/charges/page.tsx:54`). `src/lib/business-day.ts:14` 주석도 "AWS 는 UTC … 하루가 밀리면 그대로 지급 사고"라고 경고한다.
- **영향** 운영 서버(UTC)에서 "오늘" CSV는 **KST 00:00~08:59 결제가 통째로 빠진다.** 화면 헤더는 "결제 N건"인데 파일은 그보다 적고, 가맹점이 이 CSV로 충전을 일괄 반영하면 그 시간대 이용자가 충전을 못 받는다. 원인 추적도 매우 어렵다.
- **제안** `periodStart()` 를 export 해 화면과 라우트가 같은 함수를 쓰게 한다.

### H-9. 정산 계좌·주민등록번호 변경에 감사로그가 없고 재인증도 없다

- **위치** `src/app/actions/studio.ts:1409-1457`(계좌), `:1366-1407`(주민번호)
- **근거** `studio.ts` 전체에서 `writeAudit` 호출은 **단 1곳**(`:1923` 배송지 열람)뿐이다. 계좌 저장부는 `upsert` 후 바로 return 한다.
- **영향** 배송지 "열람"은 `SHIPMENT_ADDRESS_VIEW` 로 남기면서, 정작 **돈이 나가는 계좌 변경과 주민등록번호 등록은 흔적이 없다.** 세션 탈취로 계좌가 바뀌어도 언제·어느 IP에서 무엇이 무엇으로 바뀌었는지 사후 추적이 불가능하다.
- **완화되어 있는 점** 저장 시 `verified:false` 로 초기화되어(`:1439`) `runMerchantPayout` 이 미인증 계좌를 건너뛴다(`auto-settlement.ts:115`). 즉시 탈취는 막히지만 관리자가 승인하면 그대로 나간다.
- **제안** `writeAudit({ action:'SETTLEMENT_ACCOUNT_CHANGE', before/after: {bankCode, accountTail4, holderMasked} })` 추가(원문·암호문은 넣지 않는다) + 계좌 변경에 비밀번호 재입력.

### H-10. 정산 관리 페이지 1회 렌더에 DB 쿼리 수백 개 (N+1)

- **위치** `src/app/admin/settlements/page.tsx:98-102`, `src/server/services/auto-settlement.ts:431-447`, `:56-89`
- **근거** `buildPayoutDashboard()` 가 `take: 200` 가맹점을 순차 루프하며 각각 `findDueCharges`(= `resolveFeePolicy` + `charge.findMany` + `loadHolidaysAround`)를 부르고, 화면은 추가로 30곳의 `getSettlementSummary` 를 부른다. `findDueCharges` 의 `charge.findMany` 에는 `take` 가 없어 해당 가맹점의 미정산 결제를 **전부** 메모리로 올린다.
- **영향** 가맹점 200곳 기준 660여 쿼리. `force-dynamic` 이라 매 접속마다 발생하고, 같은 함수를 배치(`runScheduledPayouts`)도 쓰므로 배치 시간대에 관리자가 화면을 열면 부하가 겹친다.
- **제안** 지급 예정 집계는 SQL 집계 한 번으로 뽑거나 짧은 캐시를 두고, `findDueCharges` 에 `paidAt` 상한과 `take` 를 건다.

### H-11. `/admin/users` 의 검색 파라미터 검증이 프로토타입 키를 통과시켜 페이지가 500으로 죽는다

- **위치** `src/app/admin/users/page.tsx:34-35`
- **근거**
  ```ts
  const role = sp.role && sp.role in roleLabel ? (sp.role as UserRole) : undefined;
  ```
  `roleLabel` 은 평범한 객체 리터럴이라 `constructor`·`toString` 같은 키가 `in` 검사를 통과한다(`'constructor' in {PAYER:1}` → `true`). 그 값이 Prisma enum 조건으로 들어가 쿼리가 예외를 던진다.
- **영향** `/admin/users?role=constructor` 한 줄로 회원 관리 화면 전체가 에러 화면이 된다. 같은 저장소의 `payers`/`merchants`/`mo-numbers` 는 `.includes()`/`.some()` 을 써서 안전하므로 **이 화면만 규칙에서 벗어나 있다.**
- **제안** `(Object.keys(roleLabel) as UserRole[]).includes(...)` 로 통일한다.

### H-12. SUPPORT 등급이 전체 가맹점의 결제 금액 허용 범위를 일괄 변경할 수 있다

- **위치** `src/app/actions/admin/accounts.ts:304`(`applyGlobalAmountBounds`), `:350`
- **근거** 같은 파일의 인접 액션은 모두 SUPPORT 를 명시적으로 막는데(`:78`, `:218`, `:412`, `transactions.ts:81` 등) 금액 범위 액션 두 개에만 그 검사가 없다. `run()` 은 READ_ONLY 만 걸러낸다.
- **영향** 고객지원 등급이 버튼 한 번으로 전 가맹점의 1건 결제 상한을 바꾸고, 범위를 벗어난 충전 상품을 전부 비활성화시킬 수 있다. 결제 매출이 즉시 멈춘다.
- **함께 확인된 같은 계열** 가맹점 코드 재발급(`accounts.ts:471`), 결제 모드 변경(`:265`), 이용자 개인 한도 변경(`:182`, 상한도 없음), 약관 등록(`policy.ts:155`), 신고 상태(`:200`), 금칙어 추가·삭제(`:230`, `:256`) — 전부 SUPPORT 차단 없음. 금칙어는 `charge-flow.ts:165` 를 통해 결제 흐름에 직접 반영된다.
- **제안** 위험도가 같은 액션끼리 가드 기준을 통일한다(정산·MO 번호 액션의 패턴을 그대로 적용).

### H-13. 탈퇴·연결해제된 PayerProfile 이 번호 인증만으로 제3자에게 승계된다

- **위치** `src/app/actions/phone-link.ts:171-198`, `src/app/actions/payer.ts:330-351`
- **근거** 탈퇴 시 PayerProfile 은 `userId=null` 로만 끊기고 `phoneHash/phoneEnc/phoneMasked` 는 그대로 남는다. 재연결 쪽은 `phoneHash` 로만 찾고 `onboardingStatus` 를 보지 않는다.
  ```ts
  if (existing?.userId && existing.userId !== user.id) { ...거부... }
  // userId 가 null 이면 그대로 새 계정에 붙인다
  ```
- **영향** 휴대전화 번호는 해지 후 재판매된다. 같은 번호를 받은 **다른 사람**이 가입 후 번호 인증만 하면 이전 이용자의 전체 결제 내역·거래번호·결제 메시지 원문·차단 목록·동의 이력·한도 설정을 그대로 열람한다.
- **제안** ① `onboardingStatus === 'WITHDRAWN'` 프로필은 재연결 대상에서 제외하고 새 프로필을 만든다. ② 보존기간 후 `phoneEnc/phoneMasked/displayName` 을 파기하고 `phoneHash` 를 무효화한다.

### H-14. 이용자 개인 한도가 전역 정책 하향을 무력화한다

- **위치** `src/server/services/limits.ts:182-183`, 화면 `src/app/my/limits/page.tsx:52-53`
- **근거** 저장된 개인 한도가 있으면 전역 정책과 비교 없이 그대로 유효 한도가 된다.
  ```ts
  const payerDaily = input.payer.dailyLimit ?? policy.payerDailyLimit;
  ```
  "정책보다 높게 설정 불가" 검사는 **저장 시점에만** 있다(`payer.ts:100-105`).
- **영향** 이용자가 한도 10만원을 저장한 뒤 관리자가 전역 `payerDailyLimit` 을 5만원으로 낮춰도 그 이용자는 계속 10만원까지 결제된다. 리스크 대응으로 전역 한도를 내려도 기존 설정자에게 적용되지 않는다. 화면도 하향된 정책이 아니라 10만원을 "한도"로 표시해 오해를 굳힌다.
- **제안** 읽기 시점에도 정책값으로 클램프한다(엔진·화면 양쪽).

### H-15. 무인증 SMS 발송 액션 3종에 IP 단위 속도 제한이 없다

- **위치** `src/app/actions/charge-lookup.ts:84`, `web-charge.ts:101`, `web-charge-pin.ts:94`
- **근거** 로그인 없이 호출 가능하고 문자를 발송하는 세 액션이 **전화번호 해시 버킷만** 센다. `src/server/rate-limit.ts:8` 주석은 IP 제한 대상을 "인증 없이 계정/자원을 만들 수 있는 입구"로 정의하고, 회원가입·가맹점신청·비밀번호재설정은 실제로 `consumeIpRateLimit` 를 쓴다. **로그인이 없는 이 세 곳만** 번호 단일 버킷이다.
- **영향** 한 IP에서 번호를 바꿔가며 무제한 발송 가능(번호당 3회 × 번호 무한). 문자 비용 소진, 우리 발신번호를 쓴 대량 스팸. 특히 `startWebPinCharge` 는 문자만이 아니라 **`Charge` + `PaymentPinSession` 행 + 한도 검사**까지 매번 생성해 DB·한도 카운터를 오염시킨다.
- **제안** 번호 버킷과 함께 `consumeIpRateLimit(...)` 추가. `startWebPinCharge` 는 가맹점 단위 버킷도 함께 둔다.

---

## 4. 중간 (24건)

### 권한·감사·보안

| # | 결함 | 위치 |
|---|---|---|
| M-1 | 관리자 페이지 29개 중 **27개가 layout 가드에만 의존**한다. `/studio` 는 16개 중 15개, `/my` 는 6개 전부 페이지에서 가드를 다시 부르는데 admin 만 규약이 다르다. App Router 는 layout 과 page 를 병렬 렌더하므로 비관리자 요청에서도 페이지의 DB 조회(전화번호·결제 원장·감사로그)가 실행된다 | `src/app/admin/*/page.tsx` 전수 |
| M-2 | `adminPermission` 이 메뉴 숨김에만 쓰여 **READ_ONLY·SUPPORT 관리자도 모든 열람 화면에 접근**한다(변경은 액션이 막음). 열람 권한 분리는 `inquiries` 뿐 | `admin/layout.tsx:92` |
| M-3 | 파트너 API `GET /charges` 만 **성공 호출 기록이 없다**. 복호화된 전화번호·배송지 원문을 최대 500건 반출하는 유일한 대량 경로인데, 키 유출 시 반출량 추적 불가. 같은 디렉터리 나머지 3개 라우트는 모두 기록한다 | `api/partner/v1/charges/route.ts:126` |
| M-4 | 결제 확정 경로만 `X-Forwarded-For` **첫 홉**(위조 가능)을 쓴다. 이 값이 `SecureLink.usedIp` 에 저장되어 "누가 이 결제를 확정했는가"의 유일한 증거가 된다. 나머지 전부는 `clientIpFrom()` 사용 | `actions/charge-select.ts:78` |
| M-5 | 지급대행 이체파일 **GET 이 DB를 변경**(`payoutIssuedAt`/`payoutBatchNo`)하는데 `isSameOrigin` 검사가 없다. `<img src=...>` 만으로 발급 이력이 오염된다. `ids` 개수 상한도 없음 | `api/admin/settlements/payout/route.ts:30-55` |
| M-6 | `ALLOW_LEGACY_CONFIRM_LINK` 가 운영 기동 안전점검에서 빠져 있다. 같은 위험 등급인 `ALLOW_LEGACY_WEB_INSTANT_PAY` 만 점검한다. 이 경로는 PIN 인증 없이 빌키로 곧바로 출금한다 | `lib/env.ts:256-259` |
| M-7 | 이미지 업로드에 **속도 제한이 없고**(5MB × 무제한) 저장된 객체를 지우는 경로도 없다 | `api/upload/route.ts:41-73` |
| M-8 | 배송지 열람·CSV 감사로그에 **"누가"가 남지 않는다**. `writeAudit` 는 `adminProfile` 만 조회하므로 가맹점 사용자는 `adminId=null`, `after` 에도 userId 가 없다. 한 가맹점에 담당자가 여럿이면 추적 불가 | `actions/studio.ts:1923`, `api/studio/orders/export/route.ts:86` |
| M-9 | 환불 승인만 **사유 입력이 없다**. 거절·직접환불·수동확정은 모두 근거 2자 이상을 강제하는데, 되돌릴 수 없는 출금 액션인 승인만 `refundId` 만 받는다 | `actions/admin/transactions.ts:199-222` |
| M-10 | 관리자 직접 환불 확인창이 **대상 거래를 전혀 보여주지 않는다**. 거래번호 한 글자를 잘못 입력하면 무관한 결제가 요청 생성+승인까지 한 번에 진행된다 | `admin/refunds/page.tsx:87-99` |

### 데이터 정확성

| # | 결함 | 위치 |
|---|---|---|
| M-11 | 수수료 정책 마감 쿼리에 `effectiveTo: null` 조건이 없어 **이미 마감된 정책의 종료일이 미래로 되밀린다**. 정책 A/B 가 동시에 "적용 중"이 되어 정책 이력이 소급 변조된다(금액 산출 자체는 무영향) | `actions/admin/settlement.ts:381-384`, `:423` |
| M-12 | 한도 정책 화면이 **`active` 만으로 "현재 적용 정책"을 판단**한다. 실제 판정(`limits.ts:89`)은 `effectiveFrom/To` 를 함께 본다. 수수료 화면은 올바른데 이 화면만 다르다. `effectiveTo` 는 표시조차 안 된다 | `admin/policies/page.tsx:119,203` |
| M-13 | 전역 한도 정책 중복 검사도 `active` 기준이라 **예약 정책을 만들면 반드시 공백 구간이 생긴다**. 그 기간엔 코드 기본값(`FALLBACK_POLICY`)이 조용히 적용된다. 수수료 쪽은 이미 고쳐 두었다 | `actions/admin/policy.ts:93-98` |
| M-14 | "오늘 지급 예정" 금액이 **실제 지급될 금액과 다르다**. 대시보드는 `netAmount` 단순 합산, 실제 배치는 원장 가용 잔액 안에서만 담고 최소 지급액 미만이면 이월한다 | `auto-settlement.ts:434` vs `:126-144` |
| M-15 | 가맹점 정산 화면의 원천징수·실지급 예상이 **`summary.available`(누적 잔액)** 기준이라, 실제 회차 금액 기준으로 계산하는 배치와 결과가 다르다. 바로 아래 "지급 예정 내역" 표와도 숫자가 충돌한다 | `studio/settlement/page.tsx:176,449` |
| M-16 | 정산 캘린더가 **이미 지급이 끝난 날에도 "정산예정"을 함께 표시**한다. `PAID_STATUSES` 에 `SETTLED` 가 포함되는데 `settledAt` 을 걸러내지 않는다(바로 옆 `monthPayouts` 는 주석까지 달아 막고 있다) | `studio/settlement/page.tsx:82-89` |
| M-17 | "지급 대기" 안내가 **데드엔드**다. 결제 내역은 상품 종류를 안 보고 세는데, 주문·판매 비실물 탭은 `product.kind = DIGITAL` 로 걸러 직접 입력 결제를 통째로 뺀다. 파트너 API 는 또 다른 기준이라 셋이 서로 다르다 | `studio/orders/page.tsx:105` vs `charges/page.tsx:86` |
| M-18 | "판매 N건" 이 **결제 실패·환불 건까지 센다**. `productId` 는 금액 확정 시점에 붙으므로 PIN 실패·`LIMIT_BLOCKED`·`REFUNDED` 가 모두 포함된다 | `studio/products/page.tsx:67`, `[id]/page.tsx:38` |
| M-19 | `PAYMENT_UNKNOWN` 이 이상거래 유형 필터에서 **빠져 있다**. `level: CRITICAL` 로 생성되는 유일한 유형인데 드롭다운에도 없고 URL 파라미터도 무시된다 | `admin/risk/page.tsx:18-21` |
| M-20 | 결제를 수동 확정해도 그 건의 `PAYMENT_UNKNOWN` **탐지가 미해결로 남는다**. 대사가 끝난 건이 계속 "미해결 HIGH 이상" 타일에 잡혀 실제 잔여 위험을 가린다 | `actions/admin/transactions.ts:181-193` |

### 수치 신뢰 · 표시

| # | 결함 | 위치 |
|---|---|---|
| M-21 | 요약 타일이 **`take: N` 으로 자른 목록의 `length`** 를 건수로 쓴다. 지연 주문 300건도 "20건", 결과 확인 필요 57건인데 경고 제목은 "20건" | `admin/products/page.tsx:111,118`, `admin/payments/page.tsx:238`, `studio/reports/page.tsx:37`, `studio/products/page.tsx:139` |
| M-22 | 집계 타일에만 **`where` 가 빠져** 목록 필터와 모집단이 다르다. 정산 화면은 금액까지 틀린다 | `admin/settlements/page.tsx:88`, `admin/inquiries/page.tsx:101`, `admin/products/page.tsx:69`, `admin/payments/page.tsx:210`, `admin/refunds/page.tsx:53` |
| M-23 | 가맹점별 정산 요약이 **30곳으로 잘려** 이름순 31번째 이후 가맹점은 잔액이 얼마든 표에 나타나지 않는다. 안내 문구도 없다(필터 드롭다운에는 60곳) | `admin/settlements/page.tsx:52,101` |
| M-24 | 대시보드 두 섹션을 90줄씩 **통째로 복붙**해 두었고, 복사본의 링크가 갈라졌다(`/admin/risk` vs `/admin/mo-messages?result=BLOCKED`). 두 경로의 집합도 서로 다르다 | `admin/page.tsx:102-109`, `:218-225` |

### 개인정보·복구 경로

| # | 결함 | 위치 |
|---|---|---|
| M-25 | 이용자의 **배송 요청사항을 "가맹점 메모"로 오표기**하고 덮어쓸 수 있다. 폼 hint 는 "이용자에게는 보이지 않습니다" 인데 같은 컬럼을 이용자가 결제 화면에서 쓴다. 저장하면 "부재 시 경비실에" 같은 요청이 지워지고 CSV 에도 덮어쓴 값이 나간다 | `studio/orders/page.tsx:519`, `actions/studio.ts:1751` |
| M-26 | 환불이 **거절되면 [환불 요청] 버튼이 영구히 사라진다**. 서버는 `REJECTED` 재요청을 허용하는데 화면이 "최근 환불 이력이 있으면" 무조건 감춘다 | `studio/orders/page.tsx:535` |
| M-27 | 실물 상품 주문이 `/my` 에서 **영구히 "포인트 지급 대기"** 로 표시된다. `/my` 만 `pointStatusLabel` 을 안 쓰고 `SKIPPED` 를 빠뜨렸다. 존재하지 않는 미지급 건 문의가 가맹점으로 유입된다 | `my/page.tsx:113-119` |
| M-28 | **마케팅 수신 동의 철회가 어떤 발송 경로에도 반영되지 않는다.** `MARKETING` 동의를 읽는 코드가 전 저장소에 없다(현재 마케팅 발송 기능 자체가 없어 실피해는 없으나, 추가 시 게이트를 놓치면 법 위반) | `actions/payer.ts:233-272` |
| M-29 | 탈퇴 시 **PayerProfile 의 개인정보가 파기되지 않는다**. `phoneEnc`(복호화 가능), `phoneMasked`, `displayName`, `Charge.displayName` 이 무기한 남는다. 화면 문구는 "계정 정보가 삭제되고"라고 안내한다 | `actions/payer.ts:330-351` |
| M-30 | 탈퇴 완료 후 **아무 안내도 표시되지 않는다**. `redirect('/?withdrawn=1')` 인데 홈이 `searchParams` 를 받지 않는다(죽은 파라미터) | `actions/payer.ts:357`, `app/page.tsx:57` |
| M-31 | 관리자 화면의 "탈퇴" 처리가 **본인 탈퇴와 결과가 다르다**. 결제수단을 해지하지 않고 PII 도 남긴다. 라벨은 똑같이 "탈퇴" | `actions/admin/accounts.ts:39-46` |
| M-32 | 문자 관리 화면에 **페이지네이션이 없어 51건째부터 접근 불가**. 기간 필터도 없다. 조치(이용자 차단)해야 할 건이 50건 밖으로 밀리면 처리 자체가 불가능 | `studio/messages/page.tsx:17,62` |
| M-33 | CSV 가 **조용히 잘린다**(결제 5,000건 / 주문서 3,000건). 응답·화면·감사로그 어디에도 절단 표시가 없어 가맹점이 전량으로 믿고 반영한다 | `api/studio/charges/export/route.ts:19`, `orders/export/route.ts:22` |
| M-34 | 온보딩 체크리스트의 "사업자 정보 등록"이 **입력 화면이 없는 죽은 링크**다. `/studio/profile` 에 `businessNo` 입력란도 액션도 없다. 카드 소멸 조건이 전 항목 완료라 **개인 가맹점은 이 카드가 영구 고정**된다 | `components/studio/onboarding-checklist.tsx:69-80` |
| M-35 | 결제·환불 화면에 **mock/SAFE_MODE 표시가 없다**. `SafetyBanner` 는 `/admin` 과 `/admin/system` 에만 있고, 표의 `provider` 는 실행 시점 어댑터가 아니라 설정값이라 `SAFE_MODE=true`+`hecto` 면 실제 mock 처리인데 `hecto` 로 보인다. CLAUDE.md 절대 규칙 2 위반 | `admin/payments/page.tsx`, `admin/refunds/page.tsx` |
| M-36 | 배너 **종료일이 하루 일찍 끊긴다**(`endsAt: { gte: now }` + KST 00:00 변환). 또 `HOME_TOP`/`HOME_MIDDLE` 로 등록한 배너는 렌더되는 곳이 없다 | `admin/banners/page.tsx:14`, `components/public/banner-strip.tsx:19` |
| M-37 | 금칙어 안내와 **실제 동작이 어긋난다**. 화면은 "결제를 막지 않습니다"·select 는 MASK/FLAG 만인데 액션은 `BLOCK` 을 허용하고 `content-filter.ts:224` 는 실제로 차단한다. 비활성 금칙어를 되살릴 UI 도 없다 | `admin/moderation/page.tsx:89`, `actions/admin/policy.ts:233` |
| M-38 | 금액 범위 변경 확인창이 **"자동 보정" 이라고 잘못 안내**한다. 실제로는 범위를 벗어난 충전 상품을 `active:false` 로 끈다(같은 화면 설명 문단은 올바르게 적혀 있어 서로 모순) | `admin/merchants/page.tsx:199` |
| M-39 | 환불 거절 시 트랜잭션 밖에서 두 테이블을 갱신한다. 승인 경로는 `$transaction` 으로 묶고 주석까지 경고하는데 거절만 빠져 있다 | `services/refund.ts:188-204` |
| M-40 | 이미 `DONE` 인 환불을 다시 승인해도 **"성공"으로 응답하고 감사로그가 한 번 더 쌓인다**(`REQUESTED` 가 아닌 다른 상태는 정상적으로 거부됨) | `services/refund.ts:66`, `actions/admin/transactions.ts:204` |

> 중간 항목이 24건을 넘는 것은 표 안에서 유사 결함을 묶어 번호를 이어 붙였기 때문입니다. 실제 개별 건수는 40개 항목 중 높음으로 승격되지 않은 것들입니다.

---

## 5. 낮음 (요약)

| 영역 | 결함 | 위치 |
|---|---|---|
| 공통 | 마지막 페이지를 넘어가면 목록도 Pager 도 사라져 되돌아올 수단이 없다(`?page=999`) | users/payers/codes/merchants/audit/settlements/moderation/inquiries/my |
| 공통 | READ_ONLY·SUPPORT 에게도 모든 변경 버튼이 활성 상태로 보인다(서버는 정상 차단). 권한을 UI에 반영하는 곳은 `/admin/admins` 하나뿐 | 관리자 화면 전반 |
| 공통 | 요청마다 세션을 2회 조회(layout + page). `React.cache()` 미적용 | `my/layout.tsx`, `components/my/payer.ts` |
| admin | 감사로그 "액션 종류/대상 유형" 수치가 `distinct take` 상한에 잘려 100에서 멈추고 드롭다운도 잘린다 | `admin/audit/page.tsx:50` |
| admin | 이용자 상세에 **제목이 똑같은 표가 두 개**("결제 내역" — Charge / PaymentTransaction) | `admin/payers/[id]/page.tsx:256,290` |
| admin | 대시보드가 SUPER_ADMIN 만 열 수 있는 문의 타일을 모든 등급에 노출(메뉴에서 숨긴 건수가 대시보드로 샌다) | `admin/page.tsx:134,250` |
| admin | 가맹점 심사 상태 변경 시 알림 저장이 실패하면 감사로그가 유실된다(순서 문제, 트랜잭션 아님) | `actions/admin/accounts.ts:239-256` |
| admin | 요율 표시가 소수점 2자리 고정이라 1.8375% 가 "1.84%" 로 보인다(입력은 4자리 허용) | `lib/money.ts:47` |
| admin | 소액 결제에서 공급가액 0원 + 부가세 전액으로 기록된다(10원 결제 · 15% 포함요율) | `services/settlement.ts:134` |
| admin | 목록 `orderBy` 에 tie-breaker(`id`)가 없어 페이지 넘길 때 중복·누락 가능. 특히 `sortOrder` 는 기본값 0이라 동률이 많다 | `admin/products/page.tsx:58` 외 5 |
| admin | 가맹점 전체를 매 요청 로드(`mo-messages`) / 61번째부터 선택 불가(`products`) — 같은 목적인데 반대 방향 문제 | `admin/mo-messages/page.tsx:65`, `products/page.tsx:49` |
| admin | 상품 판매 집계를 전수 조회 후 메모리 선형 탐색 | `admin/products/page.tsx:63,97` |
| admin | 이상거래 목록의 연결 거래가 **내부 ID 원문**이고 그 값으론 결제 화면 검색이 안 된다(링크도 없음) | `admin/risk/page.tsx:167` |
| admin | `updateSettlementRequestStatus` 는 **호출부가 없는 죽은 액션**. 이 함수에만 있는 안전장치가 실제로는 쓰이지 않는다 | `actions/admin/settlement.ts:35-109` |
| admin | 환불 거절 안내가 "정산 대기로 되돌아갑니다"인데 실제로는 직전 상태로 복원한다 | `admin/refunds/page.tsx:119` |
| admin | 결제 관리 설명 문구가 "PG 결제 거래와 **결제 거래를**" (일괄 치환 잔재) | `admin/payments/page.tsx:221` |
| admin | MO 번호 키워드 플레이스홀더가 `MessagePay` 인데 저장은 항상 대문자 | `admin/mo-numbers/page.tsx:90` |
| admin | 이용자 상세 "가맹점별 결제 · 상위 10**명**" (용어표 위반) | `admin/payers/[id]/page.tsx:215` |
| admin | 시스템 상태가 4열 그리드에 타일 3개만 채움 | `admin/system/page.tsx:84` |
| admin | 가맹점 목록이 화면에 안 쓰는 `businessNo` 를 매번 조회 | `admin/merchants/page.tsx:31` |
| admin | 날짜 역전(`from > to`)을 검증하지 않아 항상 0건인 조합을 그대로 조회 | `admin/mo-messages/page.tsx:38` |
| admin | 공지·FAQ 가 `take:100` 에서 무언 절단(페이지네이션 없음) | `admin/contents/page.tsx` |
| studio | 보관함이 50건에서 잘리고 탭 배지도 잘린 수를 표시 | `studio/products/page.tsx:60,139` |
| studio | 신고 분류에 영문 코드 `ABUSE` 그대로 노출, 통계 타일은 최근 100건만 집계 | `studio/reports/page.tsx:84,37` |
| studio | `period`·`kind` 파라미터 미검증(오타 시 조용히 전체 기간 / 실물 폼) | `studio/orders/page.tsx:88`, `products/new/page.tsx:25` |
| studio | 비실물 상품이 한도를 넘어도 목록에서 경고하지 않는다(결제 화면에선 사라짐) | `studio/products/page.tsx:199` |
| studio | 송장 일괄 등록이 **반품·교환 진행 중 주문을 발송 완료로 덮어쓴다** | `actions/studio.ts:1855` |
| studio | 대시보드는 `receivedAt`, 정산 캘린더는 `paidAt` 기준이라 "오늘 결제 금액"이 화면마다 다르다 | `studio/page.tsx:45` vs `settlement/page.tsx:82` |
| studio | layout 과 대시보드가 동일 count 쿼리를 중복 실행(요청당 4회) | `studio/layout.tsx:106`, `page.tsx:101` |
| studio | CSV 라우트 상태 화이트리스트가 화면 드롭다운과 다르다 | `api/studio/charges/export/route.ts:44` |
| studio | 최소 지급 금액을 문구로만 언급하고 실제 값(`env.payout.minAmount`)을 안 보여준다 | `studio/settlement/page.tsx:525` |
| studio | "금칙어 포함" 탭이 별표(`*`)를 포함한 정상 문자를 오분류(같은 행에 차단 버튼 있음) | `studio/messages/page.tsx:41` |
| studio | 용어·조사 오류: "결제**과**", "결제가 완료된 결제가", "결제 설정"(→ 판매 설정), 헤더 "설정" vs 메뉴 "프로필 설정" | `studio/settlement/page.tsx:244,378,737`, `profile/page.tsx:29,48` |
| studio | 리다이렉트 전용 shim(`/studio/settlement/account`)을 아직 링크 | `components/studio/onboarding-checklist.tsx:66` |
| my | 차단 해제에 확인 절차가 없고(탈퇴·해지는 있음) 성공 메시지도 렌더되지 않는다 | `components/my/block-toggle.tsx:15` |
| my | 동의 이력 `take:100` 안에서 마케팅 최신 상태를 판정 → 다른 동의가 밀어내면 "미동의"로 뒤집힌다 | `my/consents/page.tsx:37` |
| my | 한도 입력의 `max` 가 `type="text"` 라 무효(주석은 "여기서도 막는다"고 설명) | `components/my/limits-form.tsx:37` |
| my | 번호 미연결 빈 화면이 페이지마다 다르고, 안내 문구가 없는 탭 이름("등록 계좌 탭")을 가리킨다 | `my/blocks/page.tsx:14`, `limits/page.tsx:17` |
| my | 실물 상품 주문의 **배송 상태를 이용자가 볼 수 없다**(`shipment` 미조회) | `my/page.tsx:59` |
| my | 가맹점에 "님" 호칭(용어표 위반) | `actions/payer.ts:186` |
| my | 환불 가능 상태 목록에 "(미사용)" 상태 3종이 섞여 있고 같은 목록이 두 파일에 복제 | `my/page.tsx:16`, `services/refund.ts:22` |
| API | 스튜디오 CSV 라우트가 미인증 요청을 500으로 떨어뜨린다(관리자 라우트는 401로 정리) | `api/studio/*/export/route.ts` |
| API | 웹훅 본문 크기 검사가 chunked 요청을 막지 못한다(전량 버퍼링 후 검사) | `webhooks/mo/route.ts:27`, `pin-callback/route.ts:87` |
| API | 부수효과 있는 GET/PATCH 에 `isSameOrigin` 없음(문의 승계·읽음 처리, 알림 읽음) | `api/inquiry/route.ts:17`, `api/notifications/route.ts:31` |
| API | `merchant-lookup` 의 `kv.incr` 가 try/catch 밖 → Redis 순단 시 액션 전체 실패 | `actions/merchant-lookup.ts:66` |
| API | 웹훅 재생공격 방어가 서명이 아니라 중복 키에만 의존(MO 는 본문 HMAC만, PIN 은 정적 공유비밀). 이중결제로는 이어지지 않으나 `retryable` 분기는 재처리 허용 | `adapters/mo/index.ts:14`, `webhooks/pin-callback/route.ts:55` |
| API | 소셜 로그인 콜백에 `state` 쿠키 검증이 없다(현재 스텁이므로 무해, 실연동 시 필수) | `api/auth/social/[provider]/callback` |
| lint | 미사용 인자 `aside` | `components/layout/public-shell-client.tsx:77` |

---

## 6. 페이지별 상태표

### 운영자 관리자 `/admin` (29)

| 경로 | 판정 | 요약 |
|---|---|---|
| `/admin` | 결함 | 두 섹션 복붙으로 링크 갈라짐, 한도 차단 수치와 링크 대상 집합 불일치, 문의 타일 권한 노출 |
| `/admin/system` | 주의 | 기능·마스킹 정상. 4열 그리드에 타일 3개 |
| `/admin/users` | 결함 | `?role=constructor` 500, 관리자 "탈퇴"가 결제수단·PII 잔존 |
| `/admin/payers` | 주의 | 개인 한도 상한 없음, SUPPORT 차단 없음 |
| `/admin/payers/[id]` | 주의 | 제목 같은 표 2개, 용어 "상위 10명" |
| `/admin/merchants` | 결함 | 전체 일괄 적용 SUPPORT 가드 없음, 확인 문구 오기, `businessNo` 미사용 조회 |
| `/admin/merchants/[id]` | 주의 | 금액 범위 확인 문구 오기. 수수료 기본값 표기는 정상 |
| `/admin/codes` | 주의 | 재발급 액션 SUPPORT 가드 없음 |
| `/admin/mo-numbers` | 주의 | 권한·감사로그·중복검증 견고. 키워드 플레이스홀더만 어긋남 |
| `/admin/mo-messages` | 주의 | 마스킹 원칙 준수. 가맹점 전체 로드, 날짜 역전 미검증 |
| `/admin/mt-messages` | 정상 | 본문 이중 마스킹, mock 발송함 명시. 결함 없음 |
| `/admin/mt-templates` | 정상 | 잠금 템플릿 방어 이중. UI 권한 미반영만 |
| `/admin/payments` | 결함 | 경고 건수 절단, mock/SAFE_MODE 표시 없음, 설명 비문 |
| `/admin/products` | 결함 | 통계 타일이 잘린 목록 길이, 타일마다 필터 다름, 정렬 tie-breaker 없음 |
| `/admin/refunds` | 결함 | **승인 실패 시 복구 불가(H-1)**, 승인 사유 미수집, DONE 재승인 오응답, 거절 비원자성, 안내 오기 |
| `/admin/risk` | 결함 | `PAYMENT_UNKNOWN` 필터 누락, 대사 후 탐지 미해결, 연결 거래가 조회 불가 ID |
| `/admin/settlements` | 결함 | **N+1 수백 쿼리(H-10)**, 타일 필터 무시, 요약 30곳 절단, 지급예정액 불일치, 권한 없는 다운로드 버튼 |
| `/admin/fees` | 결함 | **수수료 상한 시 부가세 미보정(H-7)**, 정책 마감 기간 겹침, 요율 2자리 반올림 |
| `/admin/policies` | 결함 | `active` 만으로 현재 정책 판단, 예약 시 공백 구간, `effectiveTo` 미표시 |
| `/admin/holidays` | 정상 | KST 연도 판정·SUPPORT 차단·삭제 확인 모두 적절 |
| `/admin/banners` | 주의 | 종료일 하루 손실, 렌더 안 되는 위치 선택 가능 |
| `/admin/contents` | 주의 | `take:100` 무언 절단 |
| `/admin/moderation` | 결함 | SUPPORT 차단 없음, 안내문과 BLOCK 동작 모순, 비활성 복구 UI 없음 |
| `/admin/inquiries`, `/[id]` | 주의 | SUPER_ADMIN 검사 페이지·액션 양쪽 존재(양호). 타일 필터 무시 |
| `/admin/terms` | 결함 | **미래 시행일 등록 시 공개 약관 소멸(H-2)**, SUPPORT 차단 없음 |
| `/admin/admins` | 정상 | 본인 권한 변경 차단, 마지막 SUPER_ADMIN 방지, UI `disabled` 까지 유일하게 완비 |
| `/admin/audit` | 주의 | `distinct take` 상한으로 종류 수·드롭다운 절단 |
| `/admin/simulator` | 정상 | `isLocal` + `MO_PROVIDER=mock` 이중 차단, mock 명시, 토큰 마스킹 |

### 가맹점 콘솔 `/studio` (15)

| 경로 | 판정 | 요약 |
|---|---|---|
| `/studio` | 주의 | 격리·마스킹 정상. 집계 기준일이 정산 화면과 다름, count 중복 |
| `/studio/layout` | 정상 | 미승인/정지 분기와 뱃지 count 모두 `merchantId` 한정 |
| `/studio/charges` | 주의 | 화면 자체 정상. 연결 CSV 라우트 결함이 화면 숫자와 어긋남 |
| `/studio/charges/[id]` | 정상 | 소유권 재검증, 원문 미노출, 환불은 요청만, 확인창까지 규칙 준수 |
| `/studio/messages` | 결함 | 페이지네이션·기간 필터 없음(50건 밖 접근 불가), 별표 오분류 |
| `/studio/products` | 결함 | 노출 토글·순서 저장 파손(H-3,H-4), 판매 건수에 실패 건 포함, 보관함 50건 절단 |
| `/studio/products/new` | 주의 | 폼 정상. `kind` 미검증 |
| `/studio/products/[id]` | 결함 | 저장 시 `active` 항상 true, `sortOrder` 0으로 덮임 |
| `/studio/orders` | 결함 | **환불 요청 시 목록에서 소멸(H-6)**, 직접 입력 지급 대기 누락, 배송 메모 오표기·덮어쓰기, 반려 후 재요청 불가 |
| `/studio/settings` | 결함 | **직접 입력 범위·단위 미적용(H-5)**. 배송·연동·수신 탭은 정상 |
| `/studio/moderation` | 정상 | 소유권 검증·전역/내 금칙어 분리·미리보기 모두 정상 |
| `/studio/reports` | 주의 | 분류 `ABUSE` 원문 노출, 타일이 최근 100건만 집계 |
| `/studio/settlement` | 결함 | 원천징수·실지급 예상 기준 금액 불일치, 캘린더 지급 완료 건 중복 표시 |
| `/studio/settlement/account` | 정상 | 리다이렉트 스텁 |
| `/studio/profile` | 주의 | 용어·조사 오류, 헤더 제목과 메뉴 라벨 불일치 |
| `/studio/docs/partner-api` | 정상 | 가드 정상, 시크릿 노출 없음 |

### 결제자 마이페이지 `/my` (6)

| 경로 | 판정 | 요약 |
|---|---|---|
| `/my` (layout) | 정상 | 세션 가드. 중복 조회만 개선 여지 |
| `/my` | 주의 | 포인트 `SKIPPED` 미처리, 미사용 상태 혼재, page 상한 미보정, 배송 정보 부재 |
| `/my/account` | 주의 | 탈퇴 후 데이터 미파기, 탈퇴 완료 안내 없음 |
| `/my/blocks` | 주의 | 해제 확인 절차 없음, 성공 메시지 미표시, 빈 상태 CTA 부재 |
| `/my/consents` | 주의 | 마케팅 철회 미반영, `take:100` 최신 판정 오류 |
| `/my/limits` | 결함 | **개인 한도가 전역 정책 무력화(H-14)**, `max` 무효 |

---

## 7. 확인 결과 문제가 없었던 항목

검수 중 **의심했다가 소스로 확인해 결함이 아님을 확정한** 항목들입니다. 다음 점검에서 다시 파고들 필요가 없습니다.

- **서버액션 인가** — 65개 전부 가드 통과. 가드 없는 액션 0건.
- **가맹점 콘솔 IDOR** — 24개 액션 전수 확인, 모두 `merchantId` 소유 재검증. 결제자 액션도 클라이언트 `userId` 를 받지 않는다.
- **정산 원장 append-only** — 앱 코드에 `settlementLedger.update/delete/upsert` 없음. DB 트리거(`20260831200000_rename_domain_models/migration.sql:78`) 설치 확인. 정정은 반대 분개로 처리.
- **멱등 4중 방어** — `providerMessageId` UNIQUE / `idempotency_key` / PIN 세션 `updateMany` 원자적 선점 / 주문번호 재사용+거래결과조회 모두 코드에서 성립. 이중결제 경로를 찾지 못했다.
- **`status=pending` 에 실물 주문 혼입** — 없음. `charge-select.ts:359` 가 금액 확정 시점에 `PHYSICAL → SKIPPED` 로 못박고 파트너 API 가 이를 존중한다.
- **퍼센트↔소수 변환** — 여러 케이스 직접 실행 검증, 100배·0.01배 오류 없음.
- **자동 지급 이중 실행** — advisory lock + `{merchantId}:{dateKey}` 멱등키 + 트랜잭션 내부 재조회로 보호.
- **금융·개인정보 마스킹** — 계좌 `accountEnc`+`accountTail4`+`holderMasked` 3분리, 주민번호 `residentEnc`/`residentMasked`, 전화번호 `phoneMasked`. 화면 경로에 복호화 호출 없음(파트너 API 반출만 예외이며 이는 계약된 동작).
- **파트너 API 키** — 원문은 발급 응답 1회만, DB에는 `keyHash`/`signingEnc`. `safeEqual` 타이밍 안전 비교, 커서 소유권 확인.
- **`media/[name]` 경로 탈출** — 없음. 32hex 정규식 + `nosniff` + `CSP: sandbox`.
- **인메모리 폴백** — 운영에서 `ALLOW_INMEMORY_FALLBACK=true` 는 기동 실패로 처리, `degrade()` 재던짐. 닫혀 있다.
- **오픈 리다이렉트** — `safeNextPath` 가 `//`·`/\`·`/api/`·`/login` 차단.
- **로그 개인정보** — `scrubText` 가 전화번호 마스킹 + URL 제거, `SENSITIVE_KEYS` 에 secret/signature/token 포함.
- **cron/dev 라우트** — `CRON_SECRET` Bearer + `safeEqual` + 실행 잠금(fail-closed), dev 는 `isLocal` 게이트(APP_ENV 오타 시 prod 판정하는 안전한 기본값).
- **미await Promise** — 전 범위에서 의도적인 1건(`partner-auth.ts:235`, `.catch` 있음) 외 없음.
- **깨진 링크 / 고아 페이지** — 0건. `/support`·`/faq`·`/how-it-works`·`/terms/e-finance` 등 전부 실재.
- **이모지** — 0건.
- **KST 처리** — `kstDateKey/kstMonthKey/formatKst` 가 UTC+9 고정 오프셋으로 일관 처리. 예외는 H-8 의 CSV 라우트 한 곳뿐.
- **환불 시 한도 차감 복원** — `refund.ts:166` 이 `rollbackCounters` 를 호출한다(화면 안내와 일치).

---

## 8. 조치 제안 순서

**1차 (기능이 깨져 있는 것 — 사용자가 지금 겪는 문제)**
H-3 상품 노출 체크박스 · H-4 sortOrder 초기화 · H-5 직접 입력 범위 · H-6 환불 요청 시 주문 소멸 · M-17 지급 대기 데드엔드 · M-25 배송 메모 덮어쓰기

**2차 (돈·장부·법 — 사고가 나면 되돌릴 수 없는 것)**
H-1 환불 데드엔드 · H-2 약관 소멸 · H-7 부가세 미보정 · H-8 CSV 타임존 · M-11 정책 이력 변조 · M-14/M-15 지급 예정액 불일치

**3차 (보안·개인정보)**
H-9 계좌 변경 감사로그 · H-13 프로필 승계 · H-14 한도 클램프 · H-15 SMS 속도제한 · M-3 파트너 API 호출 기록 · M-4 XFF 첫 홉 · M-5 GET 부수효과 · M-8 열람자 미기록 · M-29 탈퇴 파기

**4차 (권한 경계 통일)**
H-12 및 같은 계열 액션 7종 · M-1 admin 페이지 자체 가드 · M-2 열람 권한 분리 · 낮음의 UI `disabled` 반영

**5차 (수치 신뢰 · UI)**
M-21/M-22 타일 모집단 · M-23 30곳 절단 · M-33 CSV 절단 고지 · 페이지네이션 클램프 · 용어·문구 정리

---

### 부록 · 재검증 방법

```bash
# 정적 검증 (리눅스 셸에서 유효)
npx tsc --noEmit
npx eslint src

# 테스트는 Windows 에서
도구_테스트실행.bat

# H-3 재현: 상품 수정 화면에서 "노출" 해제 후 저장 → DB 의 active 확인
# H-4 재현: 상품 순서를 바꾼 뒤 아무 상품이나 저장 → sortOrder 전부 0
# H-11 재현: /admin/users?role=constructor
```
