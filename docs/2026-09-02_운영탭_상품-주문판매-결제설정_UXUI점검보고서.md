# 운영 탭 점검 보고서 — 상품 설정 · 주문/배송 · 결제 설정

- 작성일: 2026-09-02
- 대상: `/studio` 가맹점 관리자 → 운영 그룹 (상품 설정 / 주문·배송 / 결제 설정 / 금칙어·차단 / 신고)
- 목적: 가맹점(사용자) 입장에서 상품 등록·주문 처리·결제 설정의 UX/UI와 기능 점검
- 상태: **점검·제안만. 코드 수정 없음.**

---

## 0. 한 장 요약

지금 운영 탭은 **"기능은 대부분 있는데, 일반적인 판매 관리자에서 기대하는 모양이 아니다"** 는 상태입니다.

| 구분 | 지금 | 문제의 성격 |
|---|---|---|
| 상품 설정 | 한 화면에 목록+등록폼을 모두 펼친 구조, 필드 나열식 | 일반 커머스 상품등록 폼(섹션형·목록/상세 분리)과 다름 |
| 주문·배송 | 실물만 다룸. 비실물 판매는 '결제 내역'에 섞여 있음 | 판매 관리의 절반이 메뉴에 없음 |
| 결제 설정 | 탭 7개 중 3개가 사실상 읽기 전용, 1개는 상품설정과 중복 | 메뉴 이름과 내용 불일치 |

그리고 **기능 결함 3건**(다음 항목)은 UI 개편과 별개로 먼저 고쳐야 합니다.

1. 상품 이미지가 저장되지만 구매자 화면에 **한 번도 표시되지 않음** (폼 안내문은 "결제 화면에 보여집니다"라고 잘못 안내)
2. 주문·배송 목록이 배송지(이름·전화·주소) **원문을 전 건 자동 복호화**해서 뿌림 — 마스킹 컬럼과 감사로그 설계가 있는데 사용하지 않음
3. 실물 주문이 **'포인트 지급 대기'에 섞여** 결제 내역과 파트너 API에 올라옴

---

## 1. 점검 범위와 근거 파일

| 화면 | 파일 |
|---|---|
| 운영 메뉴 정의 | `src/app/studio/layout.tsx` |
| 상품 설정 | `src/app/studio/products/page.tsx` (593줄) |
| 주문·배송 | `src/app/studio/orders/page.tsx` (279줄) |
| 결제 설정 | `src/app/studio/settings/page.tsx` (583줄) |
| 서버 액션 | `src/app/actions/studio.ts` |
| 상품 계산 로직 | `src/server/services/products.ts` |
| 구매자 선택 화면 | `src/app/r/[token]/select-amount-panel.tsx` |
| 데이터 모델 | `prisma/schema.prisma` (ChargeProduct / ChargeShipment / MerchantShippingPolicy) |

---

## 2. 상품 설정

### 2-1. 지금 구조

`상품 설정` 안에 탭 3개 — **비실물 상품 / 실물 상품 / 배송 정책**.
각 탭은 `[통계 4칸] → [등록된 상품 카드들(각 카드가 곧 수정 폼)] → [맨 아래 추가 폼]` 순서입니다.

즉 **목록과 편집 폼이 같은 화면에 겹쳐 있고, 상품 개수만큼 폼과 저장 버튼이 생깁니다.** 상품이 10개면 저장 버튼이 11개(수정 10 + 추가 1)입니다.

### 2-2. 일반적인 실물 상품 등록 폼과의 차이

셀러 어드민 계열(셀러브릭스·카페24·고도몰 등)의 **실물 상품 등록(간편등록 아님)** 폼은 보통 이렇게 섹션이 나뉩니다. 현재 구현과 대조하면:

| 일반 폼의 섹션 | 항목 | 메시지페이 현재 |
|---|---|---|
| ① 기본 정보 | 판매상태, 상품명, 상품코드(SKU), 카테고리, 노출순서 | 있음 (카테고리 없음) |
| ② 판매 정보 | 판매가, 할인가, **과세구분**, 재고, 최소/최대 구매수량 | 판매가·재고·최대수량만. **과세구분·할인 없음** |
| ③ 옵션 | 조합형/단독형, **옵션별 추가금·재고·품절** | 자유 텍스트 3줄뿐. 옵션별 재고·추가금 **불가** |
| ④ 이미지 | **대표 이미지(필수)**, 추가 이미지, 상세 이미지 | 등록 폼에만 1장. **수정 폼엔 이미지 칸 자체가 없음** |
| ⑤ 상세 설명 | 에디터(HTML/이미지) | 300자 textarea |
| ⑥ 배송 정보 | 배송비 유형, 도서산간, **출고 소요일, 반품·교환 배송비, 반품지 주소** | 배송비/조건부무료/무료배송만. **반품 관련 전무** |
| ⑦ 상품정보 제공 고시 | 전자상거래법 품목별 필수 고지 | **없음** |
| ⑧ 노출/검색 | 태그, 진열 여부 | 노출 체크박스만 |

**빠진 것 중 법적으로 문제가 되는 것**: ⑦ 상품정보 제공 고시, ⑥ 반품·교환 배송비와 반품지, 청약철회 안내. 실물 상품을 파는 이상 전자상거래법상 필요합니다.

### 2-3. 발견 사항 (상품 설정)

| # | 내용 | 근거 |
|---|---|---|
| P-1 | **상품 이미지가 구매자에게 안 보임.** `ChargeProduct.imageUrl`을 저장하지만 `/r/[token]` 선택 화면·`/c/[code]` 어디에도 렌더하는 코드가 없음. `PanelProduct` 인터페이스에 이미지 필드가 아예 없음 | `select-amount-panel.tsx:22-38` |
| P-2 | **수정 폼에 이미지 칸이 없음.** `ImageUploadField`가 실물 *추가* 폼에만 있어 등록 후 이미지 교체·삭제 불가. 비실물은 등록 폼에도 없음 | `products/page.tsx` 실물 추가 폼 |
| P-3 | 폼 안내문 "상품 이미지 (선택) … 결제 화면에 함께 보여집니다" 가 **사실과 다름** | 위 P-1 |
| P-4 | **보관(archive)이 단방향.** 복원 액션도 보관 목록 화면도 없음. 실수로 보관하면 되돌릴 수 없음 | `archivedAt`을 되돌리는 코드 0건 |
| P-5 | **재고 입력칸이 한 카드에 두 개.** 수정 폼의 `재고` + 하단 `재고 즉시 조정`. 서로 다른 액션이라 나중에 누른 쪽이 이김 | `updateChargeProductAction` / `adjustProductStockAction` |
| P-6 | **옵션이 자유 텍스트.** `사이즈: S, M, L` 형식. 옵션별 재고·추가금·품절 표시 불가. 옵션 3종/값 30개 상한이 화면에 안 나옴 | `products.ts:166-181` |
| P-7 | **노출 순서가 숫자 입력.** 드래그·위아래 이동 없음. 순서 하나 바꾸려면 상품마다 따로 저장 | |
| P-8 | **상품 종류별 12개 제한**이 사전 고지 없음. 12개째 등록을 눌러야 에러로 알게 됨 | `studio.ts:84` `MAX_CHARGE_PRODUCTS = 12` |
| P-9 | **상품 복제 없음.** 옵션만 다른 변형 상품을 만들려면 전부 재입력 | |
| P-10 | **미리보기 없음.** 등록한 상품이 구매자 화면에서 어떻게 보이는지 확인하려면 결제페이지 링크로 나가야 함 | |
| P-11 | 배송 정책이 상품 설정 안에 있고 주문·배송에는 없음 — 배송 업무가 두 메뉴에 갈라짐 | |
| P-12 | 실물 카드 하나에 입력칸 12개 + 옵션 + 설명이 한 줄로 이어져 모바일에서 대단히 김 | |
| P-13 | 보관 버튼만 브라우저 기본 `window.confirm`을 씀(다른 곳은 자체 ConfirmDialog) — 같은 화면에 확인창이 두 종류 | `action-form.tsx` `InlineActionForm` |

**잘 되어 있는 점** (유지 권장): 실물 상품의 "1개 주문 시 결제 금액 = 상품 + 배송비" 실시간 박스, 결제 한도 초과 경고, 재고 부족/품절 통계 타일, 상품별 배송비가 기본 정책을 덮어쓴다는 설명, 마지막 상품 보관을 막는 방어(`archiveChargeProductAction`).

### 2-4. 제안 — 등록 폼 개편안

**공통**: 목록과 등록/수정을 **분리**합니다. `상품 관리` 목록(표) → 행 클릭 시 `/studio/products/[id]` 상세 편집, `+ 상품 등록` 버튼 → `/studio/products/new`. 목록에는 이미지 썸네일·상품명·유형·판매가·재고·상태·판매건수, 그리고 체크박스 선택 후 **일괄 노출/숨김·보관**.

**실물 상품 등록 폼 (섹션형)**

1. 기본 정보 — 판매 상태, 상품명, 상품코드(SKU), 노출 순서
2. 판매 정보 — 판매가, **과세 구분(과세/면세)**, 재고, 재고 경고 기준, 1회 최대 수량
3. 옵션 — 옵션 없음 / 단독형 / 조합형. 옵션값마다 **추가금·재고·품절** 입력
4. 이미지 — 대표 이미지(필수), 추가 이미지 최대 5장, 상세 이미지
5. 상세 설명
6. 배송 정보 — `기본 정책 따름` / `이 상품만 개별 설정` 토글, 배송비 유형(무료·유료·조건부무료), 도서산간, **출고 소요일**, **반품/교환 배송비**, **반품지 주소**
7. **상품정보 제공 고시** — 품목 선택 후 필수 항목 입력
8. 우측 고정 패널에 **실시간 결제금액 시뮬레이션**(현재의 "1개 주문 시 결제 금액" 박스를 폼 안으로) + **구매자 화면 미리보기**

**비실물(컨텐츠) 상품 등록 폼**

1. 기본 정보 — 유형(포인트 / 상품권 / 이용권 / **컨텐츠**), 상품명, 상품코드, 노출 순서, 판매 상태
2. 판매 정보 — 판매가, 지급 수량·단위, 유효기간(일)
3. **지급 방식** — `가맹점 수동 지급` / `연동 API 자동 지급` / `즉시 발급(코드·다운로드 링크)`. 지금은 수동 + API pull만 가능하고 화면에서 고를 수 없음
4. 이미지·상세 설명
5. **청약철회 안내** — 디지털 콘텐츠 특례(사용 개시 시 철회 제한) 고지 문구
6. 미리보기

> 표기 규칙: 화면 문구는 **「비실물(컨텐츠)」** 로 통일. 유형 선택지도 포인트/상품권/이용권/컨텐츠 4종으로.

---

## 3. 주문·배송 → 「주문·판매」

### 3-1. 명칭

요청하신 대로 **"주문·배송" → "주문·판매"** 로 바꾸고 안에서 둘로 나누는 것이 맞습니다.

```
주문·판매
 ├ 실물 주문·배송     (지금의 orders)
 ├ 비실물(컨텐츠) 판매내역  (지금 '결제 내역'에 섞여 있는 지급 처리)
 └ 반품·교환          (신설)
```

지금은 비실물 판매를 관리할 전용 화면이 없고, `결제 내역`의 "포인트 지급 대기" 패널로 처리합니다. 상품 설정에서는 실물/비실물을 나눠 놓고 판매 관리에서는 안 나뉘어 있는 게 가장 큰 비대칭입니다.

### 3-2. 발견 사항 (주문·배송)

| # | 내용 | 근거 |
|---|---|---|
| O-1 | **배송지 원문 전 건 자동 복호화.** 목록을 열기만 해도 20건의 받는분 이름·전화·주소가 평문으로 렌더됨. `receiverMasked / phoneMasked / addressMasked` 컬럼이 있는데 미사용, 감사로그(`writeAudit`) 호출도 없음. 스키마 주석("원문은 … 감사로그를 남긴다")과 화면 Notice("암호화되어 저장되며")와 모두 어긋남 | `orders/page.tsx:194-198`, `schema.prisma:646-666` |
| O-2 | **검색이 없음.** 상태 필터 5개와 페이지 이동뿐. 거래번호·받는분·송장번호·상품명·기간으로 못 찾음 | |
| O-3 | **정렬 고정** (배송준비 먼저 → 오래된 순). 최신순 전환 불가 | `orderBy: [{status:'asc'},{createdAt:'asc'}]` |
| O-4 | **일괄 처리 없음.** 송장 일괄 등록(엑셀 붙여넣기/업로드), 일괄 발송처리 없음. 하루 50건이면 저장 50번 | |
| O-5 | **엑셀 내려받기 없음.** 결제 내역에는 CSV export가 있으나 컬럼이 12개뿐이고 **상품명·수량·옵션·배송비·배송지·송장이 전부 빠져 있음**. 택배사 발주서로 쓸 수 없음 | `api/studio/charges/export/route.ts:69-82` |
| O-6 | **가맹점이 환불·부분취소를 못 함.** `requestRefund`는 이용자(payer)와 관리자만 호출. 품절·배송불가 시 가맹점이 할 수 있는 건 "배송 취소 표시" 뿐이고 안내문도 "결제 환불은 별도로 진행해 주세요"로 끝남 | `actions/payer.ts:219`, `actions/admin/transactions.ts:269` |
| O-7 | **배송 상태가 구매자에게 통보되지 않음.** 송장을 등록해도 발송 알림 문자/알림이 나가지 않음 | `updateShipmentAction`에 MT 발송 없음 |
| O-8 | **반품·교환 상태값이 없음.** `ShipmentStatus` = PREPARING/SHIPPED/DELIVERED/CANCELED 4개뿐 | `schema.prisma` |
| O-9 | **배송 지연 관리 지표 없음.** `shippedAt/deliveredAt`은 쌓이는데 "결제 후 N일 미발송" 경고가 없음 | |
| O-10 | **결제 상세 화면에 상품·수량·옵션·배송비·배송상태가 없음.** 주문 카드에서 거래번호를 눌러 상세로 들어가면 오히려 정보가 줄어듦 | `studio/charges/[id]/page.tsx` |
| O-11 | 통계 타일이 4개인데 "환불됨"은 배지로만 표시되고 집계에 없음 | |

**잘 되어 있는 점**: 발송 처리 시 택배사·송장 필수 검증, `shippedAt/deliveredAt`을 최초 1회만 기록해 지연 통계를 지키는 처리, 도서산간 배지, 결제 완료 건만 주문으로 취급하는 필터, 상품 금액/배송비/결제 금액 분리 표기.

### 3-3. 제안 — 고도화 항목

**실물 주문·배송 탭**

- 상단: 기간 + 상태 + 통합검색(거래번호·받는분·송장·상품명) + 정렬 선택
- 목록 기본은 **마스킹**(홍*동 / 010-****-1234 / 서울 강남구). 행의 `[배송지 보기]`를 눌러야 원문 노출 + 감사로그 기록
- 체크박스 선택 → **일괄 발송 처리**, **송장 일괄 등록**(택배사 선택 + 거래번호·송장 붙여넣기)
- **주문서 엑셀 내려받기** / 송장 엑셀 업로드
- 발송 처리 시 구매자에게 **발송 안내 문자** 발송(옵션 토글)
- 상태에 `반품접수 / 회수중 / 반품완료 / 교환접수 / 교환발송` 추가
- "결제 후 2일 이상 미발송" 지연 경고 타일

**비실물(컨텐츠) 판매내역 탭**

- 지금 결제 내역에 있는 지급 대기/완료/보류 처리를 이쪽으로 이동
- 상품별·유형별 필터(포인트/상품권/이용권/컨텐츠)
- 지급 완료 시 구매자 알림, 지급 취소(회수) 처리
- 유효기간 있는 상품의 만료 임박 목록

**결제 상세 화면(`charges/[id]`)에 주문 정보 블록 추가** — 상품명·수량·옵션·상품금액·배송비·배송상태·송장. O-10 해소.

---

## 4. 결제 설정 — 탭별 정밀 점검

현재 탭 7개: `결제 금액 / MO 안내문자 / 감사문자 / 결제 모드 / 문자번호 / 결제페이지 / 연동 API`.
이 중 **완전 읽기 전용이 2개(결제 모드·문자번호)**, **절반이 읽기 전용인 것이 1개(결제 금액)** 입니다.

### 4-1. 결제 금액 탭 — 이름과 내용이 다름

| 문제 | 상세 |
|---|---|
| 이름-내용 불일치 | "결제 금액" 탭에서 금액을 정할 수 없음. 실제 금액은 상품 설정에서 정함 |
| 중복 | 상품 목록이 읽기 전용으로 한 번 더 나옴. 상품 설정과 이중 관리 |
| 오해 소지 | 실물 상품 금액을 **배송비 없이** 표시. 실제 결제액과 다름 |
| 기능 부족 | 직접 입력 허용 시 가맹점이 **자체 상·하한이나 입력 단위(1,000원 단위 등)를 정할 수 없음**. 플랫폼 정책 범위 그대로 |
| **코드 결함** | `createChargeProductAction`, `updateChargeProductAction`, `archiveChargeProductAction` 3개가 **import만 되고 미사용**. ESLint 경고 3건 확인. 예전에 이 탭에서 상품을 등록하던 흔적 |

### 4-2. MO 안내문자 탭 — 검증은 튼튼, 안내는 부족

- 좋은 점: 160자 제한 / 링크 금지 / 미정의 치환자 금지 / 금칙어·개인정보 필터, **현재 설정 미리보기 + 기본 문구 비교**, "아직 결제되지 않았습니다" 고지를 시스템이 강제로 붙인다는 설명. 감사문자와의 차이를 상단 Notice로 설명하는 것도 좋음.
- 지적:
  - 저장 검증이 **템플릿 원문 160자**만 봄. `{상품목록}`은 상품명 4개까지 치환되므로 **치환 후 실제 발송 길이는 검사하지 않음**. 가맹점명·상품명이 길면 SMS→LMS로 넘어가 발송 단가가 달라짐. 미리보기 옆에 **"치환 후 N자 · SMS/LMS 구분"** 표시 필요
  - **테스트 발송 버튼이 없음**. 저장 후 실제 문자를 자기 번호로 받아볼 방법 없음
  - 치환자를 클릭해서 본문에 삽입하는 기능 없음(눈으로 보고 직접 타이핑)

### 4-3. 감사문자 탭

- MO 안내문자와 같은 수준으로 잘 검증됨(링크·개인정보 금지, 미리보기, 기본 문구 비교).
- 지적: 테스트 발송 없음 / 발송 실패 건의 재발송 경로 없음 / MO 안내문자 탭과 화면 구조가 거의 같아 **두 탭을 하나로 합쳐 섹션 2개로 두는 편이 이동이 적음**.

### 4-4. 결제 모드 탭 — 탭 하나를 쓸 내용이 아님

- 전부 읽기 전용(확인형/즉시형 설명 + 플랫폼 허용 여부 + 안내 Notice).
- 가맹점이 할 수 있는 행동이 0. **문자번호 탭과 합쳐 "수신·결제 방식(읽기 전용)" 한 탭**으로 충분.

### 4-5. 문자번호 탭

- 읽기 전용. 수신번호/수신방식/키워드/상태만.
- 지적: 키워드형인 경우 **이용자에게 안내할 문구를 복사하는 버튼이 없음**(예: `1600-0000 으로 '충전' 전송`). 번호 복사 버튼도 없음.

### 4-6. 결제페이지 탭

| # | 내용 |
|---|---|
| S-1 | 섹션 설명에 **"배너·소개·라이브 표시를 관리합니다"** 라고 되어 있으나 라이브 표시 필드가 없음 — 옛 후원 서비스 잔재 문구 |
| S-2 | 배너 라디오 기본 선택 로직이 `bannerUrl == null` 이면 자동 배너를 선택된 것처럼 보여줌. 그대로 저장하면 자동 배너가 **고정 값으로 굳음**. "자동(기본 배너)" 선택지가 명시적으로 없음 |
| S-3 | **미리보기 없음.** 저장 후 `/c/{코드}` 를 새 탭으로 열어봐야 함 |
| S-4 | 상품이 결제페이지에서 어떻게 보이는지 여기서 확인 불가 (상품 설정에도 없음 — 미리보기가 어디에도 없음) |

### 4-7. 연동 API 탭

- 좋은 점: "키 원문은 발급 직후 한 번만 표시" 경고가 명확, 폐기 확인창, 마지막 사용 시각 표기, 이 API가 필요한 경우를 먼저 설명하는 구성.
- 지적:
  - 규격서를 "고객센터로 요청"하라면서 파일 경로(`docs/연동규격서_가맹점API.md`)를 그대로 노출 — **문서 페이지 또는 다운로드 링크**가 필요
  - 키별 **권한 범위·IP 허용목록** 없음
  - **최근 호출 로그/에러가 안 보임**(lastUsedAt만) — 연동 디버깅 불가
  - `GET /ping` **연결 테스트 버튼**이 화면에 없음

### 4-8. 제안 — 메뉴 통합안

요청하신 "상품설정·주문배송·결제설정을 합칠 수 있으면 합치자"에 대한 답은 **"완전 통합보다는 3개를 판매 업무 흐름대로 재배치"** 입니다. 세 메뉴를 한 페이지에 합치면 탭이 12개 이상이 되어 오히려 나빠집니다.

```
운영  →  판매

  상품 관리        목록 / 등록·수정 (실물 · 비실물(컨텐츠))
  주문·판매        실물 주문·배송 / 비실물(컨텐츠) 판매내역 / 반품·교환
  판매 설정        결제·금액 / 배송 정책 / 안내 문자 / 결제페이지 / 연동 / 수신·결제 방식(읽기전용)
  금칙어·차단
  신고
```

핵심 이동 3가지:

1. **배송 정책**: 상품 설정 → 판매 설정 (배송 업무를 한 곳으로)
2. **MO 안내문자 + 감사문자**: 두 탭 → `안내 문자` 한 탭에 섹션 2개
3. **결제 모드 + 문자번호**: 두 탭 → `수신·결제 방식` 한 탭 (읽기 전용끼리)
4. **결제 금액 탭의 상품 목록 블록 삭제**, 직접입력·한도·(신설)입력 단위만 남김

결과: 결제 설정 7탭 → 판매 설정 6탭, 읽기 전용 탭 2개 → 1개, 상품 목록 중복 제거.

---

## 5. 운영 탭 전체 — 가맹점 편의성 점검

| # | 내용 | 근거 |
|---|---|---|
| G-1 | **실물 주문이 '포인트 지급 대기'에 섞임.** charge 생성 시 `pointStatus` 기본값이 PENDING이고 실물이면 SKIPPED로 두는 코드가 없음. 결제 내역의 지급 대기 패널 조건에 상품 종류가 빠져 있어 실물 주문까지 올라옴. 파트너 API `status=pending` 에도 섞여 나감(응답에 `product.kind`가 있어 가맹점 서버가 거를 수는 있음) | `schema.prisma:919`, `charges/page.tsx:87-91`, `api/partner/v1/charges/route.ts:73` |
| G-2 | **용어 불일치.** 화면은 "지급 대기 / 지급 완료 / 보류", CSV는 "대기 / 성공 / 실패"(deliveryStatusLabel 그대로 사용). 같은 값이 두 이름 | `charges/page.tsx:31-35` vs `export/route.ts:96` |
| G-3 | **사이드바에 처리 대기 뱃지가 없음.** 배송 준비 N건 / 지급 대기 N건 / 신고 접수 N건이 메뉴에 안 뜸. `NavGroup` 항목에 badge 필드 자체가 없음 | `console-shell.tsx:30` |
| G-4 | **대시보드에서 상품 설정·주문·배송으로 가는 링크가 없음** (결제내역·문자관리·정산만) | `studio/page.tsx` |
| G-5 | **온보딩 체크리스트에 "상품 등록"이 없음.** 수신번호·정산계좌·사업자정보 3개뿐. 상품이 0개이고 직접입력도 꺼져 있으면 결제 자체가 불가능한데 체크리스트가 안 짚어줌 | `onboarding-checklist.tsx:40-64` |
| G-6 | **확인창이 두 종류.** `ActionForm`은 자체 ConfirmDialog, `InlineActionForm`은 브라우저 기본 `window.confirm`. 상품 보관·API 키 폐기가 후자 | `action-form.tsx` |
| G-7 | 저장 결과가 폼 안 인라인 Notice로만 뜸. 긴 폼에서는 스크롤 위치에 따라 놓칠 수 있음(토스트 없음) | |
| G-8 | 모바일: 결제 설정 탭 7개가 4열 그리드 2줄 12px. 실물 상품 카드는 입력칸 12개가 세로로 길게 늘어남 | |
| G-9 | 메뉴 라벨·안내 문구가 전반적으로 실물 위주("주문·배송"). 비실물 판매 가맹점에게는 빈 화면 | |
| G-10 | 상품 0개 상태의 빈 화면에서 바로 등록으로 가는 CTA가 없음(Notice 문구만) | |

**금칙어·차단 / 신고 탭**은 목적이 뚜렷하고 안내(“금칙어는 결제를 막지 않고 기록만 가린다”, “신고는 통합 관리자가 처리”)가 명확해 이번 개편에서 건드릴 필요가 없습니다.

---

## 6. 우선순위 정리 (개발 지시용)

### A. 기능 결함 — UI 개편과 무관하게 먼저

| 순위 | 항목 | 참조 |
|---|---|---|
| A-1 | 배송지 마스킹 기본 + [보기] 시 복호화 + 감사로그 | O-1 |
| A-2 | 실물 주문을 포인트 지급 대상에서 제외(`pointStatus = SKIPPED`) | G-1 |
| A-3 | 상품 이미지 구매자 화면 렌더 + 수정 폼 이미지 칸 추가 (또는 기능 자체를 제거하고 안내문 수정) | P-1~P-3 |
| A-4 | 결제 상세 화면에 상품·수량·옵션·배송비·배송상태 블록 추가 | O-10 |
| A-5 | 상품 보관 복원 경로 | P-4 |
| A-6 | 결제설정 미사용 import 3개 제거, "라이브 표시" 잔재 문구 수정 | 4-1, S-1 |
| A-7 | 지급 상태 용어 통일(화면·CSV) | G-2 |

### B. 상품 등록 UX 개편

| 순위 | 항목 |
|---|---|
| B-1 | 목록/상세 분리 (`/studio/products` 목록 → `/new`, `/[id]`) |
| B-2 | 실물 등록 폼 섹션화 + 반품/교환·출고 소요일·과세구분·상품정보 제공 고시 |
| B-3 | 비실물(컨텐츠) 등록 폼 — 유형에 '컨텐츠' 추가, 지급 방식 선택, 청약철회 안내 |
| B-4 | 옵션을 구조화(조합형/단독형, 옵션별 재고·추가금) |
| B-5 | 구매자 화면 미리보기 |
| B-6 | 상품 복제, 드래그 정렬, 등록 상한 사전 고지 |

### C. 주문·판매 고도화

| 순위 | 항목 |
|---|---|
| C-1 | 메뉴명 "주문·배송" → "주문·판매", 탭 분리(실물 / 비실물(컨텐츠) / 반품·교환) |
| C-2 | 검색·기간·정렬 + 일괄 발송·송장 일괄 등록 |
| C-3 | 주문서 엑셀 내려받기(상품·수량·옵션·배송지·송장 포함) |
| C-4 | 발송 시 구매자 안내 문자 |
| C-5 | 가맹점 환불·부분취소 요청 경로 |
| C-6 | 반품·교환 상태값 확장, 배송 지연 경고 |

### D. 결제 설정 정리

| 순위 | 항목 |
|---|---|
| D-1 | 탭 통합(안내 문자 / 수신·결제 방식), 결제 금액 탭의 상품 목록 중복 제거 |
| D-2 | 배송 정책을 판매 설정으로 이동 |
| D-3 | 문자 테스트 발송, 치환 후 길이·SMS/LMS 표시, 치환자 클릭 삽입 |
| D-4 | 결제페이지 미리보기, 배너 "자동" 선택지 명시 |
| D-5 | API 규격서 링크, 연결 테스트 버튼, 호출 로그 |

### E. 공통

| 순위 | 항목 |
|---|---|
| E-1 | 사이드바 처리 대기 뱃지 |
| E-2 | 온보딩 체크리스트에 "상품 등록" 추가 |
| E-3 | 대시보드 → 상품·주문 링크 |
| E-4 | 확인창 일원화(ConfirmDialog), 저장 결과 토스트 |

---

## 7. 결정이 필요한 사항

개발 지시 전에 정해 주셔야 하는 것들입니다.

1. **옵션 구조화 범위** — 조합형 옵션(사이즈×색상별 재고·추가금)까지 갈지, 단독형(옵션값별 추가금만)에서 멈출지. 조합형은 `ChargeProduct` 아래 variant 테이블이 추가로 필요합니다.
2. **가맹점 환불 권한** — 가맹점이 직접 환불을 실행하게 할지, 환불 "요청"만 만들고 관리자 승인을 거치게 할지.
3. **상품 상세 설명** — 300자 텍스트를 유지할지, 이미지가 들어가는 에디터로 갈지(업로드 용량·XSS 처리 범위가 달라집니다).
4. **메뉴명 확정** — "주문·판매" / "판매 관리" / "주문 관리" 중 어느 것으로 할지.
5. **비실물 4번째 유형** — 기존 POINT/VOUCHER/PASS에 `CONTENT`를 추가할지, 아니면 화면 표기만 "비실물(컨텐츠)"로 하고 유형은 3종 유지할지.
