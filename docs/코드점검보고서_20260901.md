# 메시지페이 코드 전수 점검 보고서

작성일: 2026-09-01  
점검 범위: `src/`, `prisma/`, `.env`, `.env.example`  
기준 커밋: 현재 HEAD

---

## 요약

| 심각도 | 건수 |
|--------|------|
| High   | 2    |
| Medium | 6    |
| Low    | 4    |

---

## HIGH

### H1. 파트너 API GET /charges에서 이용자 전화번호 원문 노출

**파일**: `src/app/api/partner/v1/charges/route.ts` (약 line 90)

GET /api/partner/v1/charges 응답 페이로드에 `payerPhone: decrypt(r.payer.phoneEnc)` 가 포함된다. 이 엔드포인트는 GET 요청이므로 partner-auth.ts 설계상 HMAC 서명 없이 Bearer 토큰만으로 인증된다.

결과: 가맹점 API 키 1개가 유출되면 해당 가맹점과 거래한 모든 이용자의 전화번호 원문을 limit=500 단위로 페이지네이션하며 대량 추출할 수 있다.

**권고**:
- GET 응답에서 `payerPhone` 원문을 제거하고, `payerPhoneMasked`(마스킹)와 `payerRef`(해시)만 제공한다.
- 전화번호 원문이 반드시 필요한 경우 POST + HMAC 서명 요구 엔드포인트로 분리한다.

---

### H2. 소셜 로그인 콜백에 state(CSRF) 검증 코드 없음

**파일**: `src/app/api/auth/social/[provider]/callback/route.ts` (전체)

OAuth 시작 라우트(`/api/auth/social/[provider]`)는 `messagepay_social_state_<provider>` 쿠키에 state를 저장한다. 그러나 콜백 라우트에서 state 쿠키와 파라미터를 대조하는 코드가 전혀 없다. 주석에 "state 쿠키와 콜백 state 대조 (CSRF 방어)" 항목이 TODO로 나열되어 있으나 미구현 상태다.

현재는 `KAKAO_CLIENT_ID` 등 키가 없어 실제 OAuth 흐름이 막혀 있어 직접 피해는 없지만, OAuth 연동 키를 추가하는 순간 CSRF 공격에 즉각 노출된다.

**권고**: 콜백 라우트에서 반드시 다음을 수행한다.
```typescript
const cookieState = jar.get(`messagepay_social_state_${provider}`)?.value;
const paramState = url.searchParams.get('state');
if (!cookieState || !safeEqual(cookieState, paramState ?? '')) {
  return redirect('/login?error=social_csrf');
}
jar.delete(`messagepay_social_state_${provider}`);
```

---

## MEDIUM

### M1. secure 쿠키 판정 기준이 파일마다 다름

**파일**:
- `src/app/actions/inquiry.ts:137` — `process.env.APP_BASE_URL?.startsWith('https') ?? false`
- `src/app/actions/support.ts:197` — 동일
- `src/server/auth.ts:63` — `process.env.NODE_ENV === 'production' && env.baseUrl.startsWith('https')`
- `src/app/api/auth/social/[provider]/route.ts:51,58` — 동일

스테이징 환경(APP_ENV=staging, NODE_ENV=development, APP_BASE_URL=https://...)에서 세션 쿠키는 non-secure로, 문의 게스트 쿠키는 secure로 발행된다. 동작 불일치 및 예상 밖의 쿠키 drop이 발생할 수 있다.

**권고**: 모든 쿠키 secure 판정을 `env.ts`의 `isProd`(또는 `env.baseUrl.startsWith('https')`) 한 곳으로 통일한다.
```typescript
// src/lib/env.ts 에 추가
export const secureCookie = env.baseUrl.startsWith('https');
```

---

### M2. Redis 장애 시 cron 배치 잠금이 무력화됨

**파일**:
- `src/app/api/cron/payout/route.ts:49`
- `src/app/api/cron/cleanup/route.ts:44`

```typescript
const locked = await kv.setnx(LOCK_KEY, ..., LOCK_TTL_SEC).catch(() => true);
if (!locked) return skip;
```

Redis 오류 시 `catch(() => true)` 로 인해 잠금 획득 성공으로 처리되어 배치가 실행된다. Redis가 다운된 상태에서 AWS EventBridge 등이 동시에 여러 번 호출하면 `payout` 배치가 중복 실행된다.

payout 배치는 가맹점 이체를 일으키므로 DB 레벨의 멱등키로 이중 지급이 방어되긴 하나, 잠금이 무력화되는 것 자체가 위험 신호다.

**권고**: Redis 실패 시 배치를 skip 처리한다.
```typescript
const locked = await kv.setnx(LOCK_KEY, ..., LOCK_TTL_SEC).catch(() => false); // false = 잠금 미획득
if (!locked) return skip;
```

---

### M3. 구브랜드 상태명 `BROADCAST_PENDING` / `BROADCASTED` / `PARTIAL_DELIVERY_FAILED` 잔류

**파일**: `src/server/services/charge-flow.ts`, `executePayment` 함수 내

```typescript
if (['PAYMENT_SUCCESS', 'BROADCAST_PENDING', 'BROADCASTED', 'PARTIAL_DELIVERY_FAILED',
     'SETTLEMENT_PENDING', 'SETTLED'].includes(charge.status)) { ... }
```

`BROADCAST_PENDING`, `BROADCASTED`, `PARTIAL_DELIVERY_FAILED`는 도네이도 시절 "방송 노출"에서 비롯된 용어다. 현재 CLAUDE.md에서 "방송 노출 → 충전 반영"으로 개명했으나 Enum 값이 갱신되지 않았다(`src/generated/prisma/enums.ts:119-121` 확인).

Enum 값이 DB에 그대로 저장되어 있으므로 런타임 동작에는 문제가 없지만, CLAUDE.md의 브랜드 원칙에 따라 새 마이그레이션으로 개명해야 한다.

**권고**: 신규 마이그레이션을 추가하여 `BROADCAST_PENDING → REFLECT_PENDING`, `BROADCASTED → REFLECTED`, `PARTIAL_DELIVERY_FAILED → PARTIAL_REFLECT_FAILED` 로 변경한다. (기존 SQL은 수정하지 않는다)

---

### M4. partner-auth.ts: Redis 장애 시 rate limit 무력화

**파일**: `src/server/services/partner-auth.ts:112`

```typescript
const used = await kv.incr(`partner:rate:${key.id}:${minuteBucket}`, 90).catch(() => 0);
if (used > RATE_LIMIT_PER_MIN) { ... }
```

Redis 오류 시 `catch(() => 0)` 으로 카운터가 0 처리되어 rate limit이 우회된다. Redis 장애 중 악의적 가맹점이 초당 수천 건의 요청을 보낼 수 있다.

**권고**: Redis 실패 시 보수적으로 처리한다.
```typescript
const used = await kv.incr(..., 90).catch(() => RATE_LIMIT_PER_MIN + 1);
if (used > RATE_LIMIT_PER_MIN) return fail(429, ...);
```

---

### M5. `PayerMerchantLink.lastDonatedAt` 구브랜드 필드명 잔류

**파일**: `prisma/schema.prisma:470`, `src/server/services/charge-flow.ts`

```
lastDonatedAt  DateTime? @map("last_donated_at")
```

CLAUDE.md에서 "후원(donation) → 결제/충전"으로 개명 원칙을 명시했으나, `PayerMerchantLink`의 `lastDonatedAt` 필드가 갱신되지 않았다. 코드 전체에서 이 필드명을 참조하는 곳도 동일하게 잔류.

**권고**: 신규 마이그레이션으로 `last_donated_at → last_charged_at` 컬럼명 변경 후 스키마와 코드 일괄 갱신.

---

### M6. `expireStaleConfirmations` N+1 쿼리 패턴

**파일**: `src/server/services/charge-confirm.ts:76-86`

```typescript
const stale = await prisma.secureLink.findMany({ where: { ... } });
for (const s of stale) {
  const d = await prisma.charge.findUnique({ where: { id: s.chargeId! } }); // N번
  await setStatus(s.chargeId, 'PAYMENT_FAILED', ...);                        // N번 × 2
}
```

stale 링크가 N개이면 2N+1 번의 DB 왕복이 발생한다. 배치가 1분마다 실행되고 미확인 건이 누적될수록 실행 시간이 선형 증가하여 LOCK_TTL_SEC(55초) 초과 가능성이 있다.

**권고**: `findMany`에 `include: { charge: { select: { status: true } } }` 를 추가해 조인으로 처리하고, `setStatus` 를 `$transaction` 으로 묶어 배치 업데이트한다.

---

## LOW

### L1. admin/inquiries 페이지에 SUPER_ADMIN 전용 권한 체크 없음

**파일**: `src/app/admin/layout.tsx:79-81`, `src/app/admin/inquiries/page.tsx`

레이아웃은 `SUPER_ADMIN` 이 아니면 문의 관리 메뉴를 숨기지만, `admin/inquiries/page.tsx` 자체는 `requireAdmin()` 만 사용하여 모든 관리자가 URL 직접 접근 시 페이지를 볼 수 있다.

**권고**: `admin/inquiries/page.tsx` 최상단에 권한 체크 추가.
```typescript
const admin = await requireAdmin();
if (admin.adminPermission !== 'SUPER_ADMIN') redirect('/admin');
```

---

### L2. `.env` / `.env.example`의 `HECTO_RETURN_URL`, `HECTO_NOTI_URL`이 `env.ts`에서 미사용

**파일**: `.env`, `.env.example`, `src/lib/env.ts:112`

`.env`에는 `HECTO_RETURN_URL`과 `HECTO_NOTI_URL`이 정의되어 있으나, `env.ts`는 이 두 키를 읽지 않는다. 대신 `HECTO_CALLBACK_URL` 하나(`hectoCallbackUrl`)만 읽는다. 운영자가 `.env.example`을 참고해 설정할 경우 실제로 코드에 반영되지 않아 헥토 연동 URL 설정 오류로 이어질 수 있다.

**권고**: `.env.example`에서 `HECTO_RETURN_URL`, `HECTO_NOTI_URL` 를 제거하고 `HECTO_CALLBACK_URL` 만 남긴다.

---

### L3. `inquiry.ts` / `support.ts`에서 `process.env`를 직접 참조

**파일**: `src/app/actions/inquiry.ts:137`, `src/app/actions/support.ts:197`

```typescript
secure: process.env.APP_BASE_URL?.startsWith('https') ?? false,
```

환경변수 접근을 `env.ts` 로 중앙화하는 설계 원칙을 이 파일들만 따르지 않는다. M1과 동일한 판정 불일치의 원인이다.

**권고**: `env.ts`에 `export const secureCookie = env.baseUrl.startsWith('https');` 를 추가하고 모든 쿠키 secure 판정을 이 값으로 교체.

---

### L4. `src/lib/public-content.ts` 구브랜드 필터 정규식 범위 제한

**파일**: `src/lib/public-content.ts:2`

```typescript
const LEGACY_DONATION_TERMS = /도네이도|토네이도|문자\s*후원|후원자|크리에이터|후원\s*(번호|금액|한도|처리|내역|메시지|샵)/i;
```

"후원샵", "후원하기" 등의 단독 후원 표현이나 "문자후원서비스" 같은 변형은 탐지되지 않는다. 공개 콘텐츠에 구브랜드 표현이 섞여 있어도 필터를 통과할 수 있다.

**권고**: `후원` 단독 단어를 필터에 추가하고, admin 화면에서 저장 시에도 이 함수로 검증.
```typescript
/도네이도|토네이도|문자\s*후원|후원자|크리에이터|후원샵|후원하기|후원\s*(번호|금액|한도|처리|내역|메시지|샵)/i
```

---

## 브랜드 잔류 현황 요약

| 항목 | 위치 | 상태 |
|------|------|------|
| `BROADCAST_PENDING` / `BROADCASTED` / `PARTIAL_DELIVERY_FAILED` Enum | `enums.ts:119-121`, `charge-flow.ts` | 미변경 — M3 |
| `lastDonatedAt` 필드명 | `schema.prisma:470`, `charge-flow.ts` | 미변경 — M5 |
| 구브랜드 FAQ 제목 (시드) | `seed.ts:393-397` | 삭제 코드 있음(정상) |
| 소셜 구브랜드 확인 | `public-content.ts` | 필터 범위 제한 — L4 |

> 구브랜드 식별자(`tornado`, `donaido`, `MJP`, `CreatorProfile`, `DonorProfile` 등)는 `src/` 내에서 추가 발견되지 않음. 마이그레이션 이력(`init_tornado`, `munjapay_block_ledger_mutation` 등)은 CLAUDE.md에 따라 적용 이력으로 보존되어 정상.

---

## 점검 제외 항목

- `prisma/migrations/**` — CLAUDE.md에서 수정 금지 지정
- `public/_legacy-munjapay/` — 코드 미참조 보관함
- `src/generated/prisma/` — 자동 생성 파일
- `e2e/` — 테스트 헬퍼
