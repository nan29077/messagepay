/**
 * 시드 데이터 버전.
 *
 * 시드 내용(계정, 정책, 샘플 데이터 등)을 바꿀 때마다 이 숫자를 1 올린다.
 * 미리보기 실행 시 DB 에 기록된 버전이 이보다 낮으면 시드를 다시 실행해 부족한 데이터를 채운다.
 * 시드는 모두 upsert 방식이라 기존 데이터는 지워지지 않는다.
 *
 * 이력
 *   1 - 최초 (관리자 1, 크리에이터 2, 후원자 프로필)
 *   2 - 후원자 웹 계정 추가 및 샘플 후원 이력 추가
 *   3 - 브랜드명 변경을 기존 데이터에 반영
 *   4 - 크리에이터 프로필 캐릭터(아바타) 이미지 적용
 *   5 - MO 수신번호 050(0505) 체계 전환
 *   6 - 샘플 후원이 실제 MO 번호를 쓰도록 수정(옛 번호 실패분 정리)
 *   7 - 테스트 크리에이터의 잘못된 전체 캐릭터 시트 URL 제거
 *   8 - 문자페이 브랜드 전환(계정 도메인 munjapay.kr, 가맹점 코드 MJP-)
 *   9 - 메시지페이 브랜드 전환(계정 도메인 messagepay.kr, 가맹점 코드 MSG-)
 *  10 - 판매 관리 개편: 옵션 새 형식(값별 추가금·품절), 컨텐츠 상품, 고시정보, 반품 정책
 */
export const SEED_VERSION = 10;
export const SEED_VERSION_KEY = 'seed.version';
