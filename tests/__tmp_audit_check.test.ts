import { describe, it } from 'vitest';

/**
 * ⚠ 이 파일은 지워도 됩니다 (삭제 대기).
 *
 * 2026-09-04 점검 수정 작업 중 임시로 만들어진 파일인데, 작업 환경에서 파일 삭제가
 * 허용되지 않아 그대로 남았습니다. 실제 검증 내용은
 * `tests/audit-2026-09-04-fixes.test.ts` 로 옮겼습니다.
 *
 * vitest 는 테스트가 하나도 없는 파일을 오류로 처리하므로, 지우기 전까지
 * 건너뛰는 테스트 하나만 남겨 둡니다.
 */
describe('(삭제 대기) 임시 파일', () => {
  it.skip('내용은 audit-2026-09-04-fixes.test.ts 로 이동했습니다', () => {});
});
